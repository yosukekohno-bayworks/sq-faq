---
title: 10. 価格・販売制御
area: 10
wbs_status: 完成寄り 要確認
last_verified: 2026-06-20
---

# 10. 価格・販売制御

> このページはWBS-25エリアの第10エリアです。販売価格、予約販売、販売上限、販売閾値という4つの「ルール」をどう設定し、価格と販売可能数量をどう制御するかを理解するのが目標です。

## このエリアで学べること

- 販売設定メニューの4つのルール（販売価格・予約販売・販売上限・販売閾値）の違いを説明できる
- 販売価格ルールを作成し、SKUごとに通常価格・セール価格を登録できる
- 予約販売ルールで在庫0でも注文を受け付ける設定ができる
- 販売閾値ルールでSKUごと、またはブランドコード単位で閾値を設定できる
- 販売上限ルールがチャネル連携を前提とした機能であることを理解できる

---

## 機能概要

「販売設定」は、左メニュー「商品・在庫・注文」グループに属するメニューです。価格と販売可能数量をコントロールする **4種類のルール** を管理します。いずれも「ルールを作成 → 対象のSKUをルールに登録」という共通の操作フローになります。

| サブメニュー | URL | 役割 |
|:--|:--|:--|
| 販売価格 | `/admin/product_price_rules` | SKUごとに通常価格・セール価格を登録する |
| 予約販売 | `/admin/inventory_back_order_rules` | 在庫が0でも注文を受け付ける（バックオーダー） |
| 販売上限 | `/admin/inventory_sale_limit_rules` | 販売チャネルごとに販売数量の上限を設ける |
| 販売閾値 | `/admin/inventory_threshold_rules` | SKUごとに在庫の販売閾値（しきいち）を設定する |

> **共通の設計**: 各機能は「ルール」という単位で管理します。ルールを作成した上で、そのルールに対象のSKU（商品バリエーション）を登録していく形です。

---

## 画面・項目の説明

### 販売価格ルールの作成フォーム

> URL: `/admin/product_price_rules/create`

![販売価格ルール作成フォーム](https://raw.githubusercontent.com/yosukekohno-bayworks/sq-faq/main/_analysis/screenshots/08-price-rule-create.png)

| 項目（UIラベル） | 説明 | 必須 | 制約・選択肢 |
|:--|:--|:--|:--|
| ルール名 * | ルールを識別する名前 | 必須 | テキスト入力。プレースホルダー「年末セール価格」 |
| 通貨 * | このルールで使用する通貨 | 必須 | 米ドル / ユーロ / 日本円 / タイ バーツ / シンガポール ドル。デフォルト: 日本円 |

### 販売価格ルールの詳細画面

> URL: `/admin/product_price_rules/[id]`

![販売価格ルール詳細画面](https://raw.githubusercontent.com/yosukekohno-bayworks/sq-faq/main/_analysis/screenshots/08-price-rule-detail.png)

| 要素（UIラベル） | 説明 |
|:--|:--|
| ページタイトル | 「販売価格ルール」 |
| バッジ | 「通貨: 日本円」など、設定した通貨の名称が常時表示 |
| 通常価格（カード） | 「通常価格を設定することができます。」通常価格SKU一覧へ遷移 |
| セール価格（カード） | 「セール価格を設定することができます。」セール価格SKU一覧へ遷移 |
| ルールを編集 | 編集モーダルを開く。ルール名は変更可能、通貨は読み取り専用（「通貨は編集できません」） |

### 通常価格の登録フォーム

> URL: `/admin/product_price_rules/[id]/product_variant_regulars`（一覧）→ 作成フォーム

![通常価格作成フォーム](https://raw.githubusercontent.com/yosukekohno-bayworks/sq-faq/main/_analysis/screenshots/08-price-rule-regular-create.png)

| 項目（UIラベル） | 説明 | 必須 | 制約・選択肢 |
|:--|:--|:--|:--|
| 商品バリエーションを選択する * | 価格を登録するSKU | 必須 | テキスト + 「選択」ボタン。プレースホルダー「選択してください」 |
| 価格 * | 税抜き価格 | 必須 | 数値入力。通貨記号「￥」（全角）付き |
| 価格(税込) | 税込み価格 | 任意 | 数値入力。通貨記号「￥」（全角）付き |

### セール価格の登録フォーム

> URL: `/admin/product_price_rules/[id]/product_variant_sales`（一覧）→ 作成フォーム

![セール価格作成フォーム](https://raw.githubusercontent.com/yosukekohno-bayworks/sq-faq/main/_analysis/screenshots/08-price-rule-sale-create.png)

| 項目（UIラベル） | 説明 | 必須 | 制約・選択肢 |
|:--|:--|:--|:--|
| 商品バリエーションを選択する * | 価格を登録するSKU | 必須 | テキスト + 「選択」ボタン |
| 価格 * | セール時の税抜き価格 | 必須 | 数値入力。通貨記号「￥」（全角）付き |
| 価格（税込） | セール時の税込み価格 | 任意 | 数値入力。通貨記号「￥」（全角）付き |
| 開始日時 * | セール開始の日時 | 必須 | 日時入力 |
| 終了日時 | セール終了の日時 | 任意 | 日時入力 |

> 通常価格との違いは「開始日時（必須）」「終了日時（任意）」の期間設定フィールドがある点です。セール価格はCSVインポート（`/admin/csv_import/csv_import_operation_product_price_rule_sale_prices`）で一括登録もできます。

### 予約販売ルールの作成フォーム

> URL: `/admin/inventory_back_order_rules/create`（ページタイトル: 「予約販売のルールを作成する」）

![予約販売のルール作成フォーム](https://raw.githubusercontent.com/yosukekohno-bayworks/sq-faq/main/_analysis/screenshots/08-back-order-rule-create.png)

| 項目（UIラベル） | 説明 | 必須 | 制約・選択肢 |
|:--|:--|:--|:--|
| タイトル * | ルールを識別する名前 | 必須 | テキスト入力。プレースホルダー「2025SS先行販売」 |

### 予約販売ルールの詳細画面

> URL: `/admin/inventory_back_order_rules/[id]`

| 要素（UIラベル） | 説明 |
|:--|:--|
| ページタイトル（H1） | ルール名がそのまま表示される |
| ルールを編集する | 編集モーダルを開き、タイトルを変更できる |
| インポート | CSVでSKUを一括登録する画面へ遷移 |
| バリエーションを追加する | SKUを1件ずつ登録するフォームへ遷移 |
| SKUコードで検索する | 登録済みSKUを絞り込む |

### 予約販売のバリエーション追加フォーム

> URL: `/admin/inventory_back_order_rules/[id]/create`（ページタイトル: 「バリエーションを追加する」）

![バリエーションを追加するフォーム](https://raw.githubusercontent.com/yosukekohno-bayworks/sq-faq/main/_analysis/screenshots/audit-sales-settings-back-order-variant-add-form-2026-06-07.png)

| 項目（UIラベル） | 説明 | 必須 | 制約・選択肢 |
|:--|:--|:--|:--|
| 商品バリエーション * | 予約販売するSKU | 必須 | テキスト + 「選択」ボタン。プレースホルダー「選択してください」 |
| 販売数 * | 予約販売を受け付ける数量 | 必須 | 数値入力（spinbutton）。初期値「0」 |

### 販売上限ルールの作成フォーム

> URL: `/admin/inventory_sale_limit_rules/create`（ページタイトル: 「販売上限ルールを作成する」）

![販売上限ルール作成フォーム](https://raw.githubusercontent.com/yosukekohno-bayworks/sq-faq/main/_analysis/screenshots/08-sale-limit-rule-create.png)

| 項目（UIラベル） | 説明 | 必須 | 制約・選択肢 |
|:--|:--|:--|:--|
| 販売上限ルール名 * | ルールを識別する名前 | 必須 | テキスト入力 |
| チャネル | 対象の販売チャネル | 必須（1件以上） | チェックボックスリスト。Shopifyなど販売チャネルの統合設定が完了している場合に選択肢が表示される |

### 販売閾値ルールの作成フォーム

> URL: `/admin/inventory_threshold_rules/create`（ページタイトル: 「販売閾値ルールを作成する」）

![販売閾値ルール作成フォーム](https://raw.githubusercontent.com/yosukekohno-bayworks/sq-faq/main/_analysis/screenshots/08-threshold-rule-create.png)

| 項目（UIラベル） | 説明 | 必須 | 制約・選択肢 |
|:--|:--|:--|:--|
| ルール名 * | ルールを識別する名前 | 必須 | テキスト入力 |
| デフォルトの閾値を設定する | デフォルト閾値を有効にする | 任意 | チェックボックス。ONで「デフォルトの閾値 *」欄が展開される |
| デフォルトの閾値 * | 全バリエーションに適用する閾値の数値 | 必須（チェックON時） | 数値入力。初期値「0」 |

> 画面の説明文（原文）: 「この設定を有効にすることで、閾値ルールに含まれていないすべての商品バリエーションにデフォルト閾値を適用することができます。」

### 販売閾値ルールの詳細画面

> URL: `/admin/inventory_threshold_rules/[id]`

| 要素（UIラベル） | 説明 |
|:--|:--|
| 自動追加ルール | ブランドコードを条件にSKUを自動追加する設定画面へ遷移 |
| ルールを編集する | 編集モーダルを開き、ルール名やデフォルト閾値を変更できる |
| 閾値を追加する | SKUを1件ずつ手動登録するフォームへ遷移 |
| SKUコードで検索する | 登録済みSKUを絞り込む |
| バッジ | デフォルト閾値が有効な場合「デフォルト閾値が有効」を表示 |

### 販売閾値の追加フォーム

> URL: `/admin/inventory_threshold_rules/[id]/create`

| 項目（UIラベル） | 説明 | 必須 | 制約・選択肢 |
|:--|:--|:--|:--|
| 商品バリエーションを選択する * | 閾値を設定するSKU | 必須 | テキスト + 「選択」ボタン |
| 閾値 * | SKUに設定する閾値の数値 | 必須 | 数値入力（spinbutton）。初期値「0」 |

### 自動追加ルールの作成フォーム

> URL: `/admin/inventory_threshold_rules/[id]/automatic_add_rules`（一覧）→ 作成フォーム（H1: 「自動追加ルールを追加する」）

![自動追加ルール作成フォーム](https://raw.githubusercontent.com/yosukekohno-bayworks/sq-faq/main/_analysis/screenshots/gap-sales-settings-threshold-auto-add-rule.png)

| 項目（UIラベル） | 説明 | 必須 | 制約・選択肢 |
|:--|:--|:--|:--|
| 項目 | 自動追加の対象とする属性 | 必須 | コンボボックス。選択肢: 「ブランドコード」のみ |
| 条件 | 一致の条件 | 必須 | コンボボックス。選択肢: 「一致する」のみ |
| 値 | 照合する値 | 必須 | テキスト入力 |
| 閾値 | 自動追加されたSKUに設定する閾値 | 必須（初期値あり） | 数値入力（spinbutton）。初期値「1」 |

---

## 主な操作手順

### 販売価格を設定する

1. 左メニュー「販売設定」>「販売価格」をクリックし、販売価格一覧画面を開く。
2. 「販売価格ルールを作成する」ボタンをクリックし、作成フォームへ遷移する。
3. 「ルール名」に管理用の名前を入力する（必須）。
4. 「通貨」を選択する（必須）。デフォルトは「日本円」。
5. 「保存する」をクリックする。ルールの詳細画面へ遷移する。
6. 詳細画面で「通常価格」カードをクリックし、通常価格SKU一覧へ遷移する。
7. 「通常価格を登録する」をクリックし、作成フォームへ進む。
8. 「商品バリエーションを選択する」の「選択」ボタンから対象SKUを選ぶ。
9. 「価格」に税抜き価格を入力し（必須）、「価格(税込)」に税込み価格を入力する（任意）。
10. 「保存する」をクリックする。他のSKUも登録する場合は手順7〜10を繰り返す。

> **セール価格を登録する場合**: 詳細画面の「セール価格」カードから同様に進めます。セール価格には「開始日時（必須）」「終了日時（任意）」があります。CSV一括登録はセール価格のみ対応（「インポート」ボタン）。

### 予約販売を設定する

1. 左メニュー「販売設定」>「予約販売」をクリックし、予約販売一覧画面を開く。
2. 「予約販売ルールを作成する」ボタンをクリックし、作成フォームへ遷移する。
3. 「タイトル」にルールの管理名を入力する（必須）。
4. 「保存する」をクリックする。ルールの詳細画面へ遷移する。
5. 「バリエーションを追加する」をクリックし、登録フォームへ進む。
6. 「商品バリエーション」の「選択」ボタンから対象SKUを選ぶ（必須）。
7. 「販売数」に予約販売を受け付ける数量を入力する（必須）。初期値は「0」。
8. 「保存する」をクリックする。

> CSVで一括追加する場合は、詳細画面の「インポート」ボタンから（`/admin/csv_import/csv_import_operation_inventory_back_order_rule_product_variants`）。

### 販売閾値を設定する

1. 左メニュー「販売設定」>「販売閾値」をクリックし、販売閾値一覧画面を開く。
2. 「販売閾値ルールを作成する」ボタンをクリックし、作成フォームへ遷移する。
3. 「ルール名」を入力する（必須）。
4. 全SKUにデフォルト閾値を適用する場合は「デフォルトの閾値を設定する」をONにし、「デフォルトの閾値」を入力する（任意）。
5. 「保存する」をクリックする。販売閾値一覧画面へ遷移する（※詳細画面ではない）。
6. 一覧でルール名をクリックし、詳細画面を開く。
7. 「閾値を追加する」をクリックし、登録フォームへ進む。
8. 「商品バリエーションを選択する」から対象SKUを選び、「閾値」を入力して「保存する」をクリックする。

> ブランドコード単位で一括追加する場合は、詳細画面の「自動追加ルール」から（「項目: ブランドコード」「条件: 一致する」「値: ブランドコード」「閾値: 数値」）。

### 販売上限を設定する（チャネル連携が必要）

1. 左メニュー「販売設定」>「販売上限」をクリックし、販売上限一覧画面を開く。
2. 「販売上限ルールを作成する」ボタンをクリックし、作成フォームへ遷移する。
3. 「販売上限ルール名」を入力する（必須）。
4. 「チャネル」のチェックボックスリストから対象チャネルを1件以上選択する（必須）。
5. 「保存する」をクリックする。

> **前提**: 販売チャネル（Shopifyなど）の統合設定が完了している環境でのみチャネル選択肢が表示されます。未接続環境では「チャネルを選択してください」エラーになり作成できません。詳細画面以降のSKU別上限設定は未確認です。

---

## 注意点・制約

- **通貨は作成後に変更不可**: 販売価格ルールの通貨は作成後に変更できません。「ルールを編集」モーダルでも読み取り専用で「通貨は編集できません」と注記されます。別の通貨が必要な場合は新しいルールを作成してください。（出典: constraints.md「仕様（変更不可）」）
- **通貨の表記揺れ**: 詳細画面のバッジは「日本円」（和名）、編集モーダルは「JPY」（ISO英字コード）で表示されますが、同じ設定を指します。
- **通常価格のCSVインポートは非対応**: 通常価格は1件ずつ手動登録のみです。CSVインポートが使えるのはセール価格のみです。
- **販売上限はチャネル連携前提**: Shopifyなど販売チャネルの統合設定が完了していないとチャネル選択肢が表示されず、ルール作成を完了できません。現環境（チャネル未接続）では詳細画面以降のUIは未確認です。
- **閾値のSKU別削除はリロード後に反映**: 詳細画面で「閾値を削除」を実行した直後は対象行が画面に残ることがあります。詳細を再読み込みすると行は消えます。削除後はリロードして反映を確認してください。（出典: constraints.md「仕様（注意）」）
- **予約販売ルールに期間設定はない**: 予約販売ルールには開始日時・終了日時のフィールドがありません。「販売数」は予約販売が可能な在庫数の固定値として設定します。
- **自動追加ルールの選択肢は限定的**: 「項目」は「ブランドコード」のみ、「条件」は「一致する」のみです。

---

## このエリアの確認状態

| 項目 | 状態 | 根拠 |
|:--|:--|:--|
| 販売価格ルールの作成・通常価格・セール価格登録 | ✅ 確定 | 実機確認済み（2026-06-11） |
| 通貨は作成後変更不可 | ✅ 確定 | 実機確認済み（VERIFY-CHECKLIST グループ25 / constraints.md） |
| 予約販売ルールの作成・バリエーション追加 | ✅ 確定 | 実機確認済み（2026-06-11） |
| 予約販売バリエーションCSVインポート画面 | ✅ 確定 | 実機確認済み（2026-06-19。正しいURL `csv_import_operation_inventory_back_order_rule_product_variants`） |
| 販売閾値ルールの作成・閾値追加・自動追加ルール | ✅ 確定 | 実機確認済み（2026-06-11 / 2026-06-19 TESTルールでSKU閾値追加・削除・リロード確認） |
| 販売閾値SKU別閾値の削除挙動（リロード後に反映） | ✅ 確定 | 実機確認済み（2026-06-19） |
| 販売上限ルール作成フォーム（ルール名・チャネル見出し） | ✅ 確定 | 実機確認済み（2026-06-19。チャネル未接続のため選択肢は表示されず） |
| 販売上限のチャネル選択肢・詳細画面以降 | ⚠️ 連携待ち | チャネル接続済み環境での確認が必要 |
| 販売価格/予約販売/販売上限/販売閾値のチャネル適用 | ⚠️ 連携待ち | Shopify等のチャネル未接続のため未検証 |
| 予約販売設定後の在庫0時の注文受付挙動 | ⚠️ 連携待ち | チャネル接続環境での確認が必要 |
| 閾値に達した場合の実挙動（販売停止・通知など） | ⚠️ 未確認 | UI上に説明文なし |

---

## TODO（未確認・一部確認）

- [ ] **連携待ち**: 販売上限ルールのチャネル選択肢、詳細画面のSKU別上限数・集計開始日時の登録フォーム（チャネル接続済み環境で確認）
- [ ] **連携待ち**: 販売価格・セール価格をShopify等に適用したときの実際の価格変更挙動
- [ ] **連携待ち**: 予約販売ルールを設定したとき、在庫0の商品がチャネル側で注文受付になるか
- [ ] **連携待ち**: 販売上限ルールがチャネル側で実際に販売数量を制限するか
- [ ] **連携待ち**: 販売閾値ルールがチャネル側で実際に販売可否に影響するか
- [ ] **要確認**: 閾値に達した場合の具体的な挙動（販売停止・通知など）。UI上に説明文がない
- [ ] **要確認**: セール価格の「終了日時」を空欄にした場合の動作
- [ ] **要確認**: 各ルールを実際の販売チャネル・注文に適用する全体フロー

---

## 次のエリア

→ [11. 発注・仕入](11-発注・仕入.md)
