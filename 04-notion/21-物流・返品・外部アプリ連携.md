---
title: 21. 物流・返品・外部アプリ連携
area: 21
wbs_status: 連携待ち
last_verified: 2026-07-03
---

# 21. 物流・返品・外部アプリ連携

> このページは標準24分類の第21分類です。出荷業務（ヤマトB2クラウド・DHL）、返品（Recustomer）、倉庫管理システム連携（ロジザードZERO）がSQのどこに紐づき、どのデータに戻るかを理解するのが目標です。これらの外部連携の多くは**接続前提の挙動**であり、Shopifyは接続済みストアで注文・顧客・返金・キャンセル受信まで確認済みです。2026-07-02に未連携商品注文 `#1008` / `#1009` で出荷/返品も試しましたが、SQ側には出荷指示・返品一覧として反映されませんでした。正式連携SKUでの出荷/返品反映は再検証待ちです。その他外部連携は「連携待ち」としています。

> **2026-07-02更新**: Shopify経由の注文流入は `#1008` / `#1009` まで確認済みです。ただし、今回のShopify商品はSQ既存SKUと正式リンクせず、商品行は `新規` でした。`#1008` をShopify側で発送済みにしてもSQ注文は `未出荷` / 出荷指示なし、`#1009-R1` のShopify返品もSQ返品一覧には表示されませんでした。出荷・返品反映の完全検証にはSQからShopifyへ商品同期したSKU、または正式連携済みSKUでの再テストが必要です。

> **2026-07-03 追加確認**: ヤマトB2出荷実績CSVを既存出荷指示 `#IO-1050` で再検証しました。DropZoneアップロード後にCSVインポート詳細は作成されましたが、画面は `CSVのバリデーションを実行しています`、`実行ステータス 未実行`、`検証成功 0件`、`検証失敗 0件` のままで、`実行する` はdisabledでした。2026-06-28の `#IO-1032`、2026-07-01の `IO-1048`、2026-07-02の存在しないお客様管理番号 `TEST_NO_SUCH_IO_20260702_2358` も同じ画面状態です。DHLもCSV保存・詳細作成までは進みましたが、同じく検証中0件のまま実行不可でした。CSV実行・出荷完了遷移は未確認です。

## このエリアで学べること

- ヤマトB2クラウド・DHLの出荷実績CSVを使って、SQの出荷指示を出荷完了にする手順が分かる
- ロジザードZERO（WMS）連携で、SQの移動伝票・出荷指示・入荷指示がどう同期されるかの前提を説明できる
- Recustomer連携で返品・交換を処理するための接続設定項目を説明できる
- 各外部アプリの接続設定画面がSQのどこにあるか（設定＞外部連携グループ）が分かる
- 出荷実績登録が不可逆であり、誤った場合は調整伝票で補正する運用になることを理解する

---

## 用語（このページで使う在庫・ロケーションの前提）

- **ロケーション**: 在庫の置き場所。場所種別は「倉庫」または「店舗」の2種のみ。出荷指示の配送元は原則ロケーション（倉庫）。（詳細は04.基本マスタ）
- **在庫区分**: 在庫は販売可能・引当済み・取置中・積送中・入荷予定など区分別に管理される。このページの「引当ステータス」「販売可能」「取置中」はこの区分の一部。（詳細は12.在庫状態・在庫数）

## 機能概要

このエリアは「外部連携」と「出荷業務」の2領域にまたがります。出荷実績のCSV連携は、ヤマトB2クラウド/DHLとも保存・インポート詳細作成まで実機確認済みです。ただし検証用CSVでは「CSVのバリデーションを実行しています」のまま進まず、実行後の出荷完了遷移、WMS・返品プラットフォームとの実際の同期は、実CSVサンプルまたは外部接続が必要なため連携待ちです。Shopifyについては注文受信と未連携商品での出荷/返品未反映まで確認済みですが、正式連携SKUでの出荷・返品は再検証します。

| 機能 | 画面URL | 役割 | 接続状態 |
|:--|:--|:--|:--|
| 出荷管理 | `/admin/inventory_outbound_orders` | 出荷指示の一覧・出荷実績登録（手動／CSV連携） | ✅ 画面確認済み |
| ヤマトB2クラウド 連携 | `/admin/inventory_outbound_orders` 内「インポート」「条件指定でエクスポート」 | 出荷実績CSVの一括取込・取り込み用CSV生成 | ⚠️ CSV保存・詳細作成まで確認。検証用CSVではバリデーション中表示のまま進まない。実行/出荷完了遷移は未確認 |
| DHL 連携 | `/admin/inventory_outbound_orders` 内「インポート」 | DHL出荷実績CSVの一括取込 | ⚠️ CSV保存・詳細作成まで確認。検証用CSVではバリデーション中表示のまま進まない。実行/出荷完了遷移は未確認 |
| ロジザードZERO 連携 | `/admin/logizard_integrations` | WMS（倉庫管理）連携 ※一般知識・実機画面に役割表記なし | ❌ 連携待ち（接続前提） |
| Recustomer 連携 | `/admin/recustomer_integrations` | Recustomerアカウント接続設定（ストアID・シークレット） | ⚠️ UI確認済み・返品フローは外部連携待ち |

### できること（実機確認済みの範囲）

- 出荷管理画面からヤマトB2クラウド／DHLの出荷実績CSVインポート一覧・アップロードフォームを開ける
- ヤマトB2クラウド出荷実績CSVは、CSV選択後に保存するとインポート詳細が作成される
- ヤマトB2クラウド取り込み用CSVの条件指定フォームを開き、日時・配送先国・決済方法・ステータス等の条件を指定できる
- ロジザードZERO連携設定フォームで、グループ番号・接続番号・認証情報・ファイルID等の入力項目を確認できる
- Recustomer連携フォームで、ストアID・シークレットの入力項目を確認できる

### SQのどこに戻るか（データ連携の帰着）

| 外部アプリ | SQ側の反映先 | 確認状態 |
|:--|:--|:--|
| ヤマトB2クラウド（出荷実績CSV取込） | 出荷指示のステータスを「出荷完了」、出荷実績（配送キャリア・追跡コード）を登録 | ⚠️ CSV保存・詳細作成まで確認。検証用CSVではバリデーション中表示のまま進まない。実行/出荷完了遷移は未確認 |
| DHL（出荷実績CSV取込） | 同上 | ⚠️ CSV保存・詳細作成まで確認。検証用CSVではバリデーション中表示のまま進まない |
| ロジザードZERO | 移動伝票・出荷指示・入荷指示との同期（入荷予定登録・出荷予定登録・出荷箱明細実績エクスポート） | ❌ 未確認 |
| Recustomer | 返品・交換に伴う在庫・注文ステータスの反映先 | ❌ 外部接続後は未確認（staging検証用データでは返品・交換返品の流れを確認済み） |

> SQ側から外部アプリへの出力: ヤマトB2クラウド向けに「取り込み用CSV」を条件指定でエクスポート可能（DHL向けエクスポートはなし）。

> 返品機能については、2026-06-28時点で `/admin/order_returns` の一覧画面は存在しますが、返品0件の空状態では検索・絞り込みUIは表示されません。`/admin/order_returns/create` は「このページは存在しないようです」となり、返品一覧から新規登録する導線は確認できていません。staging検証用データでは、返品一覧のデータあり列と、注文詳細内での `返送待ち` → `返品受取済み` → `返品処理済み`、さらに交換返品の完了後に交換品用の出荷指示が生成されることまで確認済みです。Recustomer経由の返品が同じ流れで反映されるかは接続環境で検証待ちです。

---

## 画面・項目の説明

### 出荷管理（`/admin/inventory_outbound_orders`）

> 出荷指示は移動伝票の作成と同時に自動生成され、出荷管理画面から新規作成するボタンはありません。EC注文の取り寄せ販売フロー等で生じる出荷作業の単位です（実機確認: 出荷管理.md・移動伝票.md）。

出荷管理には「すべて」タブと、6つのステータスタブがあります。

![出荷管理のステータスタブ（保留中・出荷待ち・依頼済み・作業中・欠品・要対応・出荷完了）](https://raw.githubusercontent.com/yosukekohno-bayworks/sq-faq/main/_analysis/screenshots/audit-purchase-ops-06-outbound-tabs.png)

| タブ名 | URLパラメータ | 説明 |
|:--|:--|
| すべて | なし | ステータス横断の一覧。2026-06-27再確認では `キャンセル済み` の出荷指示もここに表示された |
| 保留中 | `?tab=on_hold` | 出荷作業を保留している出荷指示 |
| 出荷待ち | `?tab=waiting` | 出荷作業を待っている出荷指示 |
| 依頼済み | `?tab=requested` | 出荷依頼を済ませた出荷指示 |
| 作業中 | `?tab=in_progress` | 出荷作業中の出荷指示 |
| 欠品・要対応 | `?tab=rejected` | 欠品など対応が必要な出荷指示 |
| 出荷完了 | `?tab=complete` | 出荷実績が登録された出荷指示 |

キャンセル専用タブはありません。2026-06-27の再確認では、キャンセル済みの出荷指示（例: `#IO-1015`, `#IO-1014`, `#IO-1007`）は「すべて」タブに `キャンセル済み` として表示され、ステータス別タブには表示されませんでした。関連する移動伝票詳細のリンクや直接URLから詳細を開ける場合があります。

#### 「インポート」ボタン（ドロップダウン）

![「インポート」ボタンのドロップダウン（ヤマトB2クラウド（出荷実績）・DHL（出荷実績））](https://raw.githubusercontent.com/yosukekohno-bayworks/sq-faq/main/_analysis/screenshots/gap-purchase-ops-15-outbound-import-dropdown.png)

| 選択肢（UIラベル） | 遷移先 |
|:--|:--|
| ヤマトB2クラウド（出荷実績） | ヤマトB2クラウドの出荷実績インポート画面 |
| DHL（出荷実績） | DHLの出荷実績インポート画面 |

いずれの画面でも「新規インポート」からCSVファイルをアップロードします。アップロードできるのは**CSVファイルのみ**です。2026-07-02再確認では、ファイル未選択のまま `保存する` を押してもヤマトB2クラウド/DHLとも同じ作成画面に留まり、画面上に赤いエラーメッセージは出ませんでした。2026-07-03にはヤマトB2/DHLともCSV保存・詳細作成までは進みましたが、検証中0件のまま `実行する` はdisabledでした。

#### ヤマトB2クラウドCSVフォーマット

- 全10列固定
- 必須列は「伝票番号」「出荷日」「お客様管理番号」の3列のみ
- 列の詳細はインポート一覧画面内「テンプレート」のリンクから確認できます。2026-07-02にテンプレートの「定義書」「フォーマット」タブをCSV取得し、任意7列は処理で読み捨てる旨も確認しました
- DHLインポート一覧・新規インポート作成フォームでは、2026-07-03再確認時点でも「テンプレート」リンク、列数、必須列、列名説明は表示されませんでした。DHLの正確なCSVフォーマットは実CSVサンプルまたは開発元確認が必要です

![ヤマトB2クラウド インポート画面](https://raw.githubusercontent.com/yosukekohno-bayworks/sq-faq/main/_analysis/screenshots/gap-purchase-ops-16-yamato-import-form.png)

#### 「条件指定でエクスポート」ボタン（ドロップダウン）

クリックすると「ヤマトB2クラウド」の選択肢のみ表示されます（**DHLのエクスポートはありません**）。

![「条件指定でエクスポート」ドロップダウン（ヤマトB2クラウドのみ）](https://raw.githubusercontent.com/yosukekohno-bayworks/sq-faq/main/_analysis/screenshots/gap-purchase-ops-18-outbound-export-dropdown.png)

##### エクスポートフォームの項目

![ヤマトB2クラウド エクスポートフォーム](https://raw.githubusercontent.com/yosukekohno-bayworks/sq-faq/main/_analysis/screenshots/gap-outbound-yamato-export-form.png)

| 項目（UIラベル） | 必須 | 説明・選択肢 |
|:--|:--|:--|
| 開始日時 | 必須（*） | 出荷指示の絞り込み開始日時 |
| 終了日時 | 必須（*） | 出荷指示の絞り込み終了日時（デフォルト: 現在日時） |
| 配送先 (国) | 任意 | 国を選択して絞り込む（マルチ選択可） |
| 決済方法 | 任意 | すべての決済 / 代引きのみ / 代引き以外の決済。代引きは受領時に代金回収を行うためヤマトB2クラウド側で送り状の支払い情報等が異なり別扱いになるケースがある。代引きのみ／代引き以外でCSVを分けて出力したい場合に使用する。通常は「すべての決済」でよい（※代引き絞り込みの運用理由は実機未確認の推定） |
| 出荷作業ステータス | 任意 | 指定しない / 出荷待ち / 保留中 / 依頼済み / 作業中 / 欠品・要対応 / キャンセル済み / 出荷完了 |
| 注文タグ（含む） | 任意 | 入力したタグを含む出荷指示のみが対象（例: priority） |
| 注文タグ（除外） | 任意 | 入力したタグを含まない出荷指示のみが対象（例: test） |
| CSVの出力後に出荷指示のステータスを出荷作業中に変更する | 任意 | チェックボックス（デフォルト: 未チェック） |

「実行する」ボタンを押すと非同期処理が開始されます。画面上の出力結果は「ヤマトB2クラウド取り込み用CSV」と「同梱する納品書PDF」です。ステータス変更チェックはデフォルトOFFです（2026-07-02再確認）。ONにするとラベル上は対象出荷指示を「出荷作業中」に変更する設定ですが、2026-06-28追加確認では、検証用 `#IO-1029`（移動伝票由来・配送希望日なし）を開始 `2026-06-01T00:00` / 終了 `2026-06-28T06:59` / 出荷作業ステータス `出荷待ち` / ステータス変更ONで実行しても、出荷作業中へ移らず `出荷待ち` タブに残りました。2026-07-02には成功済みの23:34履歴と23:35完了メールからCSV実ファイルを確認しましたが、メール内に同梱PDFリンクはありませんでした。同日、新規に条件指定エクスポートを実行した際は開始トーストのみ確認でき、新しい履歴行・メールは確認できませんでした。

#### 出荷実績の個別登録（出荷指示詳細）

出荷指示の詳細画面右上「出荷実績を登録する」ボタンからダイアログを開きます。

![「出荷実績を登録する」ダイアログ（配送キャリア・追跡コード）](https://raw.githubusercontent.com/yosukekohno-bayworks/sq-faq/main/_analysis/screenshots/flow-06-outbound-shipment-dialog.png)

| 項目（UIラベル） | 必須 | 説明 |
|:--|:--|:--|
| 配送キャリア | 任意 | 配送業者名（ヤマト・DHL等）。空でも登録可 |
| 追跡コード | 任意 | 荷物追跡番号（送り状番号）。空でも登録可 |

「登録する」を押すと出荷実績が記録され、ステータスが「出荷完了」に変わります。

> **表示上の注意**: 配送キャリア・追跡コードは入力できますが、2026-06-28の `#IO-1026` では出荷完了後の出荷指示詳細・「すべて」一覧・「出荷完了」一覧に入力値が表示されませんでした。外部通知やチャネル連携へ実際に引き継がれるかは接続前提のため未確認です。画面上で後から確認できる前提では運用しないでください。

### ロジザードZERO連携（`/admin/logizard_integrations`）

> **公式ガイド上の位置づけ**: ロジザードZEROは[公式ガイド](https://docs.sqstack.com/docs/guide/app-integrations)の全12ページに記載がありません（標準連携サービスは Shopify / スマレジ / OmnibusCore / Recustomer の4つのみ）。ただし **SQアプリの連携UIは実機に存在**し、設定フォームが入力可能です。本項は実機で確認できた事実として記載しますが、公式には未公開（ベンダー固有・非公開扱いの可能性）である点にご注意ください。

設定トップ > 外部連携グループ > ロジザード から遷移します。画面説明文（UIの原文）: 「ロジザードZEROとの連携設定を管理することができます」。

#### ロジザードZEROの基本

ロジザードZEROはWMS（倉庫管理システム）です。SQ側では、倉庫で発生する在庫・入荷・出荷の実績をSQの在庫管理や出荷管理に戻すための外部連携として扱います。販売チャネル（Shopify / OmnibusCore / スマレジ）のように商品を販売する場所ではなく、**倉庫作業を管理する外部システム**です。

MK Backlog資料とSQ実機フォームから整理すると、基本の連携方向は次の4つです。MK固有の倉庫名・B2B運用・モール別帳票などは顧客別運用のため、ここでは標準理解から除外します。

| 方向 | 主なデータ | SQ側の関係画面・データ |
|:--|:--|:--|
| ロジザードZERO → SQ | 在庫同期 | 在庫管理。ロジザード側の倉庫在庫をSQのロケーション在庫に反映する |
| SQ → ロジザードZERO | 出荷予定 / 出荷指示 | 出荷管理。SQの出荷指示をロジザード側の倉庫作業に渡す |
| SQ → ロジザードZERO | 入荷予定 | 入荷管理・移動伝票。店舗/倉庫からロジザード倉庫へ商品が届く予定を渡す |
| ロジザードZERO → SQ | 出荷実績 / 入荷実績 / 出荷箱明細実績 | 出荷完了、入荷完了、送り状番号・梱包番号などの実績反映 |

商品を突き合わせるキーは、作成フォームの「商品バリエーションを特定するキー」で選びます。選択肢は **SKUコード / JANコード / EANコード / UPCコード** です。SQとロジザードZEROの双方で同じ商品を同じキーで管理していないと、在庫・出荷・入荷の実績を正しく紐づけられません。

ロジザード連携を動かすには、SQ側のフォームを保存するだけでなく、ロジザードZERO側で発行・設定された接続情報、認証情報、入荷予定登録・出荷予定登録・出荷箱明細実績エクスポート用のファイルID/パターンIDが必要です。2026-07-03時点のstagingでは、フォーム項目と空保存エラーは確認済みですが、接続後に実際の在庫・入荷・出荷が同期されるところまでは未確認です。

#### ロジザード連携作成フォーム（`/admin/logizard_integrations/create`）

![ロジザード連携作成フォーム](https://raw.githubusercontent.com/yosukekohno-bayworks/sq-faq/main/_analysis/screenshots/14-logizard-create-full.png)

フォームは以下の6セクションで構成されます。

| セクション | 主な入力項目（必須） |
|:--|:--|
| 設定名 | 設定名 * |
| 接続情報 | グループ番号 * / 接続番号 * |
| 認証情報 | オーナーID * / ユーザーID * / パスワード * / アプリケーションキー * |
| 入荷設定 | 入荷予定登録ファイルID * / 入荷予定登録パターンID * |
| 出荷設定 | 出荷予定登録(通販)ファイルID * / 出荷予定登録(通販)パターンID * / 出荷予定登録(卸)ファイルID * / 出荷予定登録(卸)パターンID * |
| 出荷箱明細実績エクスポート設定 | 出荷箱明細実績エクスポートファイルID * / 出荷箱明細実績エクスポートパターンID * |

> **各項目の値の取得方法**: グループ番号・接続番号・認証情報・ファイルID・パターンIDは、いずれもロジザードZERO管理画面で事前に確認・取得した値を入力します。各値の具体的な意味（APIエンドポイントの構成要素・認証の役割・取込定義との対応等）は実機未確認のため、接続環境での検証後に追記します。

- 接続情報には任意チェックボックス `Partner APIエンドポイントを使用する` がある。
- 認証情報には任意チェックボックス `AuthKeyの発行をスキップする` がある。画面ヒントは「すでにAuthKeyを発行済みの場合に有効にしてください。」。
- 「商品バリエーションを特定するキー *」: コンボボックス（**SKUコード（デフォルト）/ JANコード / EANコード / UPCコード**）。SQとロジザードZEROで商品バリエーションを突き合わせるためのキーで、両システムで同一のコード体系を選ぶ必要がある（例: JANコードで管理している場合はJANコードを選択）。

### Recustomer連携（`/admin/recustomer_integrations`）

設定トップ > 外部連携グループ > Recustomer から遷移します。2026-06-28再確認では、一覧見出しは `Recustomer`、一覧アクションは `アカウントを接続`、連携待ちの未接続時は `アイテムが見つかりませんでした` でした。

#### Recustomer接続フォーム（`/admin/recustomer_integrations/create`）

![Recustomer接続フォーム](https://raw.githubusercontent.com/yosukekohno-bayworks/sq-faq/main/_analysis/screenshots/14-recustomer-create.png)

| 項目（UIラベル） | 必須 | 補足 |
|:--|:--|:--|
| ストアID * | 必須 | Recustomer側で発行されるストアID。Recustomer管理画面で取得する |
| シークレット * | 必須 | Recustomer側で発行されるAPIシークレットキー |

- 「アカウントを接続」リンクから接続フォームへ遷移します
- Recustomer側のストアIDとシークレットを事前に取得する必要があります
- 2026-06-28再確認では、空保存で `ストアIDを入力してください` / `シークレットを入力してください` が表示されます
- **運用上の意味**: 接続すると返品・交換の発生がSQに通知され、在庫・注文ステータスに反映される想定。ただし反映先の具体的動作は接続環境での検証待ちであり、本ページでは接続設定までを案内する（実機未確認）。
- **接続済みデータの削除（2026-06-24実機確認）**: Recustomer連携一覧（`/admin/recustomer_integrations`）で登録済み行のチェックボックスを選択すると、上部に `アクション` メニューが表示される。`接続を削除する` を選ぶと確認ダイアログ `アカウント接続を削除します` が開き、`削除する` で削除できる。確認文言は「選択されているアカウント接続を削除します。この処理は巻き戻すことができません。」。テスト接続 `TEST_FAQ_20260624_Recustomer_100908` はこの手順で削除済み。

---

## 主な操作手順

### 手順1: ヤマトB2クラウドの出荷実績CSVをインポートする

1. 左サイドバー「出荷管理」（`/admin/inventory_outbound_orders`）を開く
2. 一覧右上の「インポート」ボタンをクリックする
3. ドロップダウンから「ヤマトB2クラウド（出荷実績）」を選択する
4. インポート画面で「新規インポート」をクリックする
5. 「テンプレート」リンクからCSVフォーマット（全10列・必須3列）を確認する
6. ヤマトB2クラウド側で出力したCSVファイルを選択し、アップロードする（CSVファイルのみ）
7. 検証成功後に「実行する」を押すと出荷実績が一括登録され、対象出荷指示が「出荷完了」になる想定

2026-06-28の検証用CSVでは、`#IO-1032` を `お客様管理番号` に入れたCSVを保存し、インポート詳細作成までは確認しました。2026-07-01には新規の出荷待ち `#IO-1048` を作成し、`お客様管理番号=IO-1048`（#なし）のCSVをDropZone経由でアップロードしました。2026-07-02には存在しないお客様管理番号 `TEST_NO_SUCH_IO_20260702_2358`、2026-07-03には既存出荷指示 `#IO-1050` のCSVを保存しました。いずれも画面上は「CSVのバリデーションを実行しています」、検証成功0件/検証失敗0件のまま進まないため、実行・出荷完了遷移は確認できていません。

> 「実行する」は取り消し不可の操作です。確認ダイアログに「この操作は巻き戻すことができません。」と表示されます。

### 手順2: DHLの出荷実績CSVをインポートする

1. 左サイドバー「出荷管理」を開く
2. 一覧右上の「インポート」ボタンをクリックする
3. ドロップダウンから「DHL（出荷実績）」を選択する
4. インポート画面で「新規インポート」をクリックする
5. DHL側で出力したCSVファイルを選択し、アップロードする（CSVファイルのみ）

2026-07-03再確認では、DHLの新規インポート作成フォームは `CSVファイル` のアップロード欄と `保存する` のみで、テンプレート・列数・必須列の説明は表示されませんでした。ファイル未選択で保存すると同じ作成画面に留まり、画面上に赤いエラーメッセージは出ませんでした。CSVをアップロードすると詳細作成までは進みましたが、画面上は「CSVのバリデーションを実行しています」、検証成功0件/検証失敗0件、`実行する` disabledのままでした。
6. 検証成功後に「実行する」を押す

### 手順3: ヤマトB2クラウド取り込み用CSVを条件指定で生成する

1. 左サイドバー「出荷管理」を開く
2. 一覧右上の「条件指定でエクスポート」ボタンをクリックする
3. ドロップダウンから「ヤマトB2クラウド」を選択する（DHLは選択肢にありません）
4. エクスポートフォームで以下を設定する:
   - 開始日時・終了日時（必須）
   - 必要に応じて配送先(国)・決済方法・出荷作業ステータス・注文タグ（含む/除外）を指定
5. 「CSVの出力後に出荷指示のステータスを出荷作業中に変更する」にチェックを入れるか判断する（デフォルト: 未チェック。OFFのまま実行すると対象出荷指示は「出荷待ち」のまま）
6. 「実行する」を押すと非同期処理が開始される
7. 完了後に「ヤマトB2クラウド取り込み用CSV」をダウンロードする

### 手順4: 出荷実績を個別に（CSVを使わず）登録する

1. 左サイドバー「出荷管理」を開く
2. 該当の出荷指示（保留中／出荷待ち等のタブ）をクリックして詳細画面を開く
3. 詳細画面右上の「出荷実績を登録する」ボタンをクリックする
4. ダイアログで「配送キャリア」「追跡コード」を入力する（いずれも任意・空欄可。完了後の詳細・一覧には入力値が表示されない）
5. 「登録する」を押すと、出荷実績が登録されステータスが「出荷完了」に変わる
6. トースト「出荷を完了しました」が表示されることを確認する

### 手順5: ロジザードZERO連携を登録する

1. 左サイドバー「設定」（`/admin/settings`）を開く
2. 外部連携グループの「ロジザード」（`/admin/logizard_integrations`）をクリックする
3. 「作成する」から作成フォーム（`/admin/logizard_integrations/create`）へ遷移する
4. 以下のセクションを順に入力する:
   - 設定名（必須）
   - 接続情報（グループ番号・接続番号、いずれも必須）
   - 認証情報（オーナーID・ユーザーID・パスワード・アプリケーションキー、いずれも必須）
   - 入荷設定（入荷予定登録ファイルID・パターンID、いずれも必須）
   - 出荷設定（通販・卸それぞれのファイルID・パターンID、いずれも必須）
   - 出荷箱明細実績エクスポート設定（ファイルID・パターンID、いずれも必須）
5. 「商品バリエーションを特定するキー」をSKUコード／JANコード／EANコード／UPCコードから選ぶ（デフォルト: SKUコード）
6. 保存する

> 認証情報・ファイルID・パターンIDはロジザードZERO側の管理画面で事前に確認・取得する必要があります。

### 手順6: Recustomer連携を接続する

1. 左サイドバー「設定」（`/admin/settings`）を開く
2. 外部連携グループの「Recustomer」（`/admin/recustomer_integrations`）をクリックする
3. 「アカウントを接続」リンクから接続フォーム（`/admin/recustomer_integrations/create`）へ遷移する
4. Recustomer側で事前に取得した「ストアID」と「シークレット」を入力する
5. 保存する

### 手順の完了確認

| 手順 | 確認場所 | 期待される結果 |
|:--|:--|:--|
| 手順1・2 完了後 | 出荷管理 一覧 > 出荷完了タブ | 対象出荷指示が「出荷完了」に表示 |
| 手順1・2 完了後 | 出荷指示 詳細 | 「出荷実績を登録する」ボタンが無効化 |
| 手順3 完了後 | 出荷管理 一覧 > 対象タブ | チェックON時はラベル上「作業中」へ変更する設定。ただし2026-06-28追加確認では、検証用 `#IO-1029` はON実行後も `出荷待ち` タブに残った。条件にヒットした実データでの成功例は未確認 |
| 手順4 完了後 | 出荷指示 詳細 > ステータス | 出荷完了 |
| 手順5・6 完了後 | 各連携の詳細画面 | 登録した設定が表示 |

---

## 注意点・制約

### 出荷実績の登録は不可逆（取り消し不可）

出荷実績を登録（手動・CSV問わず）すると**ステータスを巻き戻すことはできません**。誤って登録した場合は、調整伝票で在庫数を修正してください。実績登録は確定操作です（実機確認済み、constraints.md該当項目）。

- 出荷完了後：「出荷実績を登録する」ボタンが無効化され、画面から再登録・編集・削除は不可
- 誤って登録した場合は調整伝票で在庫数を修正する運用になる
- **調整伝票の導線**: 在庫管理 > 調整伝票（`/admin/inventory_adjustment_orders`）→「作成する」で、ロケーション・商品（増減数）を指定して在庫を増減させる。理由は `廃棄 / 紛失 / 在庫差異 / その他` の4択で選択推奨だが、2026-06-27の実機では未選択表示でも保存・実行できた。詳細は 01-by-feature/調整伝票.md 参照。

<!-- 開発元確認: 出荷実績の修正が必要な場合のAPI経由操作可否を開発元に確認 -->

### CSVインポートの「実行する」も不可逆

CSVは保存後に検証され、検証成功後に「実行する」を押すとデータへ反映されます。確認ダイアログには「この操作は巻き戻すことができません。」と表示されます（実機確認済み、constraints.md該当項目）。誤った場合は修正版CSVを新規インポートするか、在庫は調整伝票で補正してください。

### ヤマトB2クラウドエクスポートはデフォルトOFFでステータス変更しない

「CSVの出力後に出荷指示のステータスを出荷作業中に変更する」はデフォルトOFFです。ステータスを進めたい場合にチェックする項目ですが、2026-06-28追加確認では、移動伝票由来・配送希望日なしの検証用出荷指示 `#IO-1029` はON実行後も `出荷待ち` のままでした。出荷作業中へ変わる条件は、この確認範囲では特定できていません。

### 納品書PDFの実ファイル生成は成功データで再検証

2026-06-28の実機確認では、ヤマトB2クラウド条件指定エクスポートフォームに「同梱する納品書PDF」と表示されます。一方、出荷指示詳細 `#IO-1027`、入荷指示詳細 `#II-1029`、移動/調整/取置/発注の各詳細には任意に納品書PDFを新規生成する導線は確認できませんでした。2026-07-03再確認では、出荷一覧で対象行をチェックすると `アクション > 納品書（PDF）` からPDFエクスポートを実行でき、`/admin/pdf_export/pdf_export_operation_packing_slips` に履歴行が作成されました。ただし `#IO-1047` / `#IO-1046` / `#IO-1048` / `#IO-1020` と2026-07-02追加実行の `#IO-1048`、2026-07-03追加実行の `#IO-1045`（キャンセル済み）/ `#IO-1051`（出荷待ち）はいずれも `重大 / 失敗` で、ダウンロードは不可でした。`#IO-1020` は配送・引当済みでも失敗しています。キャンセル済み・出荷待ちでも失敗しており、ステータス差による成功条件は確認できていません。GraphQL上も `isFailedExecutionJob=true`、`downloadURL=null` で、公開フィールドに失敗理由はありません。`/admin/pdf_export/pdf_export_operation_packing_slips/create` は「このページは存在しないようです」になります。2026-07-02のメール確認でもPDF/納品書通知は見つかりませんでした。「PDFが必ず生成される」とは断定しないでください。

### 在庫はマイナスになり得る

配送元ロケーションの表示在庫が0のSKUでも、出荷実績登録はブロックされません。実在庫を超える出荷登録をすると在庫数がマイナスになります（2026-06-18実機確認: `#IM-1016`、constraints.md該当項目）。登録前にSKU・ロケーション・数量を確認してください。

### 出荷指示の引当ステータスは無視してよい

移動伝票経由で生成された出荷指示は、引当ステータスが「引当待ち」のまま推移します（出荷実績登録・出荷完了になっても変わりません）。出荷完了の判断は、出荷実績の登録有無と「出荷完了」ステータスを優先してください（2026-06-20実機確認: `#IO-1018`）。

### 外部連携の実際の同期挙動は未確認

ロジザードZERO・Recustomerの連携設定画面・入力項目は確認できていますが、**接続後の実際の同期挙動（移動伝票・出荷指示・入荷指示・返品・在庫への反映）はstaging環境では未確認**です。本番環境で接続した上での検証が必要です。

### 返品は返品一覧ではなく注文詳細内で扱う

2026-06-28再確認では、`/admin/order_returns` の返品一覧は表示できますが、返品一覧から新規作成するボタンはありません。`/admin/order_returns/create` は「このページは存在しないようです」となります。

一方、stagingに用意した検証用返品データでは、返品一覧に列 `返品 / 返品状況 / 交換出荷状況 / 作成日時` が表示され、注文詳細内の返品ブロックで `返送待ち` → `返品受取済み` → `返品処理済み` まで確認できました。通常返品は交換品の出荷指示を作らず、交換返品は返品数量に加えて交換先SKUと出荷倉庫を指定します。交換返品では、作成直後と受領後は交換出荷指示なし、返品完了後に交換品用の出荷指示 `#IO-1040` が生成されました。2026-07-03時点で `#IO-1040` はキャンセル済みですが、出荷管理に残っており、作成元は `FAQ-REMAINING-EXCHANGE-20260628140117` です。返品単体URL `/admin/order_returns/{id}` は存在しない画面です。Recustomer等の外部連携経由で登録された返品が同じ流れになるかは接続環境で検証待ちです。

---

## このエリアの確認状態

| 項目 | 状態 | 根拠 |
|:--|:--|:--|
| 出荷管理画面の表示・タブ構成 | ✅ 確定 | 2026-06-27再確認。「すべて」+6ステータスタブ。キャンセル済み行は「すべて」に表示 |
| 出荷実績の個別登録（手動） | ✅ 確定 | 2026-06-16/18、2026-06-28 `#IO-1026` 実機確認。配送キャリア・追跡コードは入力可能だが詳細・一覧には表示されない |
| ヤマトB2クラウド「インポート」「条件指定でエクスポート」導線 | ✅ 確定 | 2026-07-02再確認。インポート一覧/作成、エクスポートフォーム項目、決済方法3択、ステータス変更チェック初期OFF、成功履歴/完了メールのCSV実ファイルを確認 |
| DHL「インポート」導線 | ✅ 確定 | 2026-07-03再確認。インポート一覧/作成/空保存時に同画面へ留まることを確認。CSV保存・詳細作成までは進むが、検証中0件で実行不可。テンプレートリンク・列説明は表示なし |
| ヤマトB2クラウドCSVフォーマット（10列・必須3列） | ✅ 確定 | 2026-07-02 テンプレート定義書/フォーマットCSV確認 |
| ヤマトB2クラウドエクスポートのステータス変更チェック（デフォルトOFF） | ✅ 一部確定 | 初期OFFとON送信は確認済み。2026-06-28追加確認では `#IO-1029` が出荷待ちのままで、作業中への成功例は未確認 |
| ロジザードZERO連携 設定画面・入力項目 | ✅ 確定 | 2026-06-27再確認。空保存エラーも確認 |
| Recustomer連携 設定画面・入力項目 | ✅ 確定 | 2026-06-28再確認。一覧/作成フォーム項目を確認。空保存エラーは2026-06-27確認 |
| ヤマトB2クラウド／DHL 出荷実績CSVの実データ取込 | ⚠️ 一部確認 | ヤマトB2/DHLともCSV保存・インポート詳細作成まで確認。検証用CSVではバリデーション中表示のまま進まず、実行/出荷完了遷移は未確認 |
| ロジザードZERO 接続後の移動伝票・出荷・入荷同期 | ❌ 未確認 | 外部接続が必要 |
| Recustomer 接続後の返品・交換フロー | ❌ 未確認 | 外部接続が必要 |
| 返品一覧（`/admin/order_returns`） | ✅ 確定 | 2026-06-28再確認。空一覧を表示 |
| 返品一覧のデータあり列 | ✅ 確定 | staging検証用返品で `返品 / 返品状況 / 交換出荷状況 / 作成日時` を確認 |
| 注文詳細内の返品受領・完了 | ✅ 確定 | `返送待ち` → `返品受取済み` → `返品処理済み` を確認 |
| 交換返品フロー | ✅ 一部確定 | 交換返品作成→受領→完了で交換品用の出荷指示 `#IO-1040` が生成 |
| 返品新規作成導線（`/admin/order_returns/create`） | ✅ 確定（導線なし） | 2026-06-28再確認で「このページは存在しないようです」。一覧にも作成ボタンなし |
| 返品単体詳細URL（`/admin/order_returns/{id}`） | ✅ 確定（画面なし） | 検証用返品IDで直接アクセスしても「このページは存在しないようです」 |
| 納品書PDFの生成導線 | ⚠️ 一部確認 | 2026-07-03再確認。出荷一覧で対象行をチェックし、`アクション > 納品書（PDF）` から実行できる。出荷完了/出荷待ち/キャンセル済みの検証ジョブは履歴作成後に `重大 / 失敗` となりダウンロード不可。出荷/入荷/移動/調整/取置/発注詳細にPDF/印刷/納品書生成ボタンなし。ヤマトB2フォームの同梱PDF表示は確認済みだが、成功メール内に同梱PDFリンクはなし。実ファイル生成は成功データで再検証待ち |

---

## 残課題（対象外・データ依存を含む）

- [ ] **実CSVサンプル待ち** — ヤマトB2クラウド出荷実績CSVの実データインポート検証（CSV保存・詳細作成は確認済み。検証用CSVではバリデーション中表示のまま進まない。取込後のステータス遷移・出荷実績反映の完全検証が未実施）
- [ ] **連携待ち** — DHL出荷実績CSVの実データインポート検証（CSV保存・詳細作成は確認済み。フォーマット詳細・取込後の挙動の完全検証が未実施）
- [ ] **連携待ち** — ロジザードZERO接続後の同期挙動（移動伝票・出荷指示・入荷指示がWMS側とどう同期されるか、入荷予定登録・出荷予定登録・出荷箱明細実績エクスポートの実効果）
- [ ] **連携待ち** — Recustomer接続後の返品・交換フロー（返品登録がSQの返品一覧/注文詳細にどう反映されるか、在庫・注文ステータスへの影響）
- [ ] **再検証待ち** — Shopify等のチャネルからの注文流入に伴う出荷指示の自動生成（Shopify注文流入と未連携商品での出荷/返品未反映は確認済み。正式連携SKUでの出荷指示生成・出荷反映・返品反映は未確認）
- [ ] **成功データ待ち** — 納品書PDFの実ファイル生成確認（出荷一覧の一括アクション `納品書（PDF）` で履歴作成までは確認済み。ただし出荷完了/出荷待ち/キャンセル済みの検証ジョブは `重大 / 失敗` でダウンロード不可。ヤマトB2フォームには「同梱する納品書PDF」表示あり、成功メール内に同梱PDFリンクはなし。出荷/入荷/移動/調整/取置/発注詳細には任意新規生成導線なし）
- [x] 返品一覧/直URLからの新規作成導線はなし（`/admin/order_returns` の一覧は存在。`/admin/order_returns/create` は存在しない画面）
- [x] staging検証用返品では、注文詳細内で受領・完了、返品一覧で処理済み反映を確認
- [x] staging検証用交換返品では、完了処理後に交換品用の出荷指示が生成されることを確認
- [ ] **要確認** — 出荷実績の修正が必要な場合の対処方法（API経由操作可否を開発元に確認）
- [ ] **要確認** — ロジザードZERO連携の詳細設定（ロケーションのマッピング等、接続済み環境での確認が必要）
- [ ] **要確認** — DHL出荷実績CSVのフォーマット仕様（2026-06-28再確認でも管理画面UIにテンプレートリンク・列数・必須列・列名説明なし。実CSVサンプルまたは開発元確認が必要）
- [ ] **完成寄り** — ヤマトB2クラウドエクスポートフォームの全項目（実機で全項目確認済み、実データ出力の検証が残る程度）

---

## 次のエリア

→ [22. CSV・PDF・データ移行](22-CSV・PDF・データ移行.md)
