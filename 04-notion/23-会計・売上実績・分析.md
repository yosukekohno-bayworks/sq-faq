---
title: 会計・売上実績・分析
area: 23
wbs_status: 一部確認 TODO表示
last_verified: 2026-06-20
---

# 23. 会計・売上実績・分析

SQの「会計」と「分析」メニューを取り扱うエリアです。「売上実績」の確認・CSV出力は実装されていますが、「分析（収益・レポート）」は現在 TODO 表示で、機能の中身はまだ実装されていません。このエリアでは、**何が使えて何が未実装か**を整理して説明します。

## このエリアで学べること

- 「会計」メニューで何ができるか（売上実績の確認・CSVエクスポート）
- 売上実績を「注文軸」「明細軸」の2種類でCSV出力する方法
- 「売上実績を作成する」リンクが現在押せない事実と理由
- 「分析」メニュー（収益・レポート）が TODO 表示であり、現時点で機能がないこと
- 注文データが0件の環境で確認できていない項目（集計挙動など）

## 機能概要

| メニュー | 画面 | URL | 状態 |
|:--|:--|:--|:--|
| 会計 > 売上実績 | 売上実績 | `/admin/sale_change_line_items` | 実装済み（一覧・CSVエクスポート） |
| 分析 | 分析（トップ） | `/admin/analytics` | TODO 表示（未実装） |
| 分析 > 収益 | 売上（h1） | `/admin/analytics/revenue` | TODO 表示（未実装） |
| 分析 > レポート | レポート | `/admin/analytics/reports` | TODO 表示（未実装） |

- 「会計」機能は現在「売上実績」1画面のみで構成されています（サブページは1つ）。
- 売上実績は一覧表示とCSVエクスポートができますが、本環境ではデータが0件のため、一覧テーブルの列構成・集計挙動の一部は未確認です。
- 「分析」配下の3画面はいずれも本文が `TODO` で、機能操作は存在しません。

## 画面・項目の説明

### 会計 > 売上実績（`/admin/sale_change_line_items`）

![売上実績の一覧画面（データ0件の空状態。「売上実績を作成する」リンクから遷移できない）](https://raw.githubusercontent.com/yosukekohno-bayworks/sq-faq/main/_analysis/screenshots/gap-accounting-empty-list.png)
![売上実績画面。エクスポートボタンとhrefなしの「売上実績を作成する」リンク、空状態が表示される](https://raw.githubusercontent.com/yosukekohno-bayworks/sq-faq/main/_analysis/screenshots/verify-sale-change-line-items-2026-06-19.png)

| 項目 | 内容 |
|:--|:--|
| サイドナビのメニュー名 | 会計 |
| サブメニュー名 | 売上実績 |
| 画面見出し（h1） | 売上実績 |
| タブ | なし |
| 空状態の文言 | 「アイテムが見つかりませんでした」「絞り込みや検索ワードを変更してみてください」 |
| `エクスポート` ボタン | 「注文軸」「明細軸」のメニューを表示 |
| `売上実績を作成する` リンク | 表示されるが `href` が空で押せない |
| テーブル列の構成 | <!-- TODO: 要確認（売上データがある環境での列構成） --> |

#### 「売上実績を作成する」リンク

- 画面上に表示されますが、DOM 上の `href` がなくクリックしても反応しません。
- 直接 `/admin/sale_change_line_items/create` にアクセスすると「このページは存在しないようです」になります。
- 注文データがある環境で有効化されるかは未確認です。

### CSVエクスポート画面（`/admin/csv_export`）の「実績」グループ

| カテゴリ名 | 遷移先URL | 内容 |
|:--|:--|:--|
| 売上実績（注文軸） | `/admin/csv_export/csv_export_operation_sale_changes` | 注文単位の売上実績を出力 |
| 売上実績（明細軸） | `/admin/csv_export/csv_export_operation_sale_change_line_items` | 明細単位の売上実績を出力 |

- 両カテゴリとも一覧に実行履歴とダウンロードリンクが表示されます。
- 出力実行画面（`/create`）では、テナント・開始日時・終了日時を指定して「エクスポートを開始する」で出力します。

### 分析メニュー（3画面とも TODO 表示）

| 画面 | URL | ナビ名 / h1 | 状態 |
|:--|:--|:--|:--|
| 分析（トップ） | `/admin/analytics` | 分析 / 分析 | 本文 `TODO` |
| 収益 | `/admin/analytics/revenue` | ナビ「収益」 / h1「売上」（不一致） | 本文 `TODO` |
| レポート | `/admin/analytics/reports` | レポート / レポート | 本文 `TODO` |

- ナビゲーションの「収益」と、遷移先の画面見出し「売上」は表記がずれています（実装上の不一致）。
- 3画面とも本文が `TODO` で、操作や集計機能は存在しません。

## 主な操作手順

### 売上実績を確認する

1. サイドナビの「会計」を開く。
2. サブメニューの「売上実績」を選択する（`/admin/sale_change_line_items` に遷移）。
3. 一覧で売上実績を確認する。
   - 本環境ではデータ0件のため「アイテムが見つかりませんでした」が表示されます。

### 売上実績をCSVでエクスポートする

1. 「会計 > 売上実績」（`/admin/sale_change_line_items`）を開く。
2. 一覧右上の `エクスポート` ボタンを押す。
3. 表示されたメニューから「注文軸」または「明細軸」を選ぶ。
   - 注文軸: `/admin/csv_export/csv_export_operation_sale_changes`
   - 明細軸: `/admin/csv_export/csv_export_operation_sale_change_line_items`
4. 出力実行画面でテナント・開始日時・終了日時を入力する。
5. `エクスポートを開始する` を押して出力を実行する。
6. 一覧の実行履歴からダウンロードリンクを開き、CSVを取得する。

> CSVエクスポートの全般的な手順は、機能別の「CSVエクスポート・PDFエクスポート」も参照してください。

## 注意点・制約

| 種別 | 内容 | 根拠 |
|:--|:--|:--|
| 仕様（注意） | 「売上実績を作成する」リンクは `href` がなく押せない。`/admin/sale_change_line_items/create` に直接アクセスしても「このページは存在しないようです」になる | 実機確認（2026-06-19） |
| 未実装 | 「分析」メニューの「収益」（`/admin/analytics/revenue`）と「レポート」（`/admin/analytics/reports`）は本文が `TODO` で機能未実装 | 実機確認（LIVE-RECHECK B-2節） |
| 仕様（注意） | ナビ名「収益」と画面h1「売上」は表記が不一致 | 実機確認（2026-06-19） |
| 未確認 | 注文データ0件のため、売上実績が注文から自動集計されるかは未確認 | 注文データ0件の環境制約 |
| 未確認 | 「売上実績を作成する」リンクが注文データありの環境で有効化されるかは未確認 | 同上 |
| 未確認 | 売上データありの環境での一覧テーブル列構成は未確認 | 同上 |

## このエリアの確認状態

| 項目 | 状態 | 備考 |
|:--|:--|:--|
| 売上実績 一覧画面の表示 | 確定 | `/admin/sale_change_line_items`・空状態の文言 |
| 売上実績 CSVエクスポート（注文軸） | 確定 | URL `csv_export_operation_sale_changes` |
| 売上実績 CSVエクスポート（明細軸） | 確定 | URL `csv_export_operation_sale_change_line_items` |
| 「売上実績を作成する」が押せない | 確定 | `href` 空・`/create` は存在しない画面 |
| 分析（トップ）が TODO 表示 | 確定 | `/admin/analytics` |
| 収益画面が TODO 表示 | 確定 | `/admin/analytics/revenue`・ナビとh1の不一致も確認 |
| レポート画面が TODO 表示 | 確定 | `/admin/analytics/reports` |
| 売上実績の自動集計挙動 | 未確認 | 注文データ0件のため |
| 売上データありの列構成 | 未確認 | 注文データ0件のため |

## TODO（未確認・一部確認）

### TODO表示（未実装）

- [ ] 「分析」トップ（`/admin/analytics`）の実機能
- [ ] 「収益」（`/admin/analytics/revenue`）の実機能（売上集計・グラフ等）
- [ ] 「レポート」（`/admin/analytics/reports`）の実機能

### 未確認（データ0件・連携待ち）

- [ ] 注文データから売上実績が自動生成されるか（注文データ0件）
- [ ] 注文データありの環境で「売上実績を作成する」リンクが有効化されるか
- [ ] 売上データありの環境での一覧テーブル列構成
- [ ] Shopify等のチャネル連携前提での売上実績反映（チャネル未接続）

## 次のエリア

→ [24. 次エリア名](24-次エリア名.md)
