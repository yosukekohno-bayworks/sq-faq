---
title: 22. CSV・PDF・データ移行
area: 22
wbs_status: 完成寄り 一部確認
last_verified: 2026-06-28
---

# 22. CSV・PDF・データ移行

SQの「CSVインポート」「CSVエクスポート」「PDFエクスポート」の3メニューを取り扱うエリアです。商品・在庫・価格・ポイント・売上実績などを**一括で登録・更新・出力**する機能で、手作業では時間がかかる大量データの取込みと取り出しを効率化します。このエリアでは、**何が一括処理でき、インポートが失敗したときにどう確認するか**を整理して説明します。

## このエリアで学べること

- CSVインポートで一括登録・一括更新できるデータの対象（9グループ・22カテゴリ）
- CSVエクスポートでCSV出力できるデータの対象（5グループ・9カテゴリ）
- PDFエクスポートの対象（納品書）と現在の実装状況
- カテゴリごとに異なるインポートフォームの構成（ファイルのみ／ラジオ選択／ルール選択／検索型選択）
- インポートが「検証 → 実行」の2段階で進むことと、失敗時の確認方法
- 実行履歴・ダウンロードの確認方法

## 機能概要

| メニュー | 画面URL | 内容 | 状態 |
|:--|:--|:--|:--|
| CSVインポート | `/admin/csv_import` | CSVでデータを一括登録・更新（9グループ・22カテゴリ） | 実装済み |
| CSVエクスポート | `/admin/csv_export` | データをCSVで出力（5グループ・9カテゴリ） | 実装済み |
| PDFエクスポート | `/admin/pdf_export` | 出荷指示の納品書PDFカテゴリを表示 | 納品書カテゴリページは表示されるが空状態。新規作成/任意生成ボタンは確認できず、作成直URLは存在しない画面。ヤマトB2条件指定エクスポートには「同梱する納品書PDF」表示あり |

- 3メニューともサイドナビの専用エントリから開きます。ホーム（`/admin`）のクイックリンクからも遷移可能です（`CSVでデータをインポートする` / `CSVでデータをエクスポートする` / `PDFでデータをエクスポートする`）。
- CSVインポートは**登録だけでなく更新**にも使います。既存データを上書きする場合は、テンプレートに沿ったCSVを新しく取り込みます。
- インポートは保存後、**「検証」→「実行」の2段階**で処理されます。検証でエラー行があると、実行前に失敗内容を確認できます。
- CSVエクスポートには「エクスポート」という名前のボタンはなく、**各カテゴリ行のリンク**をクリックして対象カテゴリの画面に遷移します。
- CSVエクスポートの「在庫」カテゴリは、外部連携の在庫同期とは別機能です。実機上、在庫CSVエクスポートの条件はロケーション指定のみで、カタログ指定はありません。
- PDFエクスポートは現在「納品書」の1カテゴリのみです。カテゴリページ（`/admin/pdf_export/pdf_export_operation_packing_slips`）は表示されますが空状態で、新規作成/任意生成ボタンは確認できません。新規作成画面（`/create`）は存在しない画面です。ヤマトB2クラウド条件指定エクスポートフォームには「同梱する納品書PDF」と表示されますが、ヤマトB2 CSVエクスポート履歴画面は空状態でダウンロードリンクがなく、実ファイル生成は接続環境で検証待ちです。

#### 3メニューの関係とインポートの状態遷移

```
┌─────────────────────────────────────────────────────────────┐
│  CSVインポート        CSVエクスポート      PDFエクスポート   │
│  （取り込む）         （取り出す）         （取り出す/PDF）   │
└─────────────────────────────────────────────────────────────┘

CSVインポートの処理フロー:
  保存する → 検証(実行中) → 検証完了 ─┬─ 実行する → 実行完了（反映）
                                       │              └─ 完了後は実行ボタンがdisabledで残る
                                       │
                                       └─ 検証失敗 → 失敗詳細画面で原因確認 → CSV修正して再取り込み

CSVエクスポート / ヤマトB2クラウド:
  エクスポート開始 → 処理中 → 完了 → ダウンロードリンク
  （ヤマトB2は出荷管理画面起点。画面文言上は完了後メール通知）
```

## 画面・項目の説明

> **フォームに出てくる用語のメモ**（初出のみ）
> - **テナント（事業部＝運用ルール分離単位。詳細は 03.組織・通知）**: CSVエクスポート等のフォームで選択する事業区分。テナント0件状態は既存マスタ削除が必要になるため検証対象外です。候補が出ない場合は先に「設定 > テナント」を確認してください。
> - **ロケーション**: 在庫の置き場所。場所種別は「倉庫」か「店舗」の2種のみ。倉庫は在庫保管・発注入荷先・出荷元・リテールポータル連携の在庫ロケーション、店舗はリテールポータル連携の店舗ロケーション・店舗受取の受取ロケーションとして扱う。手動の在庫依頼・移動伝票では倉庫/店舗の両方が関与するため、画面ごとの選択肢は04.基本マスタで確認する。
> - **SKU（商品バリエーション）**: 商品の色・サイズ等のバリエーション1つ1つ。在庫・価格はSKU単位で持つ。
> - **販売価格ルール／予約販売ルール／販売閾値ルール**: いずれも販売設定で**事前作成が必要**なルール。CSVインポート・エクスポートでは、そのドロップダウンから既存ルールを1つ選ぶ（ルール自体はここでは作れない）。

> **2026-06-24 方針**: Shopify等の外部販売チャネル・WMS・返品サービスなど、実接続が必要な連携系の同期挙動は連携待ちです。本ページでは管理画面に表示される標準フォーム項目・未接続時の表示までを実機確認範囲とします。

### CSVインポート `/admin/csv_import`

9グループ・22カテゴリで構成されています。グループ別にカテゴリをまとめます。

#### 商品グループ

| カテゴリ（UIラベル） | 説明 |
|:--|:--|
| 商品 | 商品を一括で登録・更新することができます。 |
| 商品バリエーション | 商品バリエーションを一括で登録・更新することができます。 |
| 商品画像 | 商品画像を一括で追加することができます。 |
| 商品バリエーション画像 | 商品バリエーション画像を一括で追加することができます。 |
| カタログ | カタログに商品を一括で追加・削除することができます。 |
| 原価 | SKUごとに原価を一括で設定することができます。 |

#### 価格グループ

| カテゴリ（UIラベル） | 説明 |
|:--|:--|
| 販売価格 (通常) | 通常区分の販売価格を一括で設定することができます。 |
| 販売価格 (セール) | セール区分の販売価格を一括で設定することができます。 |

#### 販売グループ

| カテゴリ（UIラベル） | 説明 |
|:--|:--|
| 販売閾値 | 販売閾値ルールごとにSKUと閾値を一括で設定することができます。 |

#### 在庫グループ

| カテゴリ（UIラベル） | 説明 |
|:--|:--|
| 販売可能在庫 | SKUとロケーションを指定して在庫数を一括で設定することができます。 |
| 予約販売 | SKUごとに予約販売の数量を一括で設定することができます。 |
| 在庫移動伝票 | 在庫移動伝票を一括で作成することができます。 |

#### メタフィールドグループ

| カテゴリ（UIラベル） | 説明 |
|:--|:--|
| 商品 | 商品メタフィールドを一括で追加することができます。 |
| 商品バリエーション | 商品バリエーションメタフィールドを一括で登録・更新することができます。 |

#### 出荷グループ

| カテゴリ（UIラベル） | 説明 |
|:--|:--|
| ヤマトB2クラウド | ヤマトB2クラウドのCSVで出荷実績を一括で登録することができます。 |
| DHL | DHLのCSVで出荷実績を一括で登録することができます。 |

#### ポイントグループ

| カテゴリ（UIラベル） | 説明 |
|:--|:--|
| 一括加算 | 顧客にポイントを一括で加算することができます。 |
| 一括減算 | 顧客のポイントを一括で減算することができます。 |
| キャンペーン対象商品 | キャンペーンの対象商品を一括で設定することができます。 |

#### 会員ランクグループ

| カテゴリ（UIラベル） | 説明 |
|:--|:--|
| 基準ランク | 顧客の基準ランクを一括で設定することができます。 |

#### 各種マスターグループ

| カテゴリ（UIラベル） | 説明 |
|:--|:--|
| 管理ユーザー | 管理ユーザーを一括で登録・更新することができます。 |
| ロケーション | ロケーションを一括で登録・更新することができます。 |

#### FAQ/タスク記事で参照する主な直接URL

カテゴリ行から遷移する一覧URLと、「新規インポート」から開く作成フォームURLを分けて押さえます。以下はFAQ・タスク記事側で直接参照されているため、Notion側にも明示します。

| 対象 | 一覧URL | 作成/実行フォームURL |
|:--|:--|:--|
| 商品インポート | `/admin/csv_import/csv_import_operation_products` | `/admin/csv_import/csv_import_operation_products/create` |
| 販売可能在庫インポート | `/admin/csv_import/csv_import_operation_inventory_logical_available_quantities` | `/admin/csv_import/csv_import_operation_inventory_logical_available_quantities/create` |
| ヤマトB2クラウド出荷実績インポート | `/admin/csv_import/csv_import_operation_fulfillment_by_yamato_b2_clouds` | `/admin/csv_import/csv_import_operation_fulfillment_by_yamato_b2_clouds/create` |
| DHL出荷実績インポート | `/admin/csv_import/csv_import_operation_fulfillment_by_dhls` | 「新規インポート」から作成フォームへ遷移 |
| ポイント一括減算 | `/admin/csv_import/csv_import_operation_point_minuses` | 「新規インポート」から作成フォームへ遷移 |
| ポイントキャンペーン対象商品 | `/admin/csv_import/csv_import_operation_point_campaign_order_rule_point_value_product_variants` | 「新規インポート」から作成フォームへ遷移 |
| カタログ | `/admin/csv_import/csv_import_operation_catalog_products` | `/admin/csv_import/csv_import_operation_catalog_products/create` |

#### テンプレートの有無

テンプレートがあるカテゴリでは、一覧画面に `テンプレート` ボタンが表示されます。テンプレートは Google スプレッドシート形式で別タブで開きます。

- テンプレートがないカテゴリ（6種）: 販売価格 (通常) / 原価 / 販売閾値 / DHL / キャンペーン対象商品 / 基準ランク

##### 商品一覧のインポート5項目

商品一覧（`/admin/products`）右上の「インポート」メニューから開ける5項目は、CSVインポートトップの「商品」グループと完全には一致しません。

| 商品一覧の項目 | CSVインポートURL | テンプレートの主な必須列 |
|:--|:--|:--|
| 商品 | `/admin/csv_import/csv_import_operation_products` | `command`, `product_code`, `title`, `product_status`, `option1_name` |
| 商品画像 | `/admin/csv_import/csv_import_operation_product_images` | `product_code`, `image_url` |
| 商品バリエーション | `/admin/csv_import/csv_import_operation_product_variants` | `command`, `sku`, `product_code`, `option1_value`, `price`, `price_currency_code` |
| 商品メタフィールド | `/admin/csv_import/csv_import_operation_product_metafields` | `product_code`, `namespace`, `key`, `value_type`, `value` |
| 商品バリエーションメタフィールド | `/admin/csv_import/csv_import_operation_product_variant_metafields` | `sku`, `namespace`, `key`, `value_type`, `value` |

> CSVインポートトップの「商品」グループは `商品 / 商品画像 / 商品バリエーション / 商品バリエーション画像 / カタログ / 原価`。メタフィールド2項目はトップ画面では「メタフィールド」グループにあります。2026-06-27再確認時点では、原価は「価格」ではなく「商品」グループに表示されます。

> **商品バリエーションテンプレートの在庫系項目**: `inventory_policy` は「在庫切れの場合でも販売を続ける」（CONTINUE/DENY）、`is_tracked` は「在庫追跡」（TRUE/FALSE）です。両者は別項目です。2026-06-28再確認でも、管理画面のバリエーション作成フォーム初期値はどちらもOFFでした。

> **カタログ商品テンプレート**: 2026-06-28実機確認時点の列は `product_code,command` です。`product_code` には既存商品の商品コード、`command` にはカタログへ追加する場合 `NEW`、カタログから削除する場合 `DELETE` を入れます。

#### インポート実行フォームのパターン

カテゴリによってフォーム構成が異なります。

| パターン | 構成 | 該当カテゴリ |
|:--|:--|:--|
| A：ファイルのみ | CSVファイルをアップロードして `保存する` | 商品 / 商品バリエーション / 商品メタフィールド / 商品バリエーションメタフィールド / 原価 / 在庫移動伝票 / ヤマトB2クラウド / DHL / 管理ユーザー / ロケーション |
| B：ラジオ選択 + ファイル | 処理方法をラジオで選んでからCSVアップロード | 商品画像 / 商品バリエーション画像 / 販売可能在庫 |
| C：ルール選択 + ファイル | ルールをドロップダウンで選んでからCSVアップロード | 販売価格 (通常) / 販売価格 (セール) / 販売閾値 / 予約販売 / 基準ランク |
| D：検索型選択 + ファイル | 検索＋ダイアログで対象を選んでからCSVアップロード | キャンペーン対象商品 |
| E：カタログ選択 + ファイル | カタログをドロップダウンで選んでからCSVアップロード | カタログ |

##### パターンBの選択肢

| カテゴリ | ラジオボタンの選択肢（デフォルト → 変更可） |
|:--|:--|
| 商品画像 | 「画像を追加する」（デフォルト） / 「画像を上書きする」 |
| 商品バリエーション画像 | 「画像を追加する」（デフォルト） / 「画像を上書きする」 |
| 販売可能在庫 | 「絶対値で反映する」（デフォルト） / 「差分値で反映する」 |

> **画像の追加／上書きの違い**: 「画像を追加する」＝既存画像を残したまま新しい画像を追加。「画像を上書きする」＝当該商品/SKUの既存画像を削除して置き換え（分析記録に基づく）。商品画像を一新したいとき以外は追加（デフォルト）を使います。複数画像時の一部上書きなど詳細な置換範囲は追加確認。

> 販売可能在庫の画面説明原文: 「現在の在庫数が10のとき、CSVで20を指定すると、絶対値で反映した場合の在庫数は20になります。差分値で反映した場合の在庫数は30になります。」

![販売可能在庫のインポートフォーム（ラジオ選択 + ファイル）](https://raw.githubusercontent.com/yosukekohno-bayworks/sq-faq/main/_analysis/screenshots/15-csv-import-inventory-create.png)

##### パターンCのフィールドラベル

| カテゴリ | フィールドラベル |
|:--|:--|
| 販売価格 (通常) | 販売価格ルール |
| 販売価格 (セール) | 販売価格ルール |
| 販売閾値 | 販売閾値ルール |
| 予約販売 | 予約販売ルール |
| 基準ランク | テナント |

- **各ルールの運用上の意味**: いずれも販売設定で事前作成したルールを1つ選び、そのルール配下のSKUへCSV値を反映させます。
  - 販売価格ルール = 通常/セール価格をSKU別に束ねる設定（事前作成必須）
  - 販売閾値ルール = 在庫の閾値制御の設定
  - 予約販売ルール = 在庫0でも受注を続ける設定
  - 基準ランクのテナント = 基準ランクはテナント単位で顧客に設定されるため、対象テナントを指定します

#### ポイント一括加算・一括減算フォームの固有項目

![ポイント一括加算のインポートフォーム（テナント・顧客IDの種別）](https://raw.githubusercontent.com/yosukekohno-bayworks/sq-faq/main/_analysis/screenshots/15-csv-import-point-plus-create.png)

| カテゴリ | 項目（UIラベル） | 説明 | 選択肢 |
|:--|:--|:--|:--|
| 一括加算 | テナント | 加算対象のテナントを選択する | セレクトボックス（プレースホルダ「選択してください」） |
| 一括加算 | 顧客IDの種別 | CSV内の顧客識別子の種類を指定する | 「SQが発番した顧客ID」 / 「外部システムが発番した顧客ID」 |
| 一括減算 | テナント | 減算対象のテナントを選択する | セレクトボックス |

- **顧客IDの種別の使い分け**: CSVの顧客ID列にSQ内部IDを書くなら「SQが発番」、Shopify等の外部IDを書くなら「外部システムが発番」を選びます。加算対象の顧客を正しく特定するための切替です（チャネル連携時は外部IDになることが多いです ※接続環境未確認）。
- どちらのフィールドも必須マーク（*）は表示されませんが、未選択で `保存する` を押すとバリデーションエラーが出ます（実質必須）。
- 一括減算フォームに「顧客IDの種別」はありません。空保存時は `テナントを選択してください`、テナント選択済みでファイル未選択なら `ファイルを選択してください` と表示されます。

#### 在庫移動伝票インポート

| 画面 | URL | 見出し |
|:--|:--|:--|
| 一覧 | `/admin/csv_import/csv_import_operation_inventory_movement_orders` | 在庫移動伝票をCSVで一括取り込む |
| 新規インポート | `/admin/csv_import/csv_import_operation_inventory_movement_orders/create` | 在庫移動をCSVで一括取り込む |

- 新規インポート画面の空保存では `ファイルを選択してください` と表示されます。

#### 実行履歴・詳細画面

![CSVインポート操作の詳細画面（検証・実行ステータス）](https://raw.githubusercontent.com/yosukekohno-bayworks/sq-faq/main/_analysis/screenshots/gap-csv-import-detail-success.png)

カテゴリの一覧画面に過去の実行履歴が表示され、各行をクリックすると詳細を確認できます。

| 項目（UIラベル） | 説明 |
|:--|:--|
| 作成日 | インポートを実行した日付 |
| 検証ステータス | CSVの内容を検証した結果（例: 完了） |
| 実行ステータス | 実際のデータ反映の結果（例: 完了） |
| 検証成功 | 検証に成功した件数（例: 6個の商品） |
| 検証失敗 | 検証に失敗した件数。クリックで失敗詳細画面（見出し「検証失敗」）へ移動 |

2026-06-27再確認では、販売可能在庫インポートの実行済み詳細に `検証ステータス: 成功 完了`、`実行ステータス: 成功 完了`、`検証成功: 1個の商品`、`検証失敗: 0個の商品` が表示されました。完了後も `実行する` ボタンは画面上に残りますが、disabled状態で再実行できません。`0個の商品` でも検証失敗リンクは開け、見出し `検証失敗` の画面へ遷移します。

2026-06-28追加確認では、商品CSVインポートでも検証用商品1件を実行しました。CSV保存後に `CSVインポートの検証を開始しました` が表示され、詳細で `検証ステータス: 成功 完了` / `実行ステータス: 未実行` / `検証成功: 1個の商品` / `検証失敗: 0個の商品` になりました。`実行する` を押すと確認ダイアログ `CSVの取り込み処理を実行しますか？` が開き、本文は `1件の商品を登録します。この操作は巻き戻すことができません。` です。確定後、実行ステータスは時間差で `成功 完了` になり、商品一覧/詳細へ反映されました。検証用商品は商品詳細から削除済みです。

同じく2026-06-28に、カタログ商品CSVの `NEW` / `DELETE` を検証用商品1件で実行しました。カタログ商品CSVは `product_code,command` 形式で、`NEW` 実行後に対象商品がカタログ詳細の商品一覧へ表示され、`DELETE` 実行後に同一覧から消えました。どちらも詳細で `検証成功: 1件の商品` / `検証失敗: 0件の商品`、実行後 `実行ステータス: 成功 完了` です。確認ダイアログ本文は `1件のカタログ商品を登録します。この操作は巻き戻すことができません。` と表示されました。検証用の商品・カタログは削除済みです。

### CSVエクスポート `/admin/csv_export`

5グループ・9カテゴリで構成されています。「エクスポート」という名前のボタンはなく、**各カテゴリ行のリンク**をクリックして遷移します。

> **外部在庫同期との切り分け**: Stack社MTGで出た「外部に在庫数を出力したい時の対象商品一覧を作るためのカタログ」は、本ページのCSVエクスポートではなく、スマレジなど外部連携側の在庫同期設定として扱います。2026-06-25実機確認では、在庫CSVエクスポート作成フォーム（`/admin/csv_export/csv_export_operation_inventory_logical_quantities/create`）にある条件は `ロケーション` のみで、カタログ選択はありません。

| グループ | カテゴリ | 遷移先URL | 作成フォーム（`/create`）の項目 |
|:--|:--|:--|:--|
| マスターデータ | 在庫 | `/admin/csv_export/csv_export_operation_inventory_logical_quantities` | ロケーション選択 + `エクスポートを開始する` |
| マスターデータ | 商品バリエーション | `/admin/csv_export/csv_export_operation_product_variants` | `商品情報を含める` チェック + `エクスポートを開始する` |
| マスターデータ | ロケーション | `/admin/csv_export/csv_export_operation_location_by_location_group` | ロケーショングループ選択 + `エクスポートを開始する` |
| マスターデータ | ディスカウントの利用履歴 | `/admin/csv_export/csv_export_operation_order_price_adjustment_usages` | ディスカウント選択 + `エクスポートを開始する` |
| 価格 | セール価格 | `/admin/csv_export/csv_export_operation_product_price_rule_sale_prices` | 販売価格ルール選択（必須） + `エクスポートを開始する` |
| 出荷 | ヤマトB2クラウド | `/admin/csv_export/csv_export_operation_inventory_outbound_order_yamato_b2_clouds` | 当画面は**実行履歴確認専用**（エクスポートボタンなし）。出荷管理画面（`/admin/inventory_outbound_orders`）の条件指定エクスポートから実行する。フォームには出力物として「ヤマトB2クラウド取り込み用CSV」「同梱する納品書PDF」が表示され、完了後にメールでダウンロードリンクが通知される文言がある。2026-06-28追加確認では、ステータス変更ON実行でも検証用 `#IO-1029` は `出荷待ち` のまま。履歴画面も空状態でダウンロードリンクなし |
| 実績 | 売上実績（注文軸） | `/admin/csv_export/csv_export_operation_sale_changes` | テナント（必須） / 開始日時（必須） / 終了日時（必須） / `エクスポートを開始する` |
| 実績 | 売上実績（明細軸） | `/admin/csv_export/csv_export_operation_sale_change_line_items` | テナント（必須） / 開始日時（必須） / 終了日時（必須） / `エクスポートを開始する` |
| ポイント | ポイント変動履歴 | `/admin/csv_export/csv_export_operation_point_changes` | テナント（必須） / 開始日時（必須） / 終了日時（必須） / `エクスポートを開始する` |

> 売上実績（注文軸）の実行フォームを直接開くURLは `/admin/csv_export/csv_export_operation_sale_changes/create` です。売上実績一覧（`/admin/sale_change_line_items`）の `エクスポート > 注文軸` からも同じカテゴリに遷移します。

> ヤマトB2クラウドの条件指定エクスポートはCSVエクスポート履歴画面ではなく、出荷管理画面起点です。直接URLは `/admin/inventory_outbound_orders/export/yamato_b2_cloud` です。

#### フォーム項目の運用上の意味

- `商品情報を含める`（商品バリエーション）: ONにすると商品コード・商品タイトル・ブランド名など商品本体の列がCSVに追加されます（デフォルトOFF）。バリエーション単位の出力に親商品の情報を併せたい場合に使います。
- ロケーション選択（在庫）: 選んだロケーションの在庫数を出力します（複数選択可）。商品集合をカタログで絞る項目はありません。
- ロケーショングループ選択（ロケーション）: 選んだグループに属する全ロケーションの定義（名称・場所コード等）を出力します。
- ディスカウント選択（ディスカウントの利用履歴）: 選んだディスカウント1件につき、それが注文で適用された履歴を出力します。全ディスカウント横断ではなく1件単位の指定が必須です。
- **売上実績・ポイント変動履歴でテナントが必須な理由**: 売上・ポイントはテナント別の会計/ポイント単位で出力されるため、出力範囲を1テナントに限定します（テナントの定義自体は 03.組織・通知 を参照）。
- 実績系（売上実績の注文軸・明細軸）では、一覧に実行履歴と `ダウンロード` リンクが表示されます。実行直後は「処理中」→ 再読み込み後に「完了」になりダウンロードリンクが現れます。
- 出力実行は `エクスポートを開始する` ボタンで開始します。
- 旧版FAQにあった `csv_export_operation_order_price_adjustment_rule_usages` / `...usage_histories` / `sale_change_line_item_details` は存在しないページ（誤URL）。正しくは上表の `...order_price_adjustment_usages` / `...sale_change_line_items` 等。
- 2026-06-21実機では、`csv_export_operation_inventory_items` / `csv_export_operation_locations` / `csv_export_operation_sale_prices` / `csv_export_operation_point_histories` は存在しないページでした。正しくは `inventory_logical_quantities` / `location_by_location_group` / `product_price_rule_sale_prices` / `point_changes` です。

### PDFエクスポート `/admin/pdf_export`

| 項目 | 内容 |
|:--|:--|
| 画面見出し（h1） | PDFエクスポート |
| グループ | 出荷 |
| カテゴリ | 納品書 |
| 説明文（原文） | 指定された出荷指示の納品書をエクスポートできます。 |
| 新規作成ボタン | トップ画面には表示されない |
| カテゴリページ | `/admin/pdf_export/pdf_export_operation_packing_slips` は見出し「納品書をPDFでエクスポートする」。2026-06-28時点では空状態（「アイテムが見つかりませんでした」）で、新規作成/任意生成ボタンなし |
| 新規作成画面 `/create` | `/admin/pdf_export/pdf_export_operation_packing_slips/create` は存在しないページ（h1「このページは存在しないようです」） |

- 現状、PDFエクスポート画面では**納品書カテゴリとカテゴリ一覧の空状態**まで確認できますが、任意にPDFを新規生成する操作は確認できません。
- 2026-06-28に移動/調整/取置/出荷/入荷/発注の各詳細（`#IM-1027` / `#IA-1013` / `#IR-1011` / `#IO-1027` / `#II-1029` / `#IP-1010`）を横断確認しましたが、伝票詳細側にもPDF/印刷/納品書生成ボタンはありません。
- ヤマトB2クラウド条件指定エクスポートフォームには「同梱する納品書PDF」と表示されます。ただしヤマトB2 CSVエクスポート履歴画面は空状態でダウンロードリンクがなく、実際のメールリンク・PDF実ファイルは接続環境で検証待ちです。なお、納品書のテンプレートは設定画面（`/admin/settings/pdf_template_package_slip`）で管理されています。

## 主な操作手順

### CSVをインポートする（基本の流れ）

1. サイドナビの「CSVインポート」を開く（`/admin/csv_import`）。
2. 取り込みたいカテゴリの行リンクをクリックし、カテゴリの一覧画面に遷移する。
3. 必要に応じて `テンプレート` ボタンを押し、Googleスプレッドシート形式のテンプレートを開いてCSV形式を確認する。
4. 一覧画面の `新規インポート` を押し、新規インポートフォームを開く。
5. カテゴリに応じた追加項目（ラジオ／ルール／検索型／テナントなど）を選ぶ。
6. CSVファイルをアップロードし、`保存する` を押す。
7. 詳細画面に遷移する。検証ステータスが「実行中」のあいだは `実行する` ボタンが無効。
8. 検証ステータスが「完了」になったら `実行する` を押す。
9. 確認ダイアログ「CSVの取り込み処理を実行しますか？」の内容を確認し、`実行する` を押す。
10. 実行ステータスが「完了」になり、データに反映される。完了後の `実行する` はdisabledで残り、再実行できない。

### インポート失敗時の確認方法

1. カテゴリの一覧画面で、対象の実行履歴行をクリックする。
2. 詳細画面の `検証失敗` の件数リンクをクリックする。
3. 失敗詳細画面（見出し「検証失敗」）で、エラーとなった行と理由を確認する。
4. CSVを修正し、新規インポートで再度取り込む。

> 在庫系の反映誤りは、インポートの再実行だけでなく在庫調整伝票での補正も選択肢になります。

### CSVで在庫を一括更新する（販売可能在庫の例）

1. 「CSVインポート」> 在庫グループ > `販売可能在庫` を開く。
2. `新規インポート` を押す。
3. 処理方法を選ぶ:
   - 「絶対値で反映する」（デフォルト）: CSVの値をそのまま在庫数にする
   - 「差分値で反映する」: 現在の在庫数にCSVの値を足す／引く
4. CSVファイルをアップロードし、`保存する` を押す。
5. 検証完了後に `実行する` を押し、確認ダイアログで `実行する` を確定する。
6. 在庫詳細（SKU詳細）の販売可能数・手持ち数に反映される。

### CSVをエクスポートする（基本の流れ）

1. サイドナビの「CSVエクスポート」を開く（`/admin/csv_export`）。
2. 出力したいカテゴリの行リンクをクリックする。
3. `エクスポートを開始する` 画面で、カテゴリに応じた条件を入力する（テナント・開始日時・終了日時／ロケーション／ルール／ディスカウントなど）。
4. `エクスポートを開始する` を押す。
5. 一覧の実行履歴から `ダウンロード` リンクを開き、CSVを取得する。

> 「エクスポート」という名前のボタンはありません。カテゴリの行リンクから遷移してください。

## 注意点・制約

| 種別 | 内容 | 根拠 |
|:--|:--|:--|
| 仕様（不可逆） | CSVインポートの「実行」は取り消し不可。確認ダイアログには「この操作は巻き戻すことができません。」と表示される。誤った場合は修正版CSVを新規インポートするか、在庫は調整伝票で補正する。実行完了後の `実行する` はdisabledで残る | 実機確認（2026-06-15 販売可能在庫CSV、2026-06-27再確認、2026-06-28 商品CSV・カタログ商品CSV）＋`_support/constraints.md` |
| 仕様（注意） | CSVのカラム順・ヘッダー名はテンプレートに準拠する必要がある（列指定UIは存在しない） | 実機確認（テンプレート形式） |
| 仕様（注意） | CSVエクスポートに「エクスポート」という名前のボタンはなく、カテゴリ行リンクで遷移する | 実機確認（2026-06-19） |
| 未実装 | PDFエクスポートの新規作成画面（`/admin/pdf_export/pdf_export_operation_packing_slips/create`）は存在しないページ。納品書カテゴリページは空状態で、PDFエクスポート画面単体からの任意PDF出力操作は実装されていない | 実機確認（2026-06-21、2026-06-28再確認） |
| 仕様（注意） | 顧客管理画面の「インポート」ドロップダウン内の「ポイント一括付与」は、CSVインポート画面の「一括加算」と同じ機能（アプリ内で表記が異なる） | 実機確認 |
| 仕様（注意） | ヤマトB2クラウドCSVは固定カラム順・ヘッダー名一致方式。必須列は「伝票番号」「出荷日」「お客様管理番号」の3列のみ。残り7列は任意 | 実機確認 |
| 仕様（注意） | ヤマトB2クラウド出荷実績CSVは保存・CSVインポート詳細作成まで確認済み。2026-06-28の検証用CSVでは、再確認時も `実行ステータス 未実行`、`検証成功 0件`、`検証失敗 0件` のままで、実行・出荷完了遷移は未確認 | 実機確認 |
| 仕様（注意） | DHL出荷実績CSVはインポート導線とファイルアップロード欄のみ確認済み。2026-06-28再確認でもテンプレートリンク・列数・必須列・列名説明は管理画面UIに表示されない | 実機確認 |
| 仕様（注意） | 顧客データは手動新規作成不可で、Shopify等のチャネルから流入するため、顧客関連CSVの実データ確認はチャネル未接続・連携待ちではできない | `_support/constraints.md` 該当項目 |

## このエリアの確認状態

| 項目 | 状態 | 備考 |
|:--|:--|:--|
| CSVインポート 9グループ・22カテゴリの構成 | 確定 | 2026-06-27実機再確認。原価は「商品」グループに表示 |
| CSVインポート 各カテゴリの説明文（UIラベル） | 確定 | 一覧画面の原文どおり |
| CSVインポート フォームパターン A〜E | 確定 | 各カテゴリのフォーム構成。カタログはカタログ選択 + ファイル |
| 販売可能在庫の絶対値/差分値反映 | 確定 | 画面説明文＋実行フロー確認（2026-06-16） |
| CSVインポート完了後の詳細状態 | 確定 | 2026-06-27再確認。実行済み詳細は `成功 完了`、`実行する` はdisabledで残る。商品CSVは2026-06-28に検証用商品1件で実行・反映・削除まで確認。カタログ商品CSVは `NEW` / `DELETE` の実行・反映・後処理まで確認 |
| カタログ商品CSVのNEW/DELETE | 確定 | 2026-06-28実機確認。`product_code,command` 形式で `NEW` は追加、`DELETE` は削除。検証成功は `1件の商品` 表記 |
| ポイント一括加算／減算フォーム固有項目 | 確定 | テナント／顧客IDの種別。加算の空保存はテナント/顧客ID種別エラー、減算の空保存はテナントエラー、テナント選択後はファイル未選択エラー |
| 在庫移動伝票インポート画面URL | 確定 | `csv_import_operation_inventory_movement_orders` |
| 予約販売バリエーションCSVの正しいURL | 確定 | `csv_import_operation_inventory_back_order_rule_product_variants` |
| 管理ユーザーCSVの一覧・作成フォーム | 確定 | `csv_import_operation_users` |
| 実行履歴の検証/実行ステータス・失敗詳細 | 確定 | 詳細画面・検証失敗画面 |
| インポート実行の不可逆性 | 確定 | 確認ダイアログ原文 |
| CSVエクスポート 5グループ・9カテゴリの構成 | 確定 | 2026-06-21実機再確認（分析記録の「8カテゴリ」は陳腐化） |
| CSVエクスポートに「エクスポート」ボタンがない | 確定 | カテゴリ行リンク遷移 |
| 在庫CSVエクスポートにカタログ指定がない | 確定 | 2026-06-25実機確認。`ロケーション` 指定のみ |
| 売上実績CSV（注文軸・明細軸）のURL・フォーム | 確定 | `csv_export_operation_sale_changes` / `...sale_change_line_items` |
| ディスカウント利用履歴CSVのURL・フォーム | 確定 | `csv_export_operation_order_price_adjustment_usages` |
| 在庫／商品バリエーション／ロケーション／セール価格CSVの作成フォーム項目 | 確定 | 各 `/create` で確認 |
| CSVエクスポート 一部カテゴリの正式カテゴリ名・URL末尾 | 確定 | 在庫=`...inventory_logical_quantities`／ロケーション=`...location_by_location_group`／セール価格=`...product_price_rule_sale_prices`／ヤマトB2クラウド=`...inventory_outbound_order_yamato_b2_clouds`（履歴確認専用・実行は出荷管理画面）／ポイント変動履歴=`...point_changes` |
| PDFエクスポート トップ画面の表示 | 確定 | `/admin/pdf_export`・h1・出荷>納品書行 |
| PDFエクスポート 納品書カテゴリページ | 確定（空状態） | `/admin/pdf_export/pdf_export_operation_packing_slips`・h1「納品書をPDFでエクスポートする」・空状態。新規作成/任意生成ボタンなし |
| PDFエクスポート 新規作成画面 | 確定（未実装） | `/create` は存在しないページ |
| 伝票詳細のPDF/印刷導線 | 確定（なし） | 2026-06-28 `#IM-1027` / `#IA-1013` / `#IR-1011` / `#IO-1027` / `#II-1029` / `#IP-1010` 詳細でPDF/印刷/納品書生成ボタンなし |
| ヤマトB2条件指定フォームの納品書PDF表示・ステータス変更チェック | 一部確認 | フォームに「同梱する納品書PDF」と表示。ステータス変更ON実行でも検証用 `#IO-1029` は `出荷待ち` のまま。履歴画面は空状態でダウンロードリンクなし。実ファイル生成は接続環境で検証待ち |
| 実注文/実顧客前提のCSV（ポイント・売上など）の実データ | 一部確認 | 顧客データと売上実績明細は内部GraphQL検証データで作成済み。売上実績CSV（注文軸/明細軸）は2026-06-28に実ファイルをダウンロードし、ヘッダーとデータ行を確認済み。ポイント系は実績なし |

## 残課題（対象外・データ依存を含む）

### 完成寄り（ほぼ確定・残り少なめ）

- [x] CSVエクスポート「セール価格」カテゴリの正式名称とURL末尾 → `csv_export_operation_product_price_rule_sale_prices`／フォーム: 販売価格ルール選択（必須）
- [x] CSVエクスポート「ヤマトB2クラウド」カテゴリ → `csv_export_operation_inventory_outbound_order_yamato_b2_clouds`（履歴確認専用）。実行は出荷管理画面（`/admin/inventory_outbound_orders`）の条件指定エクスポートから。フォーム上には「エクスポート完了後、メールにて以下のダウンロードリンクをお知らせいたします」と表示され、出力物として「ヤマトB2クラウド取り込み用CSV」「同梱する納品書PDF」が表示される。2026-06-28追加確認では、ステータス変更ON実行でも移動伝票由来・配送希望日なしの `#IO-1029` は出荷待ちのまま。履歴画面は空状態でダウンロードリンクなし。実際のメール受信・本文・リンク有効期限・PDF実ファイルは接続環境で検証待ち
- [x] CSVエクスポート「ポイント変動履歴」カテゴリ → `csv_export_operation_point_changes`／フォーム: テナント・開始日時・終了日時（全て必須）
- [x] CSVエクスポート全9カテゴリのグループ分けと表示順の最終突合（2026-06-21実機再確認）

### 連携待ち（外部連携前提・チャネル未接続）

- [ ] Shopify等のチャネル接続前提での、ポイント一括加算／減算の実データ取込
- [x] 内部GraphQL検証注文で作成された売上実績を使った、売上実績CSV（注文軸・明細軸）の実ファイル出力内容
- [ ] **実売上/利用履歴データ待ち** — ディスカウント利用履歴CSVの実データ出力（利用履歴0件。売上実績明細は検証データあり）
- [ ] ポイント変動履歴CSVの実データ出力（ポイント実績なし）
- [ ] ヤマトB2クラウド／DHL出荷実績CSVの実データ取込（ヤマトB2はCSV保存・詳細作成まで確認済み。実行・出荷完了遷移は実CSVサンプル待ち）

### 未実装画面・実ファイル未確認

- [x] PDFエクスポート新規作成画面は現行未実装（2026-06-28再確認。`/admin/pdf_export/pdf_export_operation_packing_slips/create` は存在しないページ）
- [ ] **連携待ち** — PDF納品書の実際の出力確認（ヤマトB2条件指定フォームの「同梱する納品書PDF」が実ファイルとして届くか。現環境では履歴画面が空状態でダウンロードリンクなし）

## 次のエリア

→ [23. API・Webhook・開発者連携](23-API・Webhook・開発者連携.md)
