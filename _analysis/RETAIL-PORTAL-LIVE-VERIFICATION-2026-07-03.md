# リテールポータル実機検証 2026-07-03

リテールポータルの実URL（`https://retail.sqstackstaging.com/admin`）が判明したため、店舗スタッフ側画面を初めて実機検証した一次記録。既存の管理者アカウント（yosuke.kohno@bay-works.com / stack-ps-yosuke）のセッションでそのままログインできた。

- 検証日時: 2026-07-03 03:30〜04:00 JST（画面表記）
- 検証者ロール: リテールポータル連携「ユニクロ - 銀座店」に追加済みの管理者ユーザー
- 作成したテストデータ: 下書き注文→注文 `#202607031000`（タグ `TEST_FAQ_20260703`）、顧客住所1件（検証太郎/銀座1-1-1）、自動生成の出荷指示 `#IO-1052`
- スクショ: `screenshots/retail-portal-01〜10*.png`

---

## 1. アクセスとログイン

| 確認項目 | 結果 |
|:--|:--|
| URL | `https://retail.sqstackstaging.com/admin`（本体は `www.sqstackstaging.com`。サブドメイン別の独立アプリ） |
| 認証 | 本体管理画面と同じClerkアカウント。既存セッションが有効ならそのまま入れる |
| アクセス権 | リテールポータル連携詳細（`/admin/retail_portal_integrations/<id>`）の「ユーザーを追加する」で追加されたユーザーが対象。追加済みの管理者ユーザーでアクセスできることを実証 |
| ヘッダー表示 | ユーザー名の下に**連携の店舗ロケーション名**（ユニクロ - 銀座店）が表示される |

## 2. 販売員ゲート（販売員コード）

- ログイン直後は全画面が「販売員 / リテールポータルのご利用には販売員の選択が必要です。」でブロックされ、「販売員を選択する」ボタンのみ操作できる。
- 「販売員を検索する」ダイアログで**販売員コード**（placeholder表記）を入力→「検索する」。部分一致（`test`）はヒットせず、登録済みコード `123` の完全一致で「銀座ユニクロ」がヒット。「選択する」で解除。
- コードは 設定 > 販売員（`/admin/settings/retail_staff_members`）の「販売員コード」列の値。検証時の登録は3件（銀座ユニクロ=`123` / TEST_APPCHECK販売員 / A田B郎=`testAB`）。
- **ページを再読み込み（直URL遷移）するたびに販売員選択が再要求される**。SPA内のリンク遷移では保持される。
- ヘッダーの鍵アイコン >「**画面をロックする**」で手動で販売員選択ゲートに戻せる（店頭での販売員交代を想定した挙動）。
- 連携設定「リテールポータルで販売員の選択を必須にする」= ON の環境での挙動。
- 販売員「銀座 ユニクロ」の管理画面詳細では店舗=ユニクロ - 銀座店の1件が紐づき、詳細画面に「**店舗を追加する**」ボタンがある（作成後は複数店舗を追加できるUI。作成フォームでは1件のみ選択可だった2026-06-27記録と併記が必要）。

## 3. メニュー構成（本体管理画面のサブセット）

| メニュー | URL（retailサブドメイン配下） | 実測の表示範囲 |
|:--|:--|:--|
| ホーム | `/admin` | 「在庫管理」「客注管理」カード＋ヘルプセンター（ドキュメント・ガイド） |
| 注文管理 | `/admin/orders` | この店舗の注文のみ表示（列: 注文/制限/注文日時/チャネル/店舗/アイテム/合計/決済状況/出荷状況/返品状況/タグ）。未完了件数バッジあり |
| 注文管理 > 下書き | `/admin/draft_orders` | 客注（下書き注文）一覧。**「注文を作成する」ボタンが機能する**（`/admin/draft_orders/create` へ遷移）。列: 注文/日時/アイテム/販売員/注文/合計 |
| 在庫管理 | `/admin/inventory_items` | 一覧は簡易4区分（販売可能/引当済み/取置中/手持ち）＋積送中バッジ。ヘッダーのロケーション切替で**全ロケーション（すべて/店舗/倉庫タブ）を参照可能** |
| 顧客管理 | `/admin/purchasing_customers` | 本体と同じ全顧客が表示される（**他テナントの顧客も表示**。列: 顧客名/テナント/ステータス/メール購読） |
| 売上管理 | `/admin/sale_change_line_items` | 検証時は空（この店舗の売上実績のみ表示と推定。未検証） |
| 入荷管理 | `/admin/inventory_inbound_orders` | **到着ロケーション=この店舗の入荷指示のみ**。タブ: すべて/入荷待ち/入荷依頼済み/入荷作業中/要対応/入荷完了/キャンセル済み |
| 出荷管理 | `/admin/inventory_outbound_orders` | この店舗が出荷場所の出荷指示のみ。タブ: すべて/出荷待ち/依頼済み/作業中/欠品・要対応/出荷完了/キャンセル済み |
| 在庫依頼 | `/admin/inventory_allocation_requests` | 検証時は空（本体側には未完了2件あり→店舗スコープで絞られている）。**作成ボタンなし** |

- 商品管理・発注管理・販売設定・会計・分析・CRM・販売チャネル・設定は**メニューに存在しない**。
- ヘッダーのユーザーメニューは本体と共通（ヘルプセンター/変更ログ/チャットで質問/すべての組織/組織を管理する/アカウントを管理する/ログアウト）。

## 4. 在庫管理（EC在庫確認）

- ホームのカード説明は「EC在庫の有無を確認することができます。」
- 一覧の列: 商品 / 品番(SKU) / （状態バッジ列: `積送中` 等） / 販売可能 / 引当済み / 取置中 / 手持ち。
- 行クリックで**在庫詳細**（`/admin/inventory_items/<id>`）へ。ここは**全ロケーション×9区分**（販売可能/引当済み/取置中/破損/検品/予備/手持ち/積送中/入荷予定）の読み取り専用マトリクス。積送中は `+5` のように符号付き表示。
- 在庫詳細の右上に「**下書き注文を作成**」ボタン（客注の起点）。
- 鉛筆編集（販売可能数の直接編集）は存在しない（読み取り専用）。

## 5. 客注（下書き注文）作成 → 注文化の実測

### 作成フォーム（`/admin/draft_orders/create?product_variant_id=...`）

| セクション | 実測内容 |
|:--|:--|
| 注文明細 | 在庫詳細から遷移するとSKUが1明細（数量1）で自動セット。「追加する」で明細追加。鉛筆アイコン→「商品を編集する」ダイアログ: **単価**（円、編集可）と**数量**（スピンボタン、上限表示 `/92`） |
| 送料明細 | 「追加する」ボタンあり（空状態: 送料明細はありません） |
| メモ | 「メモを編集する」 |
| 顧客 | 「バーコードから顧客を選択する」「顧客を選択する」の2ボタン。顧客選択ダイアログは**メールアドレス完全一致検索**（部分一致 `test` は0件） |
| 配送先住所 | 顧客選択後に出現。「配送先住所を選択する」（顧客の登録住所から）/「配送先住所を作成する」。**未選択のまま保存すると「注文作成に失敗しました、入力情報を確認してください / 配送先住所を選択してください」** |
| 請求先住所 | 自動で「配送先住所と同じ」 |
| 販売員 | 選択中の販売員が自動セット（名前: 銀座ユニクロ）。編集UIなし |
| タグ | 自由入力（Enterで追加） |

### 配送先住所の作成ダイアログ

- 項目: 姓* / 名* / 会社 / 郵便番号* / 都道府県*（セレクト） / 市区町村* / 住所* / 建物名、部屋番号など / 電話番号*
- **郵便番号はハイフン必須**: `1040061` は「郵便番号は半角数字3桁-半角数字4桁の形式で入力してください」で弾かれ、`104-0061` で保存成功（トースト: 配送先住所を追加しました）。

### 数量上限 `/92` の正体（販売閾値ルールの実効を実証）

- 連携設定の在庫ロケーション=ユニクロ物流倉庫 の販売可能=**97**
- 連携設定の販売閾値ルール `TEST_FAQ_販売閾値ルール` のデフォルト閾値=**5**（ルール編集ダイアログで実測。SKU個別閾値は0件）
- ポータルの数量上限 = **97 − 5 = 92** と一致。**販売閾値ルールがリテールポータルの注文可能数を制御していることの初の実機実証**（「販売閾値は現状リテールポータルのみ適用」というStack社説明と整合）。

### 保存 → 下書き注文詳細（`/admin/draft_orders/<id>`）

- 番号 `#202607031000` が採番され、タグに **`リテールポータル (客注)` が自動付与**される。
- ボタン: 「その他の操作」（**バーコードを表示する** / **下書き注文をキャンセルする**）と「**下書き注文を完了する**」。
- バーコードダイアログのタイトルは「**バーコード(NW7)**」。対象SKUにバーコード未登録のため画像は空欄で商品名のみ表示（レジスキャン用途と推定。要バーコード登録データでの再検証）。
- お支払い欄: 小計/送料/税金/合計（今回 4,500円/0円/0円/4,500円。税金の計算条件は未検証）。

### 完了 → 注文化

- 確認ダイアログ: 「この下書き注文を完了して注文を作成します。**この処理は巻き戻すことができません。**」
- 完了すると `/admin/orders/<id>` へ遷移。注文ヘッダーに「**2026年07月03日 03:42 に客注(店舗ポータル) #202607031000 から注文**」と表示。番号は下書きと同一。
- 決済状況は**即「支払い済み」**（決済履歴タブは空。決済トランザクションなしで支払い済み扱い）。出荷状況は「未出荷」。
- タグに `#ON-19` が自動追加（下書きからのタグも引き継ぎ）。
- ポイント「付与対象に含む=はい」（付与0ポイント。ポイントルール未整備環境のため0と推定）、会員ランク「算出対象に含む=はい」。

### 注文化の副作用（本体管理画面側で確認）

| 対象 | 実測 |
|:--|:--|
| 在庫 | **在庫ロケーション（ユニクロ物流倉庫）で 販売可能 97→96、引当済み 0→1**。店舗（銀座店）の在庫は無変化。引当済み=注文済み未出荷の定義どおりの動き |
| 出荷指示 | **`#IO-1052` が注文完了と同時刻に自動作成**。作成元=注文 `#202607031000`、出荷場所=ユニクロ物流倉庫、作業ステータス=出荷待ち、引当ステータス=引当待ち、出荷方法=配送 |
| 本体の下書き一覧 | `/admin/draft_orders` に行が表示される（列: 注文/日時/顧客/店舗/アイテム/**販売員**/注文/合計）。販売員=銀座ユニクロが記録され、完了後は下書きと注文の両方へリンク |
| 本体の注文詳細 | ソース=客注(店舗ポータル)。**「外部システムから連携されている注文です — この注文は外部システムから連携されているため、編集（キャンセル・返品・返金など）は制限されています。連携元システムの管理画面より操作を行ってください。」**の警告バナー。店舗=ユニクロ - 銀座店(R0001)。タブ: 注文情報/返金履歴/出荷指示/在庫依頼 |
| 会計（売上実績） | 注文化直後は**行が増えない**（ポータル売上管理も空）。過去実績では内部注文は出荷完了後に計上されており、売上実績は出荷完了時に計上される仮説を補強（本件は未出荷のため未計上のまま） |
| 下書き注文メタフィールド | 客注が1件できたことで、下書き注文メタフィールド定義の値入力画面を確認できる可能性が出た（今回は未検証） |

## 6. 連携設定と実効の対応（全項目実証）

管理画面のリテールポータル連携「ユニクロ - 銀座店」（状態: 成功/有効）の編集ダイアログ実測値と、ポータル側の実効。

| 設定項目 | 実測値 | ポータルでの実効（実証済み） |
|:--|:--|:--|
| 店舗ロケーション | ユニクロ - 銀座店 | ヘッダー表示・客注の「店舗」・注文/入荷/出荷/在庫依頼一覧の絞り込み単位 |
| 在庫ロケーション | ユニクロ物流倉庫 | **客注完了時の引当先＆自動出荷指示の出荷場所** |
| テナント | ユニクロ | 客注の計上テナント（顧客管理一覧自体は他テナントも表示された点に注意） |
| カタログ | UNIQLO | 在庫管理（EC在庫）に表示される商品の範囲 |
| 販売閾値ルール | TEST_FAQ_販売閾値ルール（デフォルト閾値5） | 客注の数量上限 = 在庫ロケーションの販売可能 − 閾値 |
| 販売員の選択を必須にする | ON | 利用開始時の販売員コード入力ゲート・「画面をロックする」 |
| 配送先住所の編集を許可する | ON | 下書き注文の配送先住所 選択/作成/編集ボタン |
| 送料明細の編集を許可する | ON | 下書き注文の送料明細「追加する」 |
| 注文明細の価格の編集を許可する | ON | 「商品を編集する」ダイアログの単価編集 |
| 下書き注文の完了を許可する | ON | 「下書き注文を完了する」ボタン |

※ 各チェックボックスOFF時に対応ボタンが消えるかは未検証（全ONの環境のため）。

## 7. 今回の検証で訂正・確定した過去記録

| 過去の記録 | 訂正・確定 |
|:--|:--|
| 「下書き注文は現行UIで作成できない」（08/16、2026-07-01） | **下書き注文（客注）の作成入口はリテールポータル側**。本体管理画面の「注文を作成する」ボタンがdisabledなのは変わらず（本体からは作らせない設計と解釈できるが、不具合か仕様かは未確認） |
| 「店舗スタッフ側の画面・操作は接続環境待ち」（19ほか） | 主要画面・客注フロー・設定対応関係まで実測済み |
| 「販売閾値ルールの実効は未確認」（11/19） | リテールポータルの客注数量上限で実効を実証 |
| 「引当済み=注文済み未出荷」（Stack説明） | 客注の注文化で引当済み+1を実測し裏取り |

## 8. 残課題

- [ ] バーコード登録済みSKUでの「バーコード(NW7)」表示（レジ運用の実態）
- [ ] 「バーコードから顧客を選択する」の挙動（カメラ/スキャナ前提と推定）
- [ ] 5つの許可チェックボックスをOFFにした場合のポータル側ボタン消滅確認（設定変更が必要なため未実施）
- [ ] 客注注文の出荷完了→売上実績計上・ポータル売上管理への反映（倉庫側出荷 `#IO-1052` の処理が必要）
- [ ] 客注のキャンセル/返品/返金（本体からは制限。ポータル側の注文詳細に返金系ボタンがあるか未確認）
- [ ] 店舗受取（ローカルピックアップ）の客注での指定方法（今回の下書き注文フォームに受取方法の選択は見当たらず、配送のみだった）
- [ ] 在庫依頼のポータル側での操作（一覧は空・作成ボタンなし。店舗宛の在庫依頼が発生した状態での見え方）
- [ ] 販売員コード誤入力時の複数候補・0件時の文言、販売員の複数店舗紐付け（「店舗を追加する」）の実挙動
- [ ] 別権限（非管理者）ユーザーでのポータルアクセス
