## C-01 — valid (live=false)

**根拠**: SUMMARY.md:95「販売可能 = 手持ち − 確定済み − 販売不可」（マーカー無しで断定）。04-inventory.md:45「販売可能な在庫（= 手持ち - 確定済み - 販売不可）」（マーカー無し）。一方 04-inventory.md:117「4区分...の詳細な定義・計算ロジック<!-- TODO: 要確認 -->」、CONFIRMATION-LIST.md:14「販売可能＝手持ち−確定済み−販売不可...全SKU=0で検算不能」。04-inventory.md:49-51 のサンプルは全て「全0」。同一ファイル内で式を確定提示しつつ計算ロジックを要確認扱いし、確認リストでも未検証（②実データ依存）と分類されており、Codexの「断定の行き過ぎ＋自己矛盾」は記録上の事実。

**対応方針**: 記録の内部整合を取る方向で修正する。式そのものは標準的な在庫恒等式で蓋然性は高いが、全SKU=0で実機検算できておらず確認リスト②（実データ依存）に分類済み。よって SUMMARY.md:95 と 04-inventory.md:45 の断定を緩め、「（推測。全SKU=0のため未検算 / 要確認）」のマーカーを付して 04-inventory.md:117 および CONFIRMATION-LIST.md:14 と表現を一致させる。FAQ本文では確定情報として書かず「要確認」保留とする（CONFIRMATION-LIST.md:6 の執筆ルールに合致）。最終的な式の真偽確定にはテストデータ投入が必要だが、本指摘の対応（マーカー整合）自体は実機操作なしで完了できる。

---

## C-02 — partial (live=false)

**根拠**: 11-operations L17「発注伝票と連携して入荷処理を行う」/L24「入荷指示の作成元: 発注伝票」は推測マーカーなしで発注を起点と断定。一方で同ファイル監査A7（L821-829）が「発注伝票の保存は2026-06-07時点でも不可（バグ未修正・継続中）」「『入荷指示は発注伝票から自動生成』は一度も実証されていない」「実証された入荷指示の生成元は移動伝票のみ」と明記し、矛盾は記録上の事実。ただしL21は既に「自動生成される設計と推測される（推測）」と表記済みで、A7自身が「L21『推測』表記は正しいため変更不要」と結論。SUMMARY.md（L117/L143/L210）とCOMPLEX-OPERATIONS.md（L25「（＋発注管理）」）は2026-06-07付の訂正コメントで既に「移動伝票から生成（実証済み）、発注経由は未実証」へ修正済みで、Codexの言う断定状態は解消されている。

**対応方針**: partial採用。3ファイルのうちSUMMARY.md・COMPLEX-OPERATIONS.mdは既に訂正済みのため対応不要（Codexの「断定で矛盾」は古い状態を見ている可能性）。残る修正対象は11-operations.md冒頭の2箇所のみ：(1) L17「発注伝票と連携して入荷処理を行う」を「移動伝票から入荷指示が自動生成される（実証済み #II-1000）。発注伝票経由は設計上想定されるがGraphQLバグにより未実証」へ書き換え、(2) L24見出し「入荷指示の作成元: 発注伝票」を「入荷指示の作成元: 移動伝票（実証済み）／発注伝票（設計上想定・未実証）」へ修正。L21は既に推測表記で監査A7が変更不要と結論済みのため触らない。実機確認は監査A7のGraphQLエラーログ（L818-823）で完了済みのため不要。

---

## C-03 — partial (live=false)

**根拠**: SUMMARY.md L72「OmnibusCore連携時に在庫ルーティングへ使用される可能性があるが…実体未確認（断定不可）」/ COMPLEX-OPERATIONS.md L76「ロケーショングループが直接ルーティングロジックに使われているかは OmnibusCore連携側の設定次第と推測。<!-- 要確認 -->」/ 13-channels.md L184「注文流入時の在庫引当先をロケーショングループで指定する設計が確定。」(L163ヒント:「ロケーションの指定がない限り設定されたロケーショングループのデフォルトのロケーションから引き当てられます」)

**対応方針**: 部分的に妥当。指摘は「同一の引当ルーティング事実が片方で未確認・片方で確定」と読んでいるが、実際は対象パスが異なる（SUMMARY/COMPLEX-OPSはOmnibusCoreパス、13-channelsはShopifyパス）ため完全な矛盾ではない。ただし13-channels:184の「設計が確定」は行き過ぎ。確認できた事実はShopifyフォームにロケーショングループが必須欄として存在することと、フォーム上のヒント文言（L163）のみで、実際の引当動作はOAuth未接続で未実行（L187に未確認TODOあり）。修正方針: 13-channels:184の「設計が確定」を「フォーム上はロケーショングループ必須欄として存在し、ヒント文言でデフォルトロケーションから引き当てる旨が明示されている（実際の引当動作はOAuth未接続のため未検証）」へ緩和し、SUMMARY:72・COMPLEX-OPS:76の未確認表記と齟齬しないよう整合させる。実機操作は不要（記録内の文言整理で対応可）。

---

## M-01 — partial (live=true)

**根拠**: SUMMARY.md L72-76は「ロケーショングループ=空」「取引先=空」「決済方法=未確認」「販売員=未確認」、14-settings.md L23-27も同じ記載。一方13-channels.mdの各接続フォームでは L163「ロケーショングループ … 選択肢:『TEST_FAQ_ロケーショングループ』（1件）」、L208も同様、さらに14-settings.md L587/L598で「TEST_FAQ_ブランド」「コード TEST_FAQ_B」が一覧に存在。つまりロケーショングループとブランドは『空/未確認』記載と矛盾しTEST_FAQレコードが実在する。ただし『取引先』はチャネルフォームに登場せず（フォームはテナント/カタログ/ロケーショングループ/販売価格ルール参照）、決済方法/販売員は14-settings.md L635/L665で作成フォームの構成のみ確認＝レコード有無は未確定で、これらに対する反証は本3ファイルにはない。

**対応方針**: 指摘の核（マスタ現状スナップショットが後続調査と不整合）は妥当だが対象が広すぎる。確実に矛盾するのはロケーショングループ（13-channels L163/L208でTEST_FAQ_ロケーショングループが1件存在）とブランド（14-settings L598でTEST_FAQ_ブランド存在）の2項目のみ。SUMMARY.md L71-77の「現状データ」表を更新し、(1)ロケーショングループを「空」→「1件（TEST_FAQ_ロケーショングループ、調査中作成のテストデータ）」、(2)ブランドを「未確認」→「1件以上（TEST_FAQ_ブランド）」に訂正。あわせて「調査中に各マスタへTEST_FAQ_テストデータを作成済み」の注記を表脚に追加し、14-settings.md L23/L24も同様に同期する。一方、取引先・決済方法・販売員はこの3ファイルでは反証がないため「空/未確認」のまま据え置き可（Codexの当該3項目への言及は却下）。正確な現在件数の確定には実機での各マスタ一覧の再確認が必要。

---

## M-02 — valid (live=false)

**根拠**: 3ファイルとも冒頭に「読み取り専用」と明記（00 L3「**読み取り専用**」、11 L3「調査日: 2026-06-05 | 読み取り専用」、13 L3「| 読み取り専用」「L148 読み取り専用（外部連携の実接続・解除は行わない）」）。しかし実機では多数の書き込みを実施。11-operations.md L430-614で取り寄せ販売フローをエンドツーエンド実証（L449「在庫依頼を作成しました」、L561「入荷を完了しました」、L589「出荷を完了しました」トースト）、L253/L686「TEST_FAQ_Supplier2 を作成済み（発注フロー検証用）」、L331-343 調整伝票#IA-1000（増減数+5、実行者:河野陽介、実施済み）。13-channels.md L386「空のカラーオプション行を3件含む状態で保存ボタンを押したところ…TEST_MAKER_001 のレコードが作成された（意図せずデータ作成。以降の操作では削除・破壊操作は行わない）」。CLAUDE.mdの「分析は読み取り専用。データの作成・更新・削除は行わない」方針にも反する。

**対応方針**: 3ファイル冒頭の「読み取り専用」表記を実態に合わせて訂正する。具体的には「本分析は読み取り専用を原則としたが、取り寄せ販売フローの動作検証のためTEST_接頭辞のテストデータ作成・伝票登録（在庫依頼/引当/移動伝票/入荷・出荷実績/調整伝票#IA-1000/取引先TEST_FAQ_Supplier・Supplier2/OmnibusCore TEST_MAKER_001等）を実施した。実顧客データの破壊・削除・外部連携の実接続は行っていない」と注記する。13 L386の「意図せずデータ作成」は特に明示残すこと。実機操作は不要（記録上の文言整合の問題のため）。なお書き込み自体の妥当性検証ではなく記述の矛盾解消が目的なのでneedsLiveCheckはfalse。

---

## M-03 — partial (live=false)

**根拠**: COMPLEX-OPERATIONS.md L28「確定フロー（2026-06-05 実機エンドツーエンド実証済み）」、11-operations.md L595「エンドツーエンド フロー確定（全ステップ実証済み）」。一方 L584「顧客通知の文言なし（内部移動なので顧客通知はない）」、L487「在庫区分の変化（推測）: …『販売可能』区分から2個が『確保済み』区分へ移動」。実証の入口は実注文でなく手動作成の在庫依頼（L436「在庫依頼を作成する」）。会計・返品はSUMMARY.mdで別領域として記載され当フローに接続なし。SUMMARY L150「在庫依頼経由と発注経由の関係はエンドツーエンドの通し検証が必要（残課題）」。

**対応方針**: 妥当（指摘の方向は正しいが記録は完全に無自覚ではないためpartial）。「エンドツーエンド実証済み」の表記が、実際は社内在庫移動オペレーション（在庫依頼→引当て→移動伝票→出荷実績/入荷実績）の通し実証に限られる範囲を過大に見せている。対応: (1) COMPLEX-OPERATIONS.md L28・11-operations.md L430/L595の見出しを「在庫移動オペレーションの社内フロー実証済み」等にスコープ限定し、実注文起点・顧客通知・在庫増減（L487は推測）・会計・返品は含まないと明記する。(2) 在庫区分の変化は推測（L487）であり実証ではない旨をE2E表現の近くに注記。(3) 既存の残課題記述（SUMMARY L150/L246の発注経由vs在庫依頼経由の通し未検証）と整合させる。新規実機操作は不要。

---

## M-04 — valid (live=false)

**根拠**: COMPLEX-OPERATIONS.md:89「何をする運用か: 在庫ゼロでも予約販売ルールを設定すればShopify上で販売を継続し、入荷時に相殺する。」（断定文・インライン注記なし）。一方 CONFIRMATION-LIST.md:28 は同じ挙動を「③ 外部接続依存」配下で未確認扱い:「予約販売（バックオーダー）の入荷相殺: 在庫0でもShopifyで販売継続→入荷時に相殺する実挙動（議事録の重要運用）。」。08-sales-settings.md で実機確認できたのはルールUIのみ（作成フォーム・詳細画面・バリエーション追加フォームの「販売数」＝GraphQL quantity、API description「予約販売が可能な在庫数の固定値」L644-645）で、Shopify販売継続・入荷相殺の実挙動の確認記述は無し。チャネルリストも空（L307）。

**対応方針**: Codexの指摘は妥当。COMPLEX-OPERATIONS.md:89の冒頭断定文を、確認できた事実（予約販売ルールUI＝対象SKUに「販売数」を設定する機能の存在）と未確認の仮説（Shopify販売継続・入荷相殺の実挙動）に分離する。具体的には L89 を「在庫ゼロでも予約販売ルールを設定すると、SKUごとに予約販売可能数（販売数）を指定できる。これによりShopify上で販売を継続し入荷時に相殺する運用が想定されるが、Shopify連携の実挙動は未確認（議事録ベースの仮説）」のように仮説であることを明記する書き換えにする。なお同ファイルL94-97には既に「実証TODO」ブロックがあり相殺の実挙動はShopify連携要として未確認と記載済みなので、L89の断定文だけを整合させれば足りる。実機操作は不要（記録内の整合修正のみ）。

---

## M-05 — partial (live=false)

**根拠**: COMPLEX-OPERATIONS.md L103「既存のShopify連携商品をSQへ取り込む。店舗在庫のみの商品はカタログで制御。」は推測/未確認マーカー無しの断定文。ただし直後のL104「現状: Shopify未接続。」、L108-110「確定すべき点 <!-- 要ユーザー確認（外部連携）-->: …カタログでの「店舗在庫のみ商品」の制御方法」で既に要確認扱い。CONFIRMATION-LIST.md L29「商品データ移行（Shopify→SQ）: 取り込みの入口、カタログによる「店舗在庫のみ商品」の制御。」も③外部接続依存で未確認と明記。03-products.md A-06 L747「OmnibusCore経路は確定。Shopify/スマレジ/リテールポータルは未接続で未確認」。

**対応方針**: 部分的に妥当。L103の説明文が「カタログで制御」と断定形である点はCodexの指摘どおり（Shopify側カタログ経路は未接続で未確認）。ただしファイルは同セクションL104/L108-110とCONFIRMATION-LIST L29で同事項を「要ユーザー確認（外部連携）」と既に明示しており、反映漏れではなく説明文と確定すべき点の整合の問題。対応はL103の文末表現を「店舗在庫のみの商品はカタログで制御すると想定される（未確認・Shopify未接続）」等の推測表記に修正するだけで足りる。実機操作は不要（Shopify実接続は別途必要だが本指摘の修正には不要）。invalidではない。

---

## M-06 — partial (live=false)

**根拠**: CONFIRMATION-LIST.md は ②実データ依存/③外部接続/④環境バグ の3節のみで、返品時ポイント・ディスカウント併用・決済方法効果・採寸割当の項目は一切ない（grep結果: 「ポイント」は L16『顧客詳細画面（注文履歴/ポイント残高…）』のみ）。一方で詳細ファイルには未確認記録が存在: 12-crm.md L849『返品・キャンセル時のポイント挙動（減算・失効・保留）は実データがある環境で要確認』、L914『FAQ論点: クーポンコードの文字種・長さバリデーション（未検証）、複数ディスカウントの併用可否』、14-settings.md L658-659『支払い待ち…出荷処理を進められる（推測）』『代引き…代金引換扱いになる（推測）』、L391『採寸ルールを個別商品に紐付けるフローは存在しない…API経由…と推測』。ただし返品導線は CONFIRMATION-LIST L15『返品/キャンセル/交換の起票導線（注文0件で詳細に入れない）』で既に反映済み。

**対応方針**: 概ね妥当（partial）。Codexが挙げた5項目のうち「返品導線」はCONFIRMATION-LIST L15に既出のため反映漏れではない。残り4項目は詳細ファイルに未確認/推測として明記されているのにリスト本体に欠落しており、リスト冒頭L5『残り(約92件)を確認先別に整理』『FAQでは確定情報として書かない』という宣言と齟齬がある。対応: CONFIRMATION-LISTに以下を追記する。②実データ依存節へ『返品/キャンセル時のポイント挙動（減算・失効・保留）の確認（12-crm.md L844-849）』。FAQ論点/開発元確認として『複数ディスカウントの併用可否・クーポンコードの文字種/長さバリデーション（12-crm.md L914）』。UIで未確認の挙動として『決済方法の「支払い待ちでも注文を出荷する」「代引き」チェックの実際の効果（14-settings.md L656-659、現状は推測）』『採寸ルールの商品割り当てフロー＝管理画面UIに存在せずAPI/連携側処理と推測（14-settings.md L388-391）』。返品導線については却下（既反映）。新規実機確認は不要で、既存記録の集約のみ。

---

## M-07 — valid (live=false)

**根拠**: CONFIRMATION-LIST.md L4-5: 「speculation-killer により『UIで確認可能な推測62件』は実機で解決済み（✓実機確認マーカー 256件）。…検証できない残り（約92件）」。この3数値はヘッダー前文のみに出現し、以下の本文箇条書き（②9件+③7件+④1件＝計19項目）とは一切対応しない。さらに実測では `grep -roh "✓実機確認" *.md | wc -l` = 130 で、「256件」と乖離。62/92の内訳・算出根拠を示すログやファイルは _analysis 配下に存在しない。

**対応方針**: 対応すべき。CONFIRMATION-LIST.md L4-5 の「62件」「256件」「約92件」は(a)本文19項目と対応せず、(b)256はリポジトリ実測値130と矛盾、(c)算出根拠の記録なし。修正案: 検算不能な3数値を削除し、前文を「speculation-killer により UIで確認可能な推測は実機で解消済み。本リストは staging単体・読み取り系では原理的に検証できない残項目を確認先別に整理したもの」と件数表現を外して書き換える。件数を残すなら本文項目数と一致させ（例:「以下19項目」）、✓実機確認マーカー数も実測値に合わせる。SQ-調査報告.html L426 の同一前文も同様に修正。FAQ本文へは流入していない内部記録なので実機確認は不要。

---

## M-08 — valid (live=false)

**根拠**: SUMMARY.md:248「各種「作成フォーム」のうち未到達のもの（移動伝票・バリエーション・会員ランク等の細目）。」と残課題に列挙。しかし詳細ファイルでは調査済み: 04-inventory.md:329「移動伝票 作成フォーム（/admin/inventory_movement_orders/create）」＋入力項目テーブル・スクショあり/03-products.md:265「バリエーション作成フォーム…の全項目」＋158「解決済み: 2026-06-05追記参照」/12-crm.md:224「会員ランク算出ルール作成フォーム…」＋全項目テーブル・スクショ（crm-rank-create.png）あり。いずれも2026-06-05〜06-07の深掘りで実機確認済み。

**対応方針**: SUMMARY.md:248を修正する。「移動伝票・バリエーション・会員ランク」は既に詳細ファイル（04/03/12）で作成フォームの全項目・スクショ付きで調査済みのため、残課題リストから削除する。あるいは「等の細目」が真に未到達の項目を残すなら、具体名（実際に未確認のもの）に差し替える。SUMMARYは深掘り調査（2026-06-05〜06-07追記）の結果が反映されておらず古い状態なので、第7章「残課題」全体を詳細ファイルの<!-- TODO -->残存分のみと突き合わせて棚卸しするのが望ましい。実機操作は不要（記録同士の整合修正のみ）。

---

## M-09 — valid (live=false)

**根拠**: 15-csv-pdf.md:869「| 予約販売 | 予約販売ルール | 選択肢なし（本環境にルールが未登録） |」（841行の見出し「実測結果（2026-06-06）」配下）に対し、13-channels.md:209は「~~選択肢なし（空）~~ **「TEST_FAQ_予約販売ルール」（1件）**【訂正 2026-06-07: 調査後にマスタ作成済みのため現在は1件存在】」、08-sales-settings.md:259は「TEST_FAQ_予約販売ルール_遷移確認_20260607 で実測」と予約販売ルールの実在を確認済み。

**対応方針**: 反映漏れ（13は2026-06-07に訂正済みだが15が旧状態のまま残存）。15-csv-pdf.md:869の「選択肢なし（本環境にルールが未登録）」を、13-channels.mdと同様に訂正注記付きで更新する。例:「~~選択肢なし~~ 「TEST_FAQ_予約販売ルール」（1件）【訂正 2026-06-07: 調査後にマスタ作成済み。CSVインポート（予約販売バリエーション）の必須ドロップダウンでも選択可能になった】」。なお15のL276/282/291ではCSVインポート側で「予約販売ルール」が必須セレクトと記載されており、ルール未登録だとインポート不可だった点も整合する形で補足するとよい。実機再確認なしで記録内の訂正トレイルから現状（1件存在）が確定できるためneedsLiveCheck=false。

---

## M-10 — partial (live=true)

**根拠**: L629「TEST_MAKER_001が『意図せず作成』されたレコードであっても、詳細画面の構成…はOmnibusCoreの一般的な接続設定として正しく表示されている。接続方法（正規/意図せず）によって画面構成は変わらないと判断できる。✓実機確認済み」。一方L386「保存が完了し TEST_MAKER_001 のレコードが作成された（意図せずデータ作成）」、監査Fの照合(L614-623)は同じTEST_MAKER_001を再確認したのみで、正規作成レコードとの比較は行われていない。

**対応方針**: Codexの指摘は概ね妥当。L629の「接続方法（正規/意図せず）によって画面構成は変わらないと判断できる。✓実機確認済み」は、正規作成レコードを一度も観察せず同一の意図せずレコードを再確認しただけなので、比較根拠を欠いた推論であり「✓実機確認済み」の付与は過剰。ただし(1)画面構成はテンプレート描画で作成経路に依存しにくいという推定自体は自然、(2)文末が「判断できる」と一部ヘッジされている点で完全な事実誤認ではないため partial。対応: L629を「画面構成はテンプレート由来と推測されるが、正規接続レコードでの比較は未実施のため断定は避ける」と緩和し、「✓実機確認済み」を「推測（要確認）」へ変更、<!-- TODO: 正規作成レコードでの画面構成比較は未実施 --> を追記。なお、この観察は内部の分析記録であり、ユーザー向けFAQ本文にはTEST_MAKER_001等のテストレコード前提の一般化を持ち込まないこと。確定にはアプリ上で正規に作成した接続レコードの詳細画面構成を確認する実機操作が必要。

---

## M-11 — valid (live=true)

**根拠**: L1032「✓実機確認(2026-06-07): 同じ作成フォーム構造（INPUT×4 + SELECT 13オプション）が全16オブジェクトで共通（注文・顧客・バリエーションの3オブジェクトで抜き取り確認済み…）」。対象オブジェクトは L991-1006 で16件列挙されている。

**対応方針**: Codexの指摘は事実。「全16オブジェクトで共通」と断定しているが実機確認は3オブジェクト（注文・顧客・バリエーション）のみで、範囲を超えた一般化。ただし同一文で抜き取り3件であることを自己開示しており、隠れた誤りではない。対応は文言の緩和で足りる: 「全16オブジェクトで共通」→「確認した3オブジェクト（注文・顧客・バリエーション）で同一構造。他オブジェクトも同様と推測されるが未検証」等に修正。全16共通を確定させたい場合のみ残り13オブジェクトの作成フォームを実機で抜き取り確認（needsLiveCheck=true）。

---

## M-12 — partial (live=true)

**根拠**: L386「データ0件の空状態でもフィルター選択肢はUI定義として固定されており…注文データが増えても選択肢の種類は変わらない（選択肢はデータ依存ではなくUI設計で固定）と判断。ただし将来的なUI変更の可能性は排除できないため、「現時点での実測値」として記録。」確認は0件の空状態（L386「データ0件の空状態でも」）で実施。

**対応方針**: Codexの主張は事実関係としては正しい。記録は0件の空状態DOMで確認した2種を根拠に「データ増加後も種類は変わらない」と判断しており、この「データ依存ではなくUI設計で固定」は空状態の観察だけからは証明できない推論（断定の行き過ぎ気味）。ただしCodexが触れていないが、記録は同じ文で「将来的なUI変更の可能性は排除できない」「現時点での実測値」と留保を付しており、無条件の断定ではない。よってvalidではなくpartial。対応: L386の「注文データが増えても選択肢の種類は変わらない…と判断」を「空状態でこの2種が定義されていることを確認。これらがUI設計上固定か、データに応じて選択肢が増えるかは空状態のみでは断定できない」と推論を観察事実と分離する表現に弱める。確定にはデータ投入後の再確認が必要だが、読み取り専用方針のため実機での返品データ作成は不可。FAQ本文（faq/配下）に種類数の断定が転記されていれば同様に「現時点の実測値」と留保を付ける。

---

## M-13 — partial (live=false)

**根拠**: SUMMARY.md L109「Shopify等のチャネル経由で自動生成される設計と推測（未検証）。会社の『担当者』は purchasing_customers とは別エンティティ（…2026-06-07実証済み）」、05-orders.md [照合7] L436「『購入顧客データが必要』という推測は依然として検証不可（Shopify接続環境が必要）だが、会社・担当者では解放されないことは確定した」、06-customers.md [照合5] L428「『顧客はチャネル経由でのみ自動生成』という推測は依然として検証不可…『担当者≠顧客』は確定」。一方 05-orders.md L40 は推測マーカーなしで「（ただし購入顧客はShopify等のチャネル連携で生成されるため、Shopify未接続環境では解放不可）」と断定調。

**対応方針**: Codexの懸念の大半は既に対応済み。記録は「担当者≠顧客（実証済み）」「会社・担当者データでは下書きボタン未解放（実証済み）」を確定事実、「顧客はチャネル経由でのみ生成」「顧客があれば下書き作成可」を未検証の推測として正しく分離しており、SUMMARY/05/06 の主要箇所に訂正コメントと（推測・未検証）マーカーが付与済み。よってvalidではない。ただし残存不整合が1点: 05-orders.md L40 の主要アクション内パラグラフが「購入顧客はShopify等のチャネル連携で生成されるため」と推測マーカーなしの断定調で、同一文書 L436/L439「引き続き推測のまま」と齟齬する。対応: L40 を「（購入顧客は Shopify等のチャネル連携で生成される設計と推測。未検証のため Shopify未接続環境では解放可否を実証できていない）」へ文言統一する修正のみで十分。実機操作は不要（Shopify接続環境での最終確認は将来TODOとして既に明記済み）。

---

## M-14 — partial (live=true)

**根拠**: 09-accounting.md L61「手動での売上実績作成は現在の実装では不可。売上実績は注文・CSVエクスポートから自動生成される設計と推定。」/ L57「『売上実績を作成する』は**disabled状態の `<a>` タグ**」/ L111「両者が同一データソースかは未実証のまま。<!-- ②実データ依存 -->」/ SUMMARY.md L132「手動記録か自動集計か、請求・入金まで含むか。」（FAQ論点として明記）。請求/入金/返金との関係への直接の記述は記録上に存在しない。

**対応方針**: 却下せず部分採用。Codexの「未確認」という事実認識は記録と一致するが、記録側は既に正しく扱っている（ボタンが無効=disabled `<a>`タグ・/create は404 は実機確定、自動集計/手動作成/同一データソースは『推定・未実証』マーカー＋②実データ依存TODOで明示）。よってM-14は記録の誤りではなくFAQ執筆時の注意喚起として扱う。対応: (1) 09-accounting.md L36/L61/L81 の『推定/推測』表現はそのまま維持し、faq-writer が会計FAQで「自動集計」「請求・入金・返金まで含む」を断定しないよう徹底する。(2) 請求/入金/返金との関係は記録に一次根拠が無いため、記録に無い概念をFAQで導入しない（憶測禁止ルール準拠）。(3) 実データ（注文あり）環境でdisabled解除条件・列構成・sale_change エクスポートとの同一データソース性を確認する残TODO（L65/L102/L111）を実機で潰す。ボタン無効・404・エクスポートフォーム項目は実機確定済みなのでFAQに記載可。

---

## M-15 — valid (live=true)

**根拠**: 05-orders.md L305「`orders:read` のみ（write なし）でログインした場合の注文画面差異（返品/キャンセルボタンの表示/非表示等）は今回のセッション（特権管理者）では実測不可」／COMPLEX-OPERATIONS.md L153「権限グループとの具体的な紐づけ方法は未確認 <!-- TODO: 要確認 -->」／同L148-149「カスタムグループでは全70権限が enabled」「権限は read と write が分離」

**対応方針**: 指摘は記録上の事実と一致しており妥当。ただし「反映漏れ・断定の行き過ぎ」の類ではなく、記録側はすでに限界を明記している（05-orders L305で画面差異は実測不可、COMPLEX-OPERATIONS L153でリテールポータル紐づけは未確認TODO）。よって記録の訂正は不要。対応はFAQ執筆方針の確認に限定: 権限グループのFAQは「作成手順・70権限・read/write分離」までを確定事実として書き、「店舗スタッフ権限でログインした際の画面差分（ボタン非表示等）・リテールポータルとの具体的紐づけ」は未検証として断定を避ける（または実機確認後に追記）。実機確認するなら制限権限グループを実メンバーに割り当て→そのアカウントでログインして画面差分・リテールポータル接続を検証する必要があるためneedsLiveCheck=true。

---

## m-01 — valid (live=false)

**根拠**: L850: 「D: 検索型選択 + ファイル | … | カタログ / キャンペーン対象商品」でカタログはパターンD。一方L852: 「注: パターンCのカタログは `<select>` による直接選択（プルダウン型）、キャンペーン対象商品はダイアログ検索型。」とパターンCと記載。grepでもカタログがパターンC行(L849)に出現しないことを確認。

**対応方針**: L852の「パターンCのカタログ」を表(L850)と整合させる。ただし注の趣旨はカタログが実態として`<select>`プルダウン型（C寄り）で、ダイアログ検索型は本来キャンペーン対象商品のみという指摘に見える。最も整合する修正は、注の主語をパターン表に合わせて「パターンDのカタログは実際には`<select>`による直接選択（プルダウン型）で、ダイアログ検索型はキャンペーン対象商品のみ」と書き換えること。あわせて、カタログが本当にプルダウン型なら表のパターン分類（D=ダイアログ検索型）との不一致が残るため、カタログをBまたはC系に再分類するか、表に脚注を付けて「カタログは選択UIが`<select>`型で例外」と明示するのが望ましい。実機確認は不要（記録内の自己矛盾の修正で対応可）。

---

## m-02 — partial (live=false)

**根拠**: 07:73「成功トースト文言: 「取引先を作成しました」」/ 11:682「成功トースト文言: 「取引先を作成しました」」に対し、11:255「取引先作成後、画面右下に「取引先が作成されました」（推測）のトーストが表示される<!-- TODO: スクショ…取引先一覧画面に遷移後のトーストと推測 -->」。文言が「を作成しました」と「が作成されました」で揺れている。

**対応方針**: partial判定の理由: 文言揺れの指摘自体は記録上の事実だが、Codexは255が「推測」明示・TODO付きである点を伝えていない。255の「取引先が作成されました（推測）」は、後から実機確認された確定値（07:73・11:682の「取引先を作成しました」）と矛盾する古い推測記述。対応: 11:255の文言を確定値「取引先を作成しました」に統一し、（推測）表記とTODOコメントを削除（または確定済みである旨に更新）する。実機再確認は不要（同一プロジェクト内に確定記録が2件あるため）。

---

## m-03 — valid (live=false)

**根拠**: SUMMARY.md:258「**スクショ取得**は実機操作前提（既に11+3枚取得済み、本制作ではさらに大量に必要）。」/ SUMMARY.md:272「| `screenshots/`（14枚） | 主要画面のスクリーンショット |」。実際の screenshots/ には画像ファイルが396枚存在（find で png/jpg等を計数）。

**対応方針**: 記録上の事実誤認として妥当。実機操作は不要（ローカルのscreenshots/を数えれば確定）。修正案: 258行目「既に11+3枚取得済み」→実枚数（screenshots/に約396枚）に更新、もしくは「多数取得済み（screenshots/参照）」のように具体枚数を固定しない表現へ。272行目「（14枚）」→「（約396枚）」または「（screenshots/参照）」に更新。今後ズレを防ぐため、付録表は枚数固定値を書かずディレクトリ参照に統一するのが望ましい。

---

## m-04 — valid (live=false)

**根拠**: 00-admin-sitemap.md L87 は今も「**未踏（要再訪）**: 注文詳細・顧客詳細（データ0件で未確認）/ 入荷作成フォーム / 権限グループ / 翻訳・採寸ルール / CSVインポート個別フォーム / Recustomer連携フォーム」と記載。しかし権限グループ=14-settings.md L144「**実証済み（2026-06-06）**」、翻訳=L301「翻訳ルール 詳細画面（保存後）✓実機確認(2026-06-07)」、採寸ルール=L366「✓実機確認(2026-06-07)」、CSVインポート個別フォーム=15-csv-pdf.md L127〜「## CSVインポート 個別フォームの構成（補完調査）」で各カテゴリ詳細化、Recustomer=14-settings.md L398〜＋05-orders.md L392「[照合4] Recustomer 接続フォームの必須項目（バリデーション文言の実測）…✓記録L213-216は正確」と、いずれも後続で補完・実機確認済み。

**対応方針**: 00-admin-sitemap.md L87 の「未踏（要再訪）」リストから、後続ファイルで補完済みの5項目（権限グループ／翻訳／採寸ルール／CSVインポート個別フォーム／Recustomer連携フォーム）を削除する。各項目の完了根拠（14-settings.md・15-csv-pdf.md・05-orders.md）への参照を添えると追跡性が上がる。なお「注文詳細・顧客詳細（データ0件で未確認）」と「入荷作成フォーム」は05-orders.md等で依然未確認のため残置が正しく、リスト全体の削除ではなく該当5項目の除去にとどめる。実機操作は不要（解消事実は記録上で完結）。

---

