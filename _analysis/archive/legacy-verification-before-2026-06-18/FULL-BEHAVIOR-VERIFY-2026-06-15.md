# 全挙動確認メモ（2026-06-15）

対象: staging 管理画面。Playwrightでログイン後、一覧・タブ・詳細・作成フォーム・確認ダイアログ・空保存バリデーション・安全な状態遷移を実操作した。

## 確認済みの範囲

| 領域 | 確認内容 | 結果 |
|:--|:--|:--|
| URL到達性 | 主要URL、作成URL、未実装URLを巡回 | 分析/卸売はTODO表示。`/admin/inventory_suppliers` は404で、正しくは `/admin/settings/suppliers`。注文/下書き/顧客の直作成URLはエラーまたは404 |
| 商品管理 | 一覧タブ、作成時ステータス、空保存、非公開作成、アーカイブ解除、再アーカイブ | 作成時「非公開」は詳細で「アーカイブ済み」。解除後は「下書き」。空保存はボタン押下後にインラインエラー |
| 調整伝票 | 未実施/実施済み/キャンセル、実行確認、未実施キャンセル、実行済みキャンセル押下 | 未実施の「キャンセル」は確認なしで即時キャンセル。実施済み詳細にも「キャンセル」ボタンは表示されるが、押すと「キャンセルできない在庫調整伝票が含まれています...」のトーストが出て実施済みのまま |
| 取置伝票 | 新規作成、保存、処理済みマーク、確認ダイアログ | 「処理済みとしてマークする」→「実行する」で処理済みになり、取置中在庫を解放 |
| 移動伝票 | 単体作成、出荷実績、入荷実績、在庫変化、完了後詳細 | `#IM-1006` を作成し、関連 `#IO-1006` / `#II-1006` を最後まで処理。移動伝票は「成功 完了 / 入荷完了」になった |
| 出荷管理 | 一覧タブ、出荷待ち/出荷完了詳細、実績登録フォーム、空の配送キャリア/追跡コード登録 | 出荷実績は配送キャリア・追跡コード空でも登録可。登録後は「成功 完了 / 出荷完了」になり、再登録ボタンはdisabled |
| 入荷管理 | 一覧タブ、入荷待ち/入荷完了詳細、入荷実績登録、一括受領 | 入荷待ちで受領数を入力して登録すると「成功 完了 / 入荷完了」。トーストは「入荷実績を登録しました」 |
| 在庫依頼 | タブ、空状態、既存データの状態 | 確認待ち/確保済み/終了のタブを確認。引当済み後の行き止まり仕様は既存検証と整合 |
| CSVインポート | トップカテゴリ、空保存、在庫移動伝票カテゴリ、販売可能在庫CSVの検証/実行/在庫反映 | 22カテゴリ。販売可能在庫CSVは保存後に検証、検証成功後に確認ダイアログを経て実行。`SET 1` で在庫反映、`SET 0` で復元まで確認 |
| CSV/PDFエクスポート | CSVカテゴリ、売上実績ドロップダウン、売上実績（注文軸）実行、PDF納品書履歴、出荷条件指定フォーム | CSVは9カテゴリ。売上実績（注文軸）は実行後「処理中」→再読み込み後「成功 完了」+ダウンロードリンク。ヤマト/PDFは実行押下後も履歴に即時レコードなし |
| 注文/下書き/返品/顧客 | 一覧、空状態、作成入口、直URL | 注文/下書き/顧客の直作成URLはエラー。返品作成URLは404。下書き作成ボタンは href なし |
| 外部連携 | Shopify/OmnibusCore/スマレジ/リテールポータル/Recustomer/Logizard の空保存 | 必須項目のバリデーションまで確認。実接続は未実行 |
| 会員ランク | 作成、Bronze自動生成、Bronze編集 | 算出方法は購入金額固定。Bronzeは表示名のみ編集可、達成条件と削除は不可 |
| 設定系 | apps/brands/location_groups/locations/notification/payment/permission/measurement/staff/suppliers/translation/local pickup/excluded products | 空保存バリデーションと選択ダイアログを確認 |

## 実操作で環境に残したデータ・状態

| 種別 | 内容 | 状態 |
|:--|:--|:--|
| 調整伝票 | 未実施伝票 `#IA-1001` の「キャンセル」を押下 | 確認ダイアログなしでキャンセル済みになった |
| 調整伝票 | `#IA-1002` を +1 で作成・実行、`#IA-1003` を -1 で作成・実行 | テストロケーション在庫は元に戻した |
| 移動伝票 | `#IM-1006` を作成し、`#IO-1006` 出荷実績、`#II-1006` 入荷実績まで登録 | 移動伝票は完了。テスト在庫は直接編集で元に戻した |
| 取置伝票 | テスト取置伝票 `#IR-1002` を作成し、処理済みに変更 | 処理済み |
| 商品 | `TEST_FAQ_STATUS_20260615080916` を作成 | アーカイブ解除後、再アーカイブし、最後に削除済み |
| 会員ランク算出ルール | `TEST_FAQ_RANK_20260615081841` を作成 | Bronze初期ランク付きで残存 |
| CSVインポート | 販売可能在庫CSVを `SET 1` → `SET 0` で実行 | テストロケーションの販売可能/手持ち 0→1→0 を確認 |
| CSVエクスポート | 売上実績（注文軸）を 2026-06-15 00:00〜23:59 で実行 | 履歴が「成功 完了」になり、ダウンロードリンクが表示された |

## 未実行にした操作

| 操作 | 未実行理由 | 確認したところ |
|:--|:--|:--|
| ブランド削除など、テストデータではない削除系 | 復元不可 | メニュー/確認ダイアログまで |
| 移動伝票の「キャンセルする」 | 出荷指示・入荷指示まで連動キャンセル | 確認ダイアログまで |
| 入荷指示の一括受領完了 | 未受領分を完了扱いにし、巻き戻し不可 | 完了画面まで |
| ヤマトB2クラウド/PDFの実ファイル受領確認 | メール確認が必要 | 実行ボタン押下、出荷一覧への戻り、履歴画面の即時表示なしまで確認 |
| Shopify等の外部連携実接続 | 外部サービス接続・OAuth/認証情報を伴う | 空保存バリデーションまで |

## 文書へ反映した差分

- CSVインポートを21カテゴリから22カテゴリへ修正し、在庫移動伝票インポートを追加。
- CSVエクスポートを8カテゴリから9カテゴリへ修正し、売上実績（明細軸）を追加。
- 商品の「非公開」作成、アーカイブ解除後の「下書き」復帰、空保存時のエラー挙動を修正。
- 調整伝票は実施済みでもキャンセルボタンが表示されるが、押すとキャンセル不可トーストが出る実挙動へ修正。
- 取置伝票の処理済みマーク操作と在庫解放を確定記述に変更。
- 移動伝票の単体作成から出荷/入荷完了までのステータス・在庫変化を追記。
- 販売可能在庫CSVインポートの検証/実行/確認ダイアログ/在庫反映を追記。
- 商品削除ダイアログと削除後の直接URLエラーを追記。
- 下書き注文/売上実績作成リンクは `href` なし、直URLはエラー/404として修正。
- PDF/ヤマトB2クラウド条件指定エクスポートは実行押下後も履歴画面に即時レコードが出ない実測を追記。

## 残リスク

- 注文・顧客・売上実績の実データが0件のため、詳細画面、注文起点の在庫引当、返品起票、売上集計は未確認。
- 外部連携は接続前バリデーションまで。接続後の同期・エラー回復・注文流入は未確認。
- CSVインポート「在庫移動伝票」のテンプレート有無と列定義は追加確認が必要。
- 一部のステータス（出荷の保留中/依頼済み/作業中/欠品、入荷の入荷依頼済み/入荷作業中/要対応、移動の一部受領済み）は、該当データがなく遷移条件未確認。
