# 未確認項目 追加進捗レビュー 19（Codex / Playwright）

対象: 取置伝票作成・確保済み在庫への反映可否  
確認日: 2026-06-09  
対象URL: `https://www.sqstackstaging.com/admin`

## 証跡

- 実行スクリプト: `faq/_analysis/deep-screen-verification-20260609/reservation-order-create-probe.mjs`
- 記録JSON: `faq/_analysis/deep-screen-verification-20260609/reservation-order-create-records.json`
- スクショ: `faq/_analysis/deep-screen-verification-20260609/reservation-order-create-screenshots/`
- 記録数: 11
- スクショ数: 11
- 失敗: なし
- 400以上のネットワーク応答: なし
- 実行した変更: なし。保存試行はバリデーションで止まり、取置伝票作成Mutationは実行されなかった。

## 確認結果

| 項目 | 実画面での現在挙動 | 判定 |
|:--|:--|:--|
| 作成前在庫 | SKU `486125-09-XL` の `物流倉庫` は `販売可能1 / 確定済み0 / 確保済み0 / 手持ち1`。 | 取置検証の前提 |
| 取置伝票一覧 | `/admin/inventory_reservation_orders` は空状態。タブは `すべて`、`未処理`、`処理済み`。 | 既存レビュー9と一致 |
| 取置伝票作成フォーム | `/create` は `取置伝票を作成する`。項目は `ロケーション`、`メモ`、`商品`、`保存する`。 | 書ける |
| ロケーション選択 | `選択` で `ロケーションを選択する` モーダルが開くが、今回の画面では `アイテムが見つかりませんでした`。 | ロケーション候補を選べず |
| 商品選択 | `参照` で `バリエーションを選択する` モーダル。SKU検索後、`486125-09-XL` を選択でき、フォームに `商品 / 商品コード / SKU / 数量` の行が出る。 | 商品選択UIは確認済み |
| 保存試行 | 保存後、フォームに `ロケーションを選択してください` が表示。GraphQLの取置作成Mutationは飛ばず、一覧も空のまま。 | 現環境では作成未成立 |
| 在庫反映 | 保存未成立のため、在庫詳細は `販売可能1 / 確保済み0 / 手持ち1` のまま変化なし。 | 確保済み増減は未確認のまま |

## 注意・未確認

- 取置伝票の作成フォームと商品/SKU選択までは確認できた。
- ただしロケーション候補が空のため、取置伝票を保存できず、`確保済み` への反映は確認できなかった。
- `取置伝票を作成すると確保済みが増える` は引き続き未確認。FAQでは断定しない。
- ロケーション候補が空になる理由は未特定。権限、ロケーション条件、在庫条件、または画面側の候補取得条件に依存する可能性がある。

## FAQ本文に使うなら安全な表現

- 取置伝票の作成画面には、ロケーション、メモ、商品/SKU選択、数量入力があります。
- 今回の検証環境ではロケーション候補が表示されず、取置伝票の保存までは確認できていません。
- 取置作成後に `確保済み` 在庫がどう増減するかは、実データまたは環境条件を整えて再確認が必要です。
