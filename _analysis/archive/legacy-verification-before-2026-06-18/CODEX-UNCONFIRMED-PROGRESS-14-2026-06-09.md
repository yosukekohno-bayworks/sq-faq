# 未確認項目 追加進捗レビュー 14（Codex / Playwright）

対象: 顧客管理・会社・会社ロケーション・会社担当者  
確認日: 2026-06-09  
対象URL: `https://www.sqstackstaging.com/admin`

## 証跡

- 実行スクリプト: `faq/_analysis/deep-screen-verification-20260609/customer-company-current-probe.mjs`
- 記録JSON: `faq/_analysis/deep-screen-verification-20260609/customer-company-current-records.json`
- スクショ: `faq/_analysis/deep-screen-verification-20260609/customer-company-current-screenshots/`
- 記録数: 17
- スクショ数: 16
- 失敗: なし
- 400以上のネットワーク応答: Vercel insights の404のみ。SQ本体画面/API由来の4xx/5xxは検出なし。

## 確認結果

| 項目 | 実画面での現在挙動 | 判定 |
|:--|:--|:--|
| 顧客一覧 | `/admin/purchasing_customers` は空状態。「アイテムが見つかりませんでした」。 | `06-customers.md` と一致 |
| 顧客インポート | 「インポート」で `ポイント一括付与` が展開され、`/admin/csv_import/csv_import_operation_point_pluses` へリンク。 | 一致 |
| 顧客フィルター | 「検索と絞り込みの結果」で `メールアドレスで検索する` が表示。「絞り込みを追加」の選択肢は `テナント`、`メタフィールド`、`会員証バーコード`。 | 一致 |
| 顧客作成 | `/admin/purchasing_customers/create` は「予期せぬエラーが発生しました」。画面上にも顧客作成導線なし。 | 一致 |
| 会社一覧 | `/admin/companies` はデータあり。列は会社、ロケーション、総注文数、販売合計、作成日。`会社を作成する` 導線あり。 | 一致 |
| 会社一覧インポート | クリックしてもドロップダウン、モーダル、遷移なし。 | 一致 |
| 会社作成 | `会社名`、`会社ID`、担当者検索、ロケーション、配送先住所、請求先住所、`保存する` を確認。 | 一致 |
| 請求先住所チェックOFF | `配送先住所と同じ` を外すと請求先住所入力群が展開し、上部に「保存されていない変更」バーが出る。 | 一致 |
| 会社詳細 | `直近の注文`、`ロケーション`、`担当者`、`会社ID`、`メモ` を確認。既知会社 `TEST_FAQ_株式会社テスト` では `TEST_FAQ_東京本社` のロケーションリンクあり。 | 一致 |
| 担当者追加 | 「担当者を追加」から `担当者を作成する` ダイアログ。項目は `姓`、`名`、`メールアドレス`、`電話番号`、ボタンは `キャンセル` / `保存する`。 | 一致 |
| ロケーション詳細 | `ロケーション` h1、`直近の注文`、`ロケーションID`、`配送先住所`、`請求先住所`、`メモ` を確認。 | 一致 |
| ロケーション追加 | `/locations/create` は `新しいロケーション`。ロケーション名/ID、配送先住所、請求先住所、`配送先住所と同じ`、`保存する` を確認。 | 一致 |
| 担当者詳細 | 既知URLで `TEST_FAQ_担当太郎` h1 と作成済み状態を確認。画面は簡素で、編集/削除などの主要操作は見当たらない。 | 一致 |

## 注意・未確認

- `顧客はShopify等のチャネル経由で自動生成される` は、今回も直接証明できていない。FAQでは引き続き「推測」「現環境では管理画面から直接作成不可」に留めるのが安全。
- 顧客データが空のため、顧客詳細画面、顧客あり状態の列、注文履歴、ポイント残高、会員ランク表示は未確認のまま。
- `TEST_FAQ_DEEP3_202606080345_会社` は担当者0件なので、同社では担当者詳細リンクなし。担当者詳細の確認は既知会社 `TEST_FAQ_株式会社テスト` の既存担当者URLで実施した。
- 会社・担当者は `purchasing_customers` とは別系統という既存判断に反証なし。ただし外部チャネル連携による顧客生成フロー自体は未接続のため未確認。

## FAQ本文に使うなら安全な表現

- 顧客は現画面から直接作成できません。現環境では一覧が空で、直接作成URLもエラーになります。
- 会社は顧客管理配下の別機能として管理画面から作成できます。
- 会社にはロケーションと担当者を紐づけられます。
- 会社の担当者は会社用の連絡先で、購買顧客一覧に出る顧客とは別扱いです。
- 会社一覧のインポートボタンは現時点では押しても画面変化がありません。
