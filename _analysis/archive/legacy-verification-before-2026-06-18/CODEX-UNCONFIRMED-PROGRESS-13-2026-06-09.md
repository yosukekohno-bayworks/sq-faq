# 未確認項目 追加進捗レビュー 13（Codex / Playwright）

検証日時: 2026-06-09  
対象: 販売設定（販売価格 / 予約販売 / 販売上限 / 販売閾値）  
方針: 元のFAQ/分析ファイルは変更せず、実画面をPlaywrightで開き、保存・削除・ルール作成・価格登録は行わずに、一覧、作成フォーム、既存ルール詳細、子フォーム、選択モーダルを確認した。

## 証跡

- 実行スクリプト: `faq/_analysis/deep-screen-verification-20260609/sales-settings-current-probe.mjs`
- 取得JSON: `faq/_analysis/deep-screen-verification-20260609/sales-settings-current-records.json`
- スクショ: `faq/_analysis/deep-screen-verification-20260609/sales-settings-current-screenshots/`
- 取得レコード数: 24
- スクショ数: 24
- 実行結果: `failed: null`
- 400系通信: 22件。意味のあるアプリ画面404はなし（実質 `_vercel/insights/script.js` の404のみ）。

## 今回確認できたこと

| 項目 | 実画面で確認したこと | FAQ上の扱い |
|:--|:--|:--|
| 販売設定サブメニュー | `販売価格`、`予約販売`、`販売上限`、`販売閾値` の4画面に到達。 | `08-sales-settings.md` のサブメニュー構成は現行画面と一致。 |
| 販売価格一覧 | 既存ルールとして `TEST_FAQ_DEEP_202606080340_販売価格ルール`、`TEST_FAQ_販売価格ルール_遷移確認_20260607`、`TEST_FAQ_販売価格ルール` を確認。通貨表示は `日本円`。 | 初期の「空状態」記述は古く、後段のテストデータ作成後状態が現行に近い。FAQ本文では件数・テスト名は書かない。 |
| 販売価格作成 | h1 `販売価格ルールを作成する`。項目は `ルール名`、`通貨`。通貨選択肢は `米ドル`、`ユーロ`、`日本円`、`タイ バーツ`、`シンガポール ドル`。 | 作成フォームの説明は現行画面と一致。 |
| 販売価格詳細 | h1 `販売価格ルール`、`通貨: 日本円`、`通常価格`、`セール価格` を確認。 | 詳細画面の構成は現行画面と一致。 |
| 販売価格編集モーダル | `販売価格ルールを編集する`、`ルール名`、`通貨`、`通貨は編集できません`、`キャンセル`、`保存する` を確認。 | 「ルール名のみ編集でき、通貨は編集不可」と書ける。 |
| 通常価格一覧/作成 | 一覧は h1 `販売価格ルール (通常価格)`。作成フォームは h1 `通常価格を作成する`、`商品バリエーションを選択する`、`価格`、`価格(税込)`、`保存する`。 | `価格(税込)` は別フィールドとして表示される。保存必須/任意は今回再検証していないため、既存のバリデーション検証に依存。 |
| 通常価格のバリエーション選択 | `選択` で `バリエーションを選択する` モーダルが開く。現在候補は空。 | 選択モーダル導線は確認済み。現在の候補空は環境/既存紐付け条件の可能性があるため断定しない。 |
| セール価格一覧/作成 | 一覧は h1 `販売価格ルール (セール価格)`、`インポート`、`セール価格を登録`。作成フォームは `商品バリエーションを選択する`、`価格`、`価格(税込)`、`開始日時`、`保存する`。 | セール価格は通常価格と違い、一覧にインポート導線があり、作成フォームに開始日時がある。 |
| 予約販売一覧/作成 | 既存ルール3件を確認。作成フォームは h1 `予約販売のルールを作成する`、保存ボタンのみ。 | 作成フォームはタイトル中心のシンプル構成という既存記録と整合。 |
| 予約販売詳細 | h1 `TEST_FAQ_予約販売ルール`。`ルールを編集する`、`インポート`、`バリエーションを追加する`、`SKUコードで検索する`、空状態を確認。 | 予約販売SKUは詳細画面配下で追加する、と書ける。 |
| 予約販売編集モーダル | `ルールを編集する`、`タイトル`、`キャンセル`、`保存する` を確認。 | 編集対象はタイトル。 |
| 予約販売バリエーション追加 | h1 `バリエーションを追加する`。項目は `商品バリエーション`、`販売数`、`保存する`。選択モーダルも開くが現在候補は空。 | 予約販売の数量ラベルは `販売数` と書ける。実注文/予約販売可能数への反映は未確認。 |
| 販売上限一覧/作成 | 一覧は空。作成フォームは h1 `販売上限ルールを作成する`、項目は `販売上限ルール名`、`チャネル`、`保存する`。 | チャネル統合未設定環境では一覧空、作成フォームもチャネル選択が必要。詳細/子フォームのUIは今回も確認不能。 |
| 販売閾値一覧/作成 | 既存ルール3件を確認。作成フォームは `ルール名` と `デフォルトの閾値を設定する`。説明文は「閾値ルールに含まれていないすべての商品バリエーションにデフォルト閾値を適用」。 | デフォルト閾値の意味は画面説明どおり書ける。 |
| 販売閾値詳細 | h1 `TEST_FAQ_DEEP_202606080340_販売閾値ルール`。`自動追加ルール`、`ルールを編集する`、`閾値を追加する`、`SKUコードで検索する`、空状態を確認。 | 閾値は個別SKU追加と自動追加ルールの2導線がある。 |
| 閾値追加フォーム | 項目は `商品バリエーションを選択する`、`閾値`、`保存する`。選択モーダルも開くが現在候補は空。 | 個別SKUに閾値を設定するフォームとして説明できる。 |
| 閾値の自動追加ルール | h1 `自動追加ルールを追加する`。選択肢は `ブランドコード`、条件は `一致する`、数値項目 `閾値`、`保存する` を確認。 | 自動追加条件は現時点でブランドコード一致のみ、と書ける。 |

## まだ残す未確認

| 項目 | 理由 |
|:--|:--|
| 販売閾値到達時の実挙動 | 在庫操作/販売チャネル連携が必要。販売停止、通知、表示制御のどれかはUIだけでは断定不可。 |
| 販売上限の詳細画面UI | 現環境では販売上限ルールが0件で、チャネル候補もないため作成後詳細へ進めない。既存のGraphQL確認はあるが、UIラベル原文は未確認。 |
| 通常価格/セール価格/予約販売/閾値の保存後反映 | 保存操作をしていない。 |
| 各バリエーション選択モーダルの候補条件 | 今回開いたモーダルは候補空。既存データや既存紐付けにより空なのか、フィルタ条件によるものかは未確定。 |
| 外部チャネル上の価格・予約販売・閾値・上限反映 | Shopify/スマレジ/OmnibusCore等の実連携確認が必要。 |

## FAQへ反映するなら安全な表現

> 販売設定には、販売価格、予約販売、販売上限、販売閾値の4種類があります。販売価格ルールでは通貨とルール名を管理し、詳細画面から通常価格とセール価格をSKU単位で登録します。セール価格には通常価格とは別に開始日時の入力があります。

> 予約販売では、ルール詳細からバリエーションを追加し、SKUごとに `販売数` を設定します。販売閾値では、個別SKUに閾値を追加する導線と、ブランドコード一致で自動追加する導線があります。ただし、閾値到達時の実際の販売停止・通知・外部チャネル反映は今回の検証環境では未確認です。

> 販売上限は作成フォームにチャネル選択が必要です。現在の検証環境では販売上限ルールがなく、チャネル候補も確認できないため、詳細画面のUIラベルや上限到達時の実挙動は未確認です。
