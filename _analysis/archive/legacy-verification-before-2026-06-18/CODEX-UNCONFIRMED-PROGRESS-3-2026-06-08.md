# 未確認項目 追加進捗レビュー 3（Codex / Playwright）

作成日: 2026-06-08

方針: 元ファイルは変更せず、残未確認のうち「クリックで開けるメニュー/モーダル/空送信バリデーション」を追加確認した。保存・削除・外部接続実行は行っていない。

## 今回の追加証跡

- `faq/_analysis/deep-screen-verification-20260608/unconfirmed-interactive-readonly-records.json`
- `faq/_analysis/deep-screen-verification-20260608/unconfirmed-click-menus-records.json`
- `faq/_analysis/deep-screen-verification-20260608/unconfirmed-user-menu-records.json`
- `faq/_analysis/deep-screen-verification-20260608/unconfirmed-retail-tenant-records.json`
- 対応スクリーンショット:
  - `unconfirmed-interactive-readonly-screenshots/`
  - `unconfirmed-click-menus-screenshots/`
  - `unconfirmed-user-menu-screenshots/`
  - `unconfirmed-retail-tenant-screenshots/`

## 追加で確認できたこと

| 項目 | 実画面での確認結果 | FAQでの扱い |
|---|---|---|
| 右上ユーザーメニュー | `stack-ps-yosuke / 陽介 河野` をクリックすると、ヘルプセンター、変更ログ、チャットで質問、すべての組織、組織を管理する、アカウントを管理する、ログアウトが表示。`アカウントを管理する` は `/account/member`。 | 右上メニューの内容は書ける。 |
| 顧客管理のインポートメニュー | 顧客管理一覧の `インポート` を開くと `ポイント一括付与` が表示。顧客CSV取込ではない。 | 顧客管理から顧客を直接作成/インポートできるとは書かない。ポイント一括付与導線として扱う。 |
| 顧客create直打ち | `/admin/purchasing_customers/create` は `予期せぬエラーが発生しました`。 | 購入顧客の直接作成URLは案内しない。 |
| 下書き注文の作成ボタン | `注文を作成する` は表示されるが、hrefなし・`Polaris-Button--disabled`。 | 下書き注文一覧から現在は作成開始できない。直URLも既存確認でエラー。 |
| 売上実績の作成ボタン | `売上実績を作成する` は表示されるが、hrefなし・`Polaris-Button--disabled`。`/create` は既存確認で404。 | 手動作成導線は未実装/無効として扱う。 |
| 卸売 | `/admin/b2b` は h1 `卸売` だが本文は `TODO`。`/admin/b2b/create` は404。 | B2B/卸売は現時点ではFAQ本編の操作手順対象外。 |
| 分析 | `/admin/analytics` は h1 `分析` だが本文は `TODO`。 | 分析は現時点では機能説明を膨らませない。 |
| 販売上限ルールの空保存 | `/admin/inventory_sale_limit_rules/create` の空保存で `販売上限ルール名を入力してください` と `チャネルを選択してください` を確認。画面上にチャネル欄はあるが候補は見えない。 | 販売上限ルールはチャネル選択必須。ただし現在環境では候補未確認/未選択で保存不可。 |
| ポイントキャンペーン種別=なし | 種別 `なし` 選択後、ポイント付与方法、ポイント、対象の注文ポイント付与ルール、適用条件、チャネル、対象店舗が表示。対象ルールに `TEST_FAQ_注文ポイント付与ルール` が候補として出る。 | 追加フォームは種別選択後に出る。 |
| ポイントキャンペーンの対象店舗選択 | `対象店舗` の選択モーダルは `ロケーションを選択する`。候補は `ユニクロ - 銀座店 / R0001` と `TEST_FAQ_DEEP2_202606080343_ロケーション / test_faq_deep2_202606080343_loc`。 | 「チャネル」見出し下に対象店舗ロケーションを選ぶUIがある。外部販売チャネルの同期挙動ではない。 |
| リテールポータルの店舗ロケーション | 店舗ロケーション選択モーダルは `ユニクロ - 銀座店 / R0001` と `TEST_FAQ_DEEP2_202606080343_ロケーション / test_faq_deep2_202606080343_loc` を表示。 | 店舗側ロケーション候補は確認済み。 |
| リテールポータルの在庫ロケーション | テナント未選択だと空。テナント `ユニクロ` 選択後、在庫ロケーション候補に `物流倉庫 / W0001` が表示。 | 在庫ロケーション候補はテナント選択に依存する。 |

## 修正/注意した方がよい表現

- `ポイントキャンペーンのチャネル選択肢が空` は、現在画面だけで見ると弱い。種別 `なし` 選択後は「チャネル」見出し下に `対象店舗` 選択が出て、ロケーション候補が2件表示される。
- `リテールポータルの在庫ロケーションが空` も弱い。テナント未選択では空だが、テナント `ユニクロ` 選択後は `物流倉庫 / W0001` が出る。
- 顧客管理の `インポート` は、少なくとも現在観測では `ポイント一括付与` メニューであり、顧客レコードのインポートとは断定しない。
- 下書き注文・売上実績はボタン文言だけ見ると作成できそうだが、実際はdisabledか404/エラーなので、FAQでは作成手順として案内しない。

## まだ残る未確認

| 項目 | 理由 |
|---|---|
| 実注文詳細、返品/返金/キャンセル操作 | 注文データがない。返品createは404。 |
| 売上実績の自動生成条件 | 注文/売上データがない。手動作成はdisabled/404。 |
| 外部連携後の同期挙動 | 接続情報・外部側データ・同期実行が必要。 |
| 権限別ログイン後のUI差分 | `TEST_FAQ_店舗スタッフ` の権限設定は確認済みだが、そのユーザーでログインしていない。 |
| 納品書PDF実ファイル | PDF対象の出荷指示/エクスポート対象がない。 |
| 発注伝票保存後の入荷連携 | 既存証跡で作成時エラー。再実行は実データ作成の可能性があるため今回も未実施。 |
