# 未確認項目 追加進捗レビュー 10（Codex / Playwright）

検証日時: 2026-06-09  
対象: CRM（ディスカウント / ポイント / ポイントキャンペーン / 会員ランク）  
方針: 元のFAQ/分析ファイルは変更せず、実画面をPlaywrightで操作して、レビュー用証跡だけを追加した。保存、削除、接続、実行系の確定操作は押していない。

## 証跡

- 実行スクリプト: `faq/_analysis/deep-screen-verification-20260609/crm_points_current_probe.mjs`
- 取得JSON: `faq/_analysis/deep-screen-verification-20260609/crm-points-current-records.json`
- スクショ: `faq/_analysis/deep-screen-verification-20260609/crm-points-current-screenshots/`
- 実行結果: `failed: null`
- 400系通信: `_vercel/insights/script.js` の404が43件、`/admin/customer_ranks` の404が1件。後者は「単独の会員ランクURLは存在しない」確認として扱う。

## 今回解消できた未確認

| 項目 | 実画面で確認したこと | FAQ上の扱い |
|:--|:--|:--|
| ポイント配下の機能群 | `/admin/point_calculation_rules` は h1 `注文ポイント`。サブメニューとして `キャンペーン`、`誕生日`、`利用外商品`、`失効通知` が存在。 | 「ポイント」は注文ポイントだけでなく、キャンペーン/誕生日/利用外商品/失効通知を含むCRM配下のポイント機能として説明できる。 |
| ポイントキャンペーン種別 | 作成フォームで `なし`、`会員ランク`、`購入金額`、`商品` の4種別を確認。 | 種別4択として書ける。 |
| ポイントキャンペーン付与方法 | 4種別すべてで `通常倍率に指定の倍率を加えた合計倍率でポイントを付与する` と `通常付与ポイントに加えて指定のポイントを追加で付与する` の2択を確認。 | `12-crm.md` の「追加ポイント時のUI差異未確認」は解消。 |
| 追加ポイント方式のUI | `通常付与ポイントに加えて指定のポイントを追加で付与する` を選ぶと、固定ポイント追加の入力説明に変わる。例として通常獲得500ポイントに100ポイント追加し合計600ポイントになる説明を確認。 | 倍率方式と固定ポイント追加方式を分けて書くべき。 |
| 倍率方式のUI | 倍率方式では「通常獲得ポイントに加えて、倍分のポイントを追加で付与する」系の入力説明と、通常2倍 + キャンペーン3倍 = 合計5倍の説明を確認。 | 「倍率」は通常倍率への加算倍率として説明する。 |
| 対象注文ポイント付与ルール | 作成フォームの候補に `TEST_FAQ_注文ポイント付与ルール` が出る。 | キャンペーンは注文ポイント付与ルールに紐付ける、と書ける。 |
| チャネル候補 | GraphQL `PointCampaignOrderRuleCreatePage` の `orderSources.nodes` は空。画面上は `チャネル` セクション配下に `対象店舗` の選択導線がある。 | 外部チャネル選択肢は現環境では未確認/空。対象店舗ロケーションの選択UIとは分ける。 |
| 対象店舗追加 | 既存キャンペーン2件の `/retail_locations/create` で `対象店舗を追加する` に到達。`店舗を選択して追加する` / `店舗グループを選択して追加する` のラジオと、ロケーション選択モーダルを確認。候補は `ユニクロ - 銀座店 / R0001` と `TEST_FAQ_DEEP2_202606080343_ロケーション`。 | キャンペーン対象店舗はロケーションまたは店舗グループで追加する、と書ける。ただし保存後反映は未確認。 |
| ポイント利用外商品 | `/admin/point_application_excluded_products/create` は `利用外商品を追加する`。商品選択モーダルで商品コード検索、商品一覧、`選択する` を確認。 | ポイント対象外商品を商品単位で選択するUIは確認済み。保存後の除外適用は未確認。 |
| 会員ランク期間切替 | 会員ランク作成フォームで `1年間` は `開始月` と詳細設定を表示。`直近365日` / `無期限` に切り替えると `開始月` と詳細セクションが消える。 | `開始月` は `1年間` のときだけ表示、と書ける。 |
| 会員ランク単独URL | `/admin/customer_ranks` は h1 `このページは存在しないようです`。 | 会員ランクは `/admin/customer_rank_calculation_rules/{id}` 配下で管理される。単独URLは案内しない。 |
| 会員ランク詳細 | 既存 `TEST_FAQ_会員ランク算出ルール` の詳細で、`会員ランク`、`詳細`、`除外商品を管理する`、`会員ランクを追加する`、`算出方法: 購入金額`、`算出タイミング: リアルタイムに算出` を確認。 | 詳細ページ構成として書ける。 |
| Bronzeランク詳細 | Bronze詳細で表示名入力は有効、購入金額入力は `disabled`、`削除する` も `disabled`。 | 初期ランクは名称のみ編集可能、達成条件と削除は不可、と書ける。 |
| 会員ランク除外商品追加 | `/exclude_products/create` は h1 `除外商品を作成`。`除外商品を選択する` の選択ボタンから商品選択モーダルを確認。 | `12-crm.md` の「除外商品追加フォーム未確認」は解消。 |
| 会員ランク追加 | `/customer_rank_rules/create` に到達。h1 は `会員ランク算出ルールを作成する`、セクションは `ランク` / `達成条件`、達成条件は購入金額。 | 会員ランク算出ルール配下で個別ランクを追加する導線は確認済み。 |

## まだ残す未確認

| 項目 | 理由 |
|:--|:--|
| キャンペーン/除外商品の保存後反映 | 保存操作は行っていない。 |
| 実注文へのポイント付与、倍率/固定ポイントの計算結果 | 注文データがないため、注文詳細・顧客ポイント履歴で確認できない。 |
| 返品・キャンセル時のポイント減算/失効/保留 | 注文・顧客データが空のため実操作不可。 |
| 外部チャネル候補 | 現環境の `orderSources.nodes` が空。連携後に候補が出るかは未確認。 |
| ディスカウントの実注文適用・併用可否 | 実注文/チェックアウト相当のデータがないため未確認。 |
| ディスカウント全商品ONの保存後表示 | 保存操作が必要なため未確認。詳細ページでは既存2件とも関連商品/顧客/店舗/利用履歴サブページは空。 |

## FAQへ反映するなら安全な表現

> ポイントキャンペーンでは、対象種別を「なし」「会員ランク」「購入金額」「商品」から選び、対象の注文ポイント付与ルールに紐付けます。付与方法は、通常ポイントに倍率を加える方式と、通常付与ポイントに固定ポイントを追加する方式があります。対象店舗はロケーションまたは店舗グループで追加できます。ただし、実注文への付与結果、返品・キャンセル時のポイント処理、外部チャネル連携後の候補表示は今回の検証環境では未確認です。

> 会員ランク算出ルールでは、算出方法は現状「購入金額」固定で、獲得ポイントへの切替は無効です。期間が「1年間」の場合のみ開始月と詳細設定が表示され、「直近365日」「無期限」では開始月は表示されません。初期ランク（Bronze）は名称のみ編集でき、達成条件と削除は無効です。
