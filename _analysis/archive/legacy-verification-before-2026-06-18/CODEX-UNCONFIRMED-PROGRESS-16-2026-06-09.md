# 未確認項目 追加進捗レビュー 16（Codex / Playwright）

対象: 設定・データ管理マスタ（テナント、ロケーション、ロケーショングループ、ブランド、取引先、決済方法、翻訳、採寸ルール、メタフィールド定義）  
確認日: 2026-06-09  
対象URL: `https://www.sqstackstaging.com/admin`

## 証跡

- 実行スクリプト: `faq/_analysis/deep-screen-verification-20260609/settings-data-masters-current-probe.mjs`
- 記録JSON: `faq/_analysis/deep-screen-verification-20260609/settings-data-masters-current-records.json`
- スクショ: `faq/_analysis/deep-screen-verification-20260609/settings-data-masters-current-screenshots/`
- 記録数: 22
- スクショ数: 22
- 失敗: なし
- 400以上のネットワーク応答: 意図的に直URL確認した `/admin/settings/company_locations` の404のみ。
- 補足: 記録JSONは認証系・静的配信系のレスポンス本文を保存しないよう再取得済み。

## 確認結果

| 項目 | 実画面での現在挙動 | 判定 |
|:--|:--|:--|
| テナント一覧 | `/admin/settings/tenants` は `テナント`。一覧に `ユニクロ`、導線に `テナントを作成` を確認。 | 書ける |
| ロケーション一覧 | `/admin/settings/locations` は `ロケーション`。列は名前、コード、場所種別、公開、アーカイブ、ロケーショングループ、タグ。既存行に `物流倉庫 / W0001`、`ユニクロ - 銀座店 / R0001`、テスト用ロケーションを確認。 | 書ける |
| ロケーション作成 | `/admin/settings/locations/create` は `ロケーションを作成`。`場所種別` は `倉庫` / `店舗`。`ロケーションの住所を登録する`、`在庫依頼を受け付ける`、`保存する` を確認。 | 書ける |
| ロケーション住所 | `ロケーションの住所を登録する` をONにすると住所入力が展開。国、都道府県などの選択肢を確認。 | 書ける |
| ロケーショングループ一覧 | `/admin/settings/location_groups` は `ロケーショングループ`。既存 `TEST_FAQ_ロケーショングループ` と `2個のロケーション` を確認。 | 書ける |
| ロケーショングループ作成 | `/admin/settings/location_groups/create` は `グループを作成`。`ロケーション` の `選択` と `保存する` を確認。 | 書ける |
| ロケーション選択モーダル | `選択` で `ロケーションを選択する` モーダル。タブは `すべて`、`店舗`、`倉庫`。候補は `物流倉庫 / W0001`、`ユニクロ - 銀座店 / R0001`、テスト用ロケーション。 | 書ける |
| ブランド一覧 | `/admin/settings/brands` は `ブランド`。`追加する` と既存ブランド行を確認。 | 書ける |
| ブランド作成 | `/admin/settings/brands/create` は `ブランド`。`保存する` を確認。 | 書ける。ただし入力項目名の抽出は限定的 |
| 取引先一覧 | `/admin/settings/suppliers` は `取引先`。`取引先を作成`、`インポート`、`アーカイブ` ビュー、名前/コード列を確認。 | 書ける |
| 取引先作成 | `/admin/settings/suppliers/create` は `取引先を作成`。`取引先名`、`取引先コード`、`保存する` を確認。 | 書ける |
| 決済方法一覧 | `/admin/settings/payment_methods` は `決済方法`。列は名前、コード、ゲートウェイ。既存のテスト決済方法を確認。 | 書ける |
| 決済方法作成 | `/admin/settings/payment_methods/create` は `決済方法を作成する`。`支払い待ちでも注文を出荷する`、`代引き`、`保存する` を確認。 | 設定項目は書ける |
| 翻訳一覧 | `/admin/settings/translation` は `翻訳`。翻訳ルール選択後にリソース選択できる構成で、リソースに `商品` を確認。 | 書ける |
| 翻訳ルール作成 | `/admin/settings/translation/translation_rules/create` は `翻訳ルールを作成する`。言語候補は日本語、英語、中国語（簡体字/繁体字）、韓国語、スペイン語、フランス語、ドイツ語、ヒンディー語、タイ語。`翻訳データを自動で作成する` を確認。 | 設定項目は書ける |
| 採寸ルール一覧 | `/admin/settings/product_measurement_rules` は `採寸ルール`。列はルール名、単位、採寸項目。既存行に `センチメートル / 肩幅` を確認。 | 書ける |
| 採寸ルール作成 | `/admin/settings/product_measurement_rules/create` は `採寸ルールを作成する`。`採寸単位` は `なし` / `センチメートル`。`最大5件まで追加できます` を確認。 | 書ける |
| 採寸項目追加 | `採寸項目を追加` で `採寸項目2` が追加され、未保存変更バーが出る。 | 挙動確認済み |
| メタフィールド定義トップ | `/admin/settings/metafield_definitions` は `メタフィールド定義`。対象は商品、バリエーション、顧客、注文、下書き注文、会社、ロケーション、在庫移動伝票、在庫調整伝票、在庫取置伝票、発注伝票、入荷指示、出荷指示、仕入れ先。 | 書ける |
| 商品メタフィールド定義一覧 | `/admin/settings/metafield_definitions/product` は `商品メタフィールドの定義`。列は定義名、コンテンツタイプ、権限、使用箇所。`定義を追加する` を確認。 | 書ける |
| 商品メタフィールド定義作成 | `/admin/settings/metafield_definitions/product/create` は `商品メタフィールドの定義を追加する`。タイプは単一行テキスト、リスト、複数行、日付/日時、true/false、バーコード、整数、小数、JSON、ID、リッチテキスト。 | 書ける |
| 会社ロケーション設定直URL | `/admin/settings/company_locations` は `このページは存在しないようです`。 | 会社ロケーションを設定配下の独立マスタとして書くのは危険 |

## 注意・未確認

- 各マスタは基本的に「一覧・作成画面・主要選択UI」まで確認。保存後の一覧反映、重複チェック、バリデーション、削除/アーカイブ動作は未実行。
- 決済方法の `支払い待ちでも注文を出荷する`、`代引き` は設定項目として確認済みだが、注文や出荷フローへの効果は未確認。
- 翻訳ルールの `翻訳データを自動で作成する` は設定項目として確認済みだが、保存後にどのリソース翻訳が生成されるかは未確認。
- 採寸ルールの最大5件表示と項目追加は確認済み。ただし商品への紐付け、表示先、保存後の反映は未確認。
- メタフィールド定義はトップと商品対象の作成画面を確認。商品以外の対象で作成フォームが完全に同一かは未確認。
- ブランド作成画面は到達・保存導線は確認済みだが、画面テキスト抽出では入力項目名が十分に取れていないため、項目名の断定は避ける。

## FAQ本文に使うなら安全な表現

- 設定画面では、テナント、ロケーション、ロケーショングループ、ブランド、取引先、決済方法、翻訳、採寸ルール、メタフィールド定義を管理できます。
- ロケーションは店舗または倉庫として作成でき、住所登録や在庫依頼受付の設定があります。
- ロケーショングループには複数ロケーションを選択して紐づけられます。
- 取引先は名前とコードで作成します。
- 採寸ルールは単位と採寸項目を管理し、採寸項目は画面上で最大5件まで追加できます。
- 会社ロケーションは設定配下の独立マスタではなく、会社詳細側のロケーションとして扱うのが安全です。
