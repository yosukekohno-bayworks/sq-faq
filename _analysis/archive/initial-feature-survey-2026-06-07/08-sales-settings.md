# 販売設定

> 調査日: 2026-06-05 | URL: `/admin/product_price_rules` | 初期偵察は読み取り中心。後続の検証では TEST_FAQ 接頭辞で一部書き込み操作を実施（販売価格/予約販売/販売閾値ルール等を作成。破壊的・不可逆操作は未実施）

## 画面目的
販売価格ルール・予約販売・販売上限・販売閾値の各種販売ルールを管理する。

## サブメニュー（販売設定配下）
| ラベル | URL | 画面見出し |
|:--|:--|:--|
| 販売価格 | `/admin/product_price_rules` | 販売価格 |
| 予約販売 | `/admin/inventory_back_order_rules` | 予約販売 |
| 販売上限 | `/admin/inventory_sale_limit_rules` | 販売上限 |
| 販売閾値 | `/admin/inventory_threshold_rules` | 販売閾値 |

---

## 販売価格（/admin/product_price_rules）

### 画面目的
販売価格ルールの管理。ルールごとに通貨・対象商品・価格を設定する。

### 主要アクション
- 「販売価格ルールを作成する」リンクボタン → `/admin/product_price_rules/create`

### 状態
- 空状態（調査初期のスナップショット。その後 TEST_FAQ_販売価格ルール 等を作成済み。最新は後段「深掘り・画面別」「正確性監査」セクション、または CONFIRMATION-LIST.md を参照）

### 販売価格ルール 作成フォーム（/admin/product_price_rules/create）

#### 基本設定セクション
| 項目 | ラベル原文 | 必須/任意 | 形式 |
|:--|:--|:--|:--|
| ルール名 | ルール名* | 必須 | テキスト、プレースホルダー「年末セール価格」 |
| 通貨 | 通貨* | 必須 | コンボボックス（選択肢: 米ドル / ユーロ / 日本円 / タイ バーツ / シンガポール ドル）、デフォルト: 日本円 |

### ボタン
- 「保存する」

---

## 予約販売（/admin/inventory_back_order_rules）

### 画面目的
商品の予約販売（バックオーダー）ルールの管理。

### 主要アクション
- 「予約販売ルールを作成する」リンクボタン

### 状態
- 空状態（調査初期のスナップショット。その後 TEST_FAQ_予約販売ルール 等を作成済み。最新は後段「深掘り・画面別」「正確性監査」セクション、または CONFIRMATION-LIST.md を参照）

---

## 販売上限（/admin/inventory_sale_limit_rules）

### 画面目的
販売数量の上限ルールの管理。

### 主要アクション
- 「販売上限ルールを作成する」リンクボタン

### 状態
- 空状態（調査初期のスナップショット。販売上限はチャネル統合未設定のため本環境では作成不可。最新は後段「正確性監査」監査B、または CONFIRMATION-LIST.md を参照）

---

## 販売閾値（/admin/inventory_threshold_rules）

### 画面目的
在庫の販売閾値ルールの管理（一定数以下になったときの挙動制御と推測）。

### 主要アクション
- 「販売閾値ルールを作成する」リンクボタン

### 状態
- 空状態（調査初期のスナップショット。その後 TEST_FAQ_販売閾値ルール 等を作成済み。最新は後段「深掘り・画面別」「抜け漏れ検証」セクション、または CONFIRMATION-LIST.md を参照）

---

## 注意点・疑問点
- 販売設定の各ルールは、実際の商品・バリエーションに紐付けて適用する手順が不明<!-- TODO: 要確認 -->
- 「販売閾値」は在庫が一定数以下になった場合の販売停止・通知等の閾値と推測<!-- TODO: 要確認 -->
- CSVインポートで「販売価格（通常）」「販売価格（セール）」が別設定になっている点に注目

---

## 深掘り・画面別（2026-06-05実測）

---

### 販売価格 一覧（/admin/product_price_rules）

- **目的**: 作成済みの販売価格ルールを一覧表示し、新規作成・既存ルール詳細へ遷移する。

- **操作要素と挙動（実測）**:

| 要素（UIラベル原文） | 種別 | 操作すると何が起こるか |
|:--|:--|:--|
| 販売価格ルールを作成する | リンクボタン | `/admin/product_price_rules/create` へ遷移 |
| ルール名（一覧行） | リンク行 | 各ルールの詳細画面へ遷移 |

- **入力項目**: なし（一覧・ナビゲーションのみ）

- **空状態**: 「販売価格ルールを作成する」ボタンのみ表示。ルール作成済みの場合はルール一覧が表示される。

- **スクショ**: （一覧画面スクショなし、空状態は既存記録参照）

---

### 販売価格ルール 作成フォーム（/admin/product_price_rules/create）

- **目的**: 新しい販売価格ルールを作成する。ルール名と適用通貨を設定する。

- **操作要素と挙動（実測）**:

| 要素（UIラベル原文） | 種別 | 操作すると何が起こるか |
|:--|:--|:--|
| ← （戻る矢印） | リンク | 販売価格一覧へ戻る |
| 保存する | ボタン | フォームを送信し、ルールを作成。✓実機確認(2026-06-07): 保存成功後、`/admin/product_price_rules/[新規ID]_ProductPriceRule` の**詳細画面へ直接遷移**する（TEST_FAQ_販売価格ルール_遷移確認_20260607 で実測） |

- **入力項目**:

| 項目（ラベル原文） | 必須/任意 | 形式・選択肢・上限・プレースホルダ |
|:--|:--|:--|
| ルール名 * | 必須 | テキスト入力。プレースホルダ「年末セール価格」 |
| 通貨 * | 必須 | コンボボックス（セレクト）。選択肢: 米ドル / ユーロ / 日本円 / タイ バーツ / シンガポール ドル。デフォルト: 日本円 |

- **制約**: 通貨はルール作成後に変更不可（編集モーダルで「通貨は編集できません」と表示される）。

- **スクショ**: ![販売価格ルール作成フォーム](screenshots/08-price-rule-create.png)

---

### 販売価格ルール 詳細（/admin/product_price_rules/[id]）

- **目的**: 作成済みルールの通貨確認と、「通常価格」「セール価格」の2種類の価格タイプへのナビゲーション。

- **操作要素と挙動（実測）**:

| 要素（UIラベル原文） | 種別 | 操作すると何が起こるか |
|:--|:--|:--|
| ← （戻る矢印） | リンク | 販売価格一覧へ戻る |
| ルールを編集 | ボタン | 編集モーダルを開く |
| 通常価格（カード行） | リンク行 | 通常価格 SKU一覧画面へ遷移 |
| セール価格（カード行） | リンク行 | セール価格 SKU一覧画面へ遷移 |

- **表示情報**:
  - ページタイトル: 「販売価格ルール」
  - タイトル横にバッジ: 「通貨: 日本円」（設定通貨が常時表示）
  - サブタイトル: ルール名（例: TEST_FAQ_販売価格ルール）
  - 「通常価格」カード: 「通常価格を設定することができます。」の説明文付き
  - 「セール価格」カード: 「セール価格を設定することができます。」の説明文付き

- **スクショ**: ![販売価格ルール詳細](screenshots/08-price-rule-detail.png)

---

### 販売価格ルール 編集モーダル（詳細画面上でインライン表示）

- **目的**: 既存ルールのルール名を変更する。通貨は変更不可。

- **操作要素と挙動（実測）**:

| 要素（UIラベル原文） | 種別 | 操作すると何が起こるか |
|:--|:--|:--|
| × （閉じるボタン） | ボタン | モーダルを閉じ、詳細画面に戻る |
| キャンセル | ボタン | モーダルを閉じ、変更を破棄する |
| 保存する | ボタン | ルール名を更新して保存し、モーダルを閉じる |

- **入力項目**:

| 項目（ラベル原文） | 必須/任意 | 形式・制約 |
|:--|:--|:--|
| ルール名 * | 必須 | テキスト入力。既存のルール名が初期値として入力されている |
| 通貨 | 編集不可 | テキスト（読み取り専用）。「JPY」と表示。フィールド下に「通貨は編集できません」の注釈あり |

- **スクショ**: ![販売価格ルール編集モーダル](screenshots/08-price-rule-edit-modal.png)

---

### 通常価格 SKU一覧（/admin/product_price_rules/[id]/product_variant_regulars）

（訂正: 旧URL `/regular_prices` → 正しくは `/product_variant_regulars`）✓実機確認済み(2026-06-07)

- **目的**: 販売価格ルール内の「通常価格」に紐付けられたSKU（商品バリエーション）と価格を一覧表示する。新規SKUの価格登録もここから行う。

- **操作要素と挙動（実測）**:

| 要素（UIラベル原文） | 種別 | 操作すると何が起こるか |
|:--|:--|:--|
| ← （戻る矢印） | リンク | 詳細画面（/admin/product_price_rules/[id]）へ戻る |
| 通常価格を登録する | リンクボタン | 通常価格 作成フォームへ遷移（`/product_variant_regulars/create`） |
| SKUコードで検索する | 検索フィールド | 入力したSKUコードで一覧をフィルタリング |
| 前へ / 次へ | ページネーションボタン | 前ページ / 次ページへ移動 |

- **表示情報**:
  - ページタイトル: 「販売価格ルール (通常価格)」（半角スペース・半角括弧）
  - サブタイトル: ルール名（例: TEST_FAQ_販売価格ルール）
  - 空状態: 「アイテムが見つかりませんでした」「絞り込みや検索ワードを変更してみてください」と表示

- **スクショ**: ![通常価格SKU一覧](screenshots/08-price-rule-regular-list.png)

---

### 通常価格 作成フォーム（/admin/product_price_rules/[id]/product_variant_regulars/create）

（訂正: 旧URL `/regular_prices/create` → 正しくは `/product_variant_regulars/create`）✓実機確認済み(2026-06-07)

- **目的**: 販売価格ルールに新しいSKUの通常価格を登録する（SKU紐付けと価格設定）。

- **操作要素と挙動（実測）**:

| 要素（UIラベル原文） | 種別 | 操作すると何が起こるか |
|:--|:--|:--|
| ← （戻る矢印） | リンク | 通常価格SKU一覧画面へ戻る |
| 選択 | ボタン | 商品バリエーション（SKU）を選択するダイアログ/モーダルを開く |
| 保存する | ボタン | 入力内容でSKUに通常価格を登録する |

- **入力項目**:

| 項目（ラベル原文） | 必須/任意 | 形式・選択肢・プレースホルダ |
|:--|:--|:--|
| 商品バリエーションを選択する * | 必須 | テキスト + 「選択」ボタン。プレースホルダ「選択してください」。SKU検索・選択ダイアログから選択する形式 |
| 価格 * | 必須 | 数値入力（spinbutton）。通貨記号「￥」（全角）付き。（訂正: 旧「¥（半角）」→「￥（全角）」）✓実機確認済み(2026-06-07) |
| 価格(税込) | 任意 | 数値入力（spinbutton）。通貨記号「￥」（全角）付き。ラベルは半角括弧「価格(税込)」。バリデーション未入力でエラーなし。（訂正: 旧ラベル「価格（税込）」全角括弧→半角括弧「価格(税込)」、旧通貨記号「¥」→「￥」、任意は確定）✓実機確認済み(2026-06-07) |

- **ページタイトル**: 「通常価格を作成する」、サブタイトル: ルール名

- **スクショ**: ![通常価格作成フォーム](screenshots/08-price-rule-regular-create.png)

---

### 予約販売 一覧（/admin/inventory_back_order_rules）

- **目的**: 予約販売（バックオーダー）ルールの一覧表示・新規作成へのナビゲーション。

- **操作要素と挙動（実測）**:

| 要素（UIラベル原文） | 種別 | 操作すると何が起こるか |
|:--|:--|:--|
| 予約販売ルールを作成する | リンクボタン | `/admin/inventory_back_order_rules/create` へ遷移 |
| 前へ | ボタン（ページネーション） | 前ページへ移動（ルールなしの場合はdisabled） |
| 次へ | ボタン（ページネーション） | 次ページへ移動（ルールなしの場合はdisabled） |

- **空状態**: 「アイテムが見つかりませんでした」「絞り込みや検索ワードを変更してみてください」と表示。

---

### 予約販売 作成フォーム（/admin/inventory_back_order_rules/create）

- **目的**: 新しい予約販売ルールを作成する。

- **操作要素と挙動（実測）**:

| 要素（UIラベル原文） | 種別 | 操作すると何が起こるか |
|:--|:--|:--|
| ← （戻る矢印） | リンク | 予約販売一覧へ戻る |
| 保存する | ボタン | ✓実機確認(2026-06-07): 保存成功後、`/admin/inventory_back_order_rules/[新規ID]_InventoryBackOrderRule` の**詳細画面へ直接遷移**する（TEST_FAQ_予約販売ルール_遷移確認_20260607 で実測） |

- **入力項目**:

| 項目（ラベル原文） | 必須/任意 | 形式・選択肢・プレースホルダ |
|:--|:--|:--|
| タイトル * | 必須 | テキスト入力。プレースホルダ「2025SS先行販売」 |

- **ページタイトル**: 「予約販売のルールを作成する」

- **スクショ**: ![予約販売作成フォーム](screenshots/08-back-order-rule-create.png)

---

### 販売上限 一覧（/admin/inventory_sale_limit_rules）

- **目的**: 販売数量の上限ルールの一覧表示・新規作成へのナビゲーション。

- **操作要素と挙動（実測）**:

| 要素（UIラベル原文） | 種別 | 操作すると何が起こるか |
|:--|:--|:--|
| 販売上限ルールを作成する | リンクボタン | `/admin/inventory_sale_limit_rules/create` へ遷移 |

- **空状態**: 「アイテムが見つかりませんでした」と表示。

---

### 販売上限 作成フォーム（/admin/inventory_sale_limit_rules/create）

- **目的**: 新しい販売上限ルールを作成する。ルール名とチャネルを設定する。

- **操作要素と挙動（実測）**:

| 要素（UIラベル原文） | 種別 | 操作すると何が起こるか |
|:--|:--|:--|
| ← （戻る矢印） | リンク | 販売上限一覧へ戻る |
| 保存する | ボタン | フォームを送信してルールを作成する |

- **入力項目**:

| 項目（ラベル原文） | 必須/任意 | 形式・選択肢・プレースホルダ |
|:--|:--|:--|
| 販売上限ルール名 * | 必須 | テキスト入力。プレースホルダなし |
| チャネル | **必須**（訂正: 旧「任意（推測）」→「必須（1件以上必要）」） | チェックボックスリスト。販売チャネル統合が設定済みの場合に選択肢が表示される（実測時はリスト空）。チャネル未統合環境では選択不可能でルール作成自体が不可。 |

- **ページタイトル**: 「販売上限ルールを作成する」

- **注意**: 「チャネル」セクションの選択肢は販売チャネル（Shopify等）の統合設定状況に依存すると推測。✓実機確認(2026-06-07): チェックボックスリスト要素（`group "チャネル"` > `list`）は DOM 上に存在するが、リスト内のアイテムは0件（空リスト）。UIにはラベル「チャネル」と空のチェックボックスエリアのみが表示される。注記文言は画面に存在しない（「チャネルを統合してください」等のガイダンステキストなし）。選択肢ゼロのままでも「保存する」ボタンは押下可能だが、チャネル未選択でバリデーションエラーが出る（B-1確認済み: 1件以上必須）。依存関係の本質（統合状況によって選択肢が変わる）は外部要因のため実証不可。

- **スクショ**: ![販売上限作成フォーム チャネル空](screenshots/kill-sales-settings-sale-limit-channel-empty-2026-06-07.png)

- **スクショ**: ![販売上限作成フォーム](screenshots/08-sale-limit-rule-create.png)

---

### 販売閾値 一覧（/admin/inventory_threshold_rules）

- **目的**: 在庫の販売閾値ルールの一覧表示・新規作成へのナビゲーション。

- **操作要素と挙動（実測）**:

| 要素（UIラベル原文） | 種別 | 操作すると何が起こるか |
|:--|:--|:--|
| 販売閾値ルールを作成する | リンクボタン | `/admin/inventory_threshold_rules/create` へ遷移 |

- **空状態**: 「アイテムが見つかりませんでした」と表示。

---

### 販売閾値 作成フォーム（/admin/inventory_threshold_rules/create）

- **目的**: 新しい販売閾値ルールを作成する。ルール名とデフォルト閾値の有効化を設定する。

- **操作要素と挙動（実測）**:

| 要素（UIラベル原文） | 種別 | 操作すると何が起こるか |
|:--|:--|:--|
| ← （戻る矢印） | リンク | 販売閾値一覧へ戻る |
| デフォルトの閾値を設定する（チェックボックス） | チェックボックス | オンにすると「デフォルトの閾値*」数値入力欄1つが展開される（訂正: 旧「推測」→「検証1で確定」）✓実機確認済み(2026-06-06) |
| 保存する | ボタン | ✓実機確認(2026-06-07): 保存成功後、**販売閾値一覧（`/admin/inventory_threshold_rules`）へ遷移**する（詳細画面ではなく一覧へ）。販売価格・予約販売とは異なる挙動 |

- **入力項目**:

| 項目（ラベル原文） | 必須/任意 | 形式・選択肢・プレースホルダ |
|:--|:--|:--|
| ルール名 * | 必須 | テキスト入力。プレースホルダなし |
| デフォルトの閾値を設定する | 任意 | チェックボックス。説明文: 「この設定を有効にすることで、閾値ルールに含まれていないすべての商品バリエーションにデフォルト閾値を適用することができます。」 |

- **ページタイトル**: 「販売閾値ルールを作成する」

- **販売閾値の目的（実機確認）**: 閾値ルールに含まれていない商品バリエーションを含む全バリエーションに対し、在庫の販売可否を制御する閾値を設定するルール。

- **スクショ**: ![販売閾値作成フォーム](screenshots/08-threshold-rule-create.png)

---

### セール価格 SKU一覧（/admin/product_price_rules/[id]/product_variant_sales）

- **目的**: 販売価格ルール内の「セール価格」に紐付けられたSKUと期間・価格を一覧表示する。新規登録・CSVインポートもここから行う。

- **操作要素と挙動（実測）**:

| 要素（UIラベル原文） | 種別 | 操作すると何が起こるか |
|:--|:--|:--|
| ← 販売価格ルール (セール価格) | リンク（パンくず） | 詳細画面へ戻る |
<!-- 訂正2026-06-10: 旧「販売価格ルール（セール価格）」（全角括弧）→「販売価格ルール (セール価格)」（半角スペース・半角括弧）。実機再検証(LIVE-RECHECK-2026-06-10.md)で確定 -->
| インポート | リンクボタン | `/admin/csv_import/csv_import_operation_product_price_rule_sale_prices` へ遷移（CSVインポート画面） |
| セール価格を登録 | リンクボタン | セール価格 作成フォームへ遷移 |
| SKUコードで検索する | 検索フィールド | 入力したSKUコードで一覧をフィルタリング |
| 前へ / 次へ | ページネーションボタン | 前ページ / 次ページへ移動 |

- **表示情報**:
  - ページタイトル: 「販売価格ルール (セール価格)」（半角スペース・半角括弧）
<!-- 訂正2026-06-10: 旧「販売価格ルール（セール価格）」（全角括弧。通常=半角/セール=全角の差異あり）→「販売価格ルール (セール価格)」（半角括弧）。2026-06-10実機では通常・セールとも半角括弧で差異なし。実機再検証(LIVE-RECHECK-2026-06-10.md)で確定 -->
  - サブタイトル: ルール名
  - 空状態: 「アイテムが見つかりませんでした」「絞り込みや検索ワードを変更してみてください」

- **補足**: ✓実機確認(2026-06-07): 通常価格一覧（`/product_variant_regulars`）の画面上部ボタン群は「通常価格を登録する」のみ。**「インポート」ボタンは存在しない**。セール価格一覧（`/product_variant_sales`）には「インポート」ボタンがあるため、インポート機能はセール価格専用。通常価格は1件ずつ手動登録のみ対応。

  - **スクショ**: ![通常価格一覧（インポートなし）](screenshots/kill-sales-settings-regular-price-no-import-2026-06-07.png)

---

### セール価格 作成フォーム（/admin/product_price_rules/[id]/product_variant_sales/create）

- **目的**: 販売価格ルールに新しいSKUのセール価格（期間・価格）を登録する。

- **操作要素と挙動（実測）**:

| 要素（UIラベル原文） | 種別 | 操作すると何が起こるか |
|:--|:--|:--|
| ← （戻る矢印） | リンク | セール価格SKU一覧画面へ戻る |
| 選択 | ボタン | 商品バリエーション（SKU）を選択するダイアログを開く |
| 保存する | ボタン | 入力内容でSKUにセール価格を登録する |

- **入力項目**:

| 項目（ラベル原文） | 必須/任意 | 形式・選択肢・プレースホルダ |
|:--|:--|:--|
| 商品バリエーションを選択する * | 必須 | テキスト + 「選択」ボタン。プレースホルダ「選択してください」 |
| 価格 * | 必須 | 数値入力（spinbutton）。通貨記号「￥」付き |
| 価格（税込） | 任意 | 数値入力（spinbutton）。通貨記号「￥」付き |
| 開始日時 * | 必須 | 日時入力（テキストボックス）。プレースホルダなし |
| 終了日時 | 任意 | 日時入力（テキストボックス）。プレースホルダなし |

- **ページタイトル**: 「セール価格を作成する」、サブタイトル: ルール名

- **通常価格との差異**: セール価格は「開始日時（必須）」「終了日時（任意）」の期間設定フィールドが追加されている。通常価格に期間設定はない。

- **スクショ**: ![セール価格作成フォーム](screenshots/08-price-rule-sale-create.png)

---

## 抜け漏れ検証（2026-06-06）

> 検証方針: 最優先3点（予約販売全容・販売閾値チェックボックスON時詳細・販売上限フォームの上限数量欄）を先に確認し、順次エッジケース・未踏画面を検証する。内部操作（TEST_FAQ_接頭辞でのデータ作成）はOK。外部連携への実反映・顧客通知・課金・破壊操作は手前で止める。

---

### 検証1: 販売閾値 作成フォーム「デフォルトの閾値を設定する」チェックボックスON時の展開フォーム

**結果: 確認済み**

チェックボックスをONにすると、以下の1項目のみが展開される:

| 項目（ラベル原文） | 必須/任意 | 形式・初期値 |
|:--|:--|:--|
| デフォルトの閾値 * | 必須 | 数値テキストボックス。プレースホルダ「入力してください」。初期値「0」 |

- 単位表示（個/点など）はなし
- 対象範囲の選択肢（全バリエーション対象固定で変更不可）はなし
- チェックボックスOFF時はこの項目は非表示

**推測ではなく確定**: 「デフォルトの閾値を設定する」をONにすると数値入力欄1つだけが展開される。単位・対象範囲の追加設定項目は存在しない。

- **スクショ**: ![閾値チェックボックスON時](screenshots/gap-sales-settings-threshold-default-checked.png)

---

### 検証2: 販売閾値ルール 詳細画面・子フォーム全容（/admin/inventory_threshold_rules/[id]）

**結果: 確認済み（TEST_FAQ_販売閾値ルールで実測）**

#### 詳細画面の構成

| 要素（ラベル原文） | 種別 | 内容 |
|:--|:--|:--|
| ← （戻る） | リンク | 閾値一覧へ |
| ルール名 | 見出し（H1） | 例: TEST_FAQ_販売閾値ルール |
| 「情報」バッジ | バッジ | 「デフォルト閾値が有効」と表示（デフォルト閾値設定時のみ） |
| 自動追加ルール | リンクボタン | `/automatic_add_rules` サブ画面へ遷移 |
| ルールを編集する | ボタン | 編集モーダルを開く |
| 閾値を追加する | リンク | `/[id]/create` — 個別SKU閾値登録フォームへ |
| SKUコードで検索する | 検索フィールド | 登録済みSKUを絞り込む |

- 一覧部分は空状態（SKU未登録）: 「アイテムが見つかりませんでした」表示

#### 「閾値を追加する」フォーム（/admin/inventory_threshold_rules/[id]/create）

| 項目（ラベル原文） | 必須/任意 | 形式・初期値 |
|:--|:--|:--|
| 商品バリエーションを選択する * | 必須 | テキスト+「選択」ボタン。プレースホルダ「選択してください」 |
| 閾値 * | 必須 | spinbutton（数値）。初期値「0」 |

- 単位表示なし。閾値割れ時の具体的挙動（販売停止/通知）はUI上に説明文なし<!-- TODO: 実際の挙動（販売停止か通知か）は実機での在庫操作が必要で未確認 -->

#### 「自動追加ルール」サブ画面（/automatic_add_rules）

**新発見**: 閾値ルールに商品バリエーションを条件ベースで自動追加する機能が存在する。

「自動追加ルールを追加する」フォーム（/automatic_add_rules/create）の入力項目:

| 項目（ラベル原文） | 必須/任意 | 形式・選択肢 |
|:--|:--|:--|
| 項目 | **必須** | コンボボックス。選択肢: 「ブランドコード」のみ。バリデーション「項目を選択してください」✓実機確認済み(2026-06-07) |
| 条件 | **必須** | コンボボックス。選択肢: 「一致する」のみ。バリデーション「条件を選択してください」✓実機確認済み(2026-06-07) |
| 値 | **必須** | テキスト入力。プレースホルダなし。バリデーション「値を入力してください」✓実機確認済み(2026-06-07) |
| 閾値 | **実質必須**（初期値「1」あり） | spinbutton（数値）。初期値「1」。上下矢印ボタン付き。✓実機確認済み(2026-06-07) |

- 意味: 「ブランドコードが〇〇と一致するバリエーション」を閾値ルールに自動追加し、個別閾値を一括設定できる機能と解釈できる
- ✓実機確認(2026-06-07): 「項目」コンボボックスの全選択肢を DOM で列挙。選択肢は `option "選択してください"（disabled/placeholder）` と `option "ブランドコード"` の2つのみ。**現時点で「ブランドコード」が唯一の実選択肢**。（将来的な追加有無はUIでは判断不可）

- **スクショ**: ![閾値詳細画面](screenshots/gap-sales-settings-threshold-detail.png)
- **スクショ**: ![閾値を追加するフォーム](screenshots/gap-sales-settings-threshold-add-form.png)
- **スクショ**: ![自動追加ルール作成フォーム](screenshots/gap-sales-settings-threshold-auto-add-rule.png)

---

## 正確性監査（2026-06-07）

> 実機: https://www.sqstackstaging.com/admin (組織: stack-ps-yosuke)
> 照合順: (1)保存後遷移の推測vs実測 + 通貨表記食い違い → (2)販売上限の上限数量欄 → (3)予約販売の詳細画面・子フォーム → (4)その他疑義項目

---

### 監査D: 通常価格作成フォームの URL誤記・通貨記号・税込フィールド任意確認

**照合対象**: 「価格(税込)の任意/必須が要確認」「通常価格/セール価格の通貨記号 ¥（半角）vs ￥（全角）食い違い」「通常価格SKU一覧URL」

**結果**: 複数の誤記録を発見・訂正。

#### D-1: 通常価格SKU一覧のURL（訂正）

- 記録L182: `/admin/product_price_rules/[id]/regular_prices` → **誤り**
- 実機URL: `/admin/product_price_rules/[id]/product_variant_regulars` ✓実機確認済み(2026-06-07)
- 「通常価格を登録する」リンクURL: `/product_variant_regulars/create`
- ページタイトル: 「販売価格ルール (通常価格)」（括弧が半角スペースと半角括弧）
- パンくずラベル: 「← 販売価格ルール（通常価格）」— 記録L190は「← 販売価格ルール（通常価格）」（全角括弧）

#### D-2: 通常価格作成フォームURL（訂正）

- 記録L204: `/admin/product_price_rules/[id]/regular_prices/create` → **誤り**
- 実機URL: `/admin/product_price_rules/[id]/product_variant_regulars/create` ✓

#### D-3: 「価格(税込)」の任意/必須（確定）

- 「保存する」押下時のバリデーション:
  - 「商品バリエーションを選択してください」エラー → 必須
  - 「価格を入力してください」エラー → 必須
  - 「価格(税込)」 → **エラーなし → 任意で正しい** ✓実機確認済み(2026-06-07)
- 記録L222「価格（税込）｜任意」は正確。「推測」を「確定」に更新。

#### D-4: 通貨記号の全角/半角（訂正）

- 通常価格作成フォームの価格フィールド: `￥`（全角）— DOM: `generic "￥"`
- 価格(税込)フィールド: `￥`（全角）— 同上
- 記録L221「通貨記号「¥」付き（半角）」は **誤り** → 正しくは `￥`（全角）
- セール価格フォームの記録L390「￥」（全角）は正確
- **結論**: 通常価格・セール価格ともに全角「￥」で統一されており、記録の「¥（半角）」「￥（全角）」の食い違いは実機差異ではなく**通常価格フォームの記録が誤り**

#### D-5: 「価格(税込)」ラベルの括弧（訂正）

- 実機ラベル: `価格(税込)` （半角括弧）
- 記録L222: `価格（税込）`（全角括弧）→ **誤り**

- **スクショ**: ![通常価格作成フォームバリデーション](screenshots/audit-sales-settings-regular-price-create-validation-2026-06-07.png)

---

### 監査B: 販売上限 作成フォームの「チャネル」必須/任意 + 詳細画面・上限数量欄の確認

**照合対象**: 「チャネル任意（推測）」「販売上限フォームに上限数量欄が無い」

**結果**: 2点とも訂正必要。

#### B-1: チャネルの必須/任意（訂正）

実機確認:
- 作成フォームで「保存する」を押すと「**チャネルを選択してください**」エラーが表示される（フォームUI実測）
- GraphQL API でも `channelSources: []`（空配列）で送ると「**channelSourcesの要素数は1件以上でなければなりません**」エラーが返る
- **結論: チャネルは必須（1件以上）**。記録L299「任意（推測）」は誤り。

**訂正**: L299「チャネル ｜ 任意（推測）」→「チャネル ｜ **必須**（1件以上選択が必要）」

- ただしチャネル統合（Shopify等）が設定されていない環境では選択肢が0件となり、作成フォームはフォームとして成立しない（チャネル統合なしでは販売上限ルールの作成自体が不可能）
- この環境（stack-ps-yosuke）ではチャネル統合未設定のためUIに選択肢がなく、ルール作成ができない

- **スクショ(フォームバリデーション)**: ![販売上限作成フォームバリデーション](screenshots/audit-sales-settings-sale-limit-create-validation-2026-06-07.png)

#### B-2: 販売上限の詳細画面・子フォーム（上限数量欄）（GraphQL確認）

チャネル統合未設定のためUIでの詳細画面確認は不可。GraphQLスキーマ introspection（実機APIから取得）で判明した構造:

**GraphQLスキーマから確定した子フォームの入力フィールド**（`InventorySaleLimitRuleProductVariantCreateInput`）:

| フィールド名 | 説明（API） | 型 | 必須/任意 |
|:--|:--|:--|:--|
| `productVariantID` | 商品のバリエーションのID | ID | 必須 |
| `limit` | **販売上限数** | Int（整数） | 必須 |
| `startsAt` | **販売数を集計する開始日時** | DateTime | 必須 |

- `limit`（販売上限数）フィールドが存在することをAPIで確認。これが記録L296-299に欠けていた肝心の数量欄。
- `startsAt`（集計開始日時）は完全未記録の項目。販売上限は「いつから何個まで」という期間付き上限設定。
- UIラベルは未確認（詳細画面に到達できなかったため）。<!-- TODO: チャネル統合設定後にUIラベルを実機確認 -->

**追加判明事項（`InventorySaleLimitRuleProductVariant` 型から）**:
- 子レコードは `id`, `productVariant`, `limit`, `startsAt`, `createdAt`, `updatedAt` を持つ
- ✓実機確認(2026-06-07): GraphQL introspection で全Mutationを検索した結果、`automatic`を含むMutationは `catalogAutomaticAddRuleCreate`/`catalogAutomaticAddRulesDelete`（カタログ向け）と `inventoryThresholdRuleAutomaticAddRuleCreate`/`inventoryThresholdRuleAutomaticAddRuleUpdate`/`inventoryThresholdRuleAutomaticAddRulesDelete`（販売閾値向け）の5件のみ。**`inventorySaleLimitRule`系の自動追加Mutationは存在しない。販売上限ルールには自動追加ルール機能は実装されていない**（販売閾値専用機能）。

**記録L285「目的: ルール名とチャネルを設定する」への訂正**: 作成フォームはルール名＋チャネルのみだが、詳細画面でSKUごとに「販売上限数（limit）」と「集計開始日時（startsAt）」を登録する子フォームが存在する（API確認済み）。UIラベル原文は未確認。

---

### 監査E: 自動追加ルールの必須/任意バリデーション確定

**照合対象**: 「自動追加ルールの4項目すべて必須（推測）」

**結果**: ✓実機確認済み(2026-06-07) — バリデーションエラーで確定。

| 項目（ラベル原文） | バリデーションエラー文言 | 必須/任意 確定 |
|:--|:--|:--|
| 項目 | 「項目を選択してください」 | **必須** |
| 条件 | 「条件を選択してください」 | **必須** |
| 値 | 「値を入力してください」 | **必須** |
| 閾値 | エラーなし（初期値「1」があるため空にならない） | **実質必須**（初期値あり） |

- 記録L463-467の「必須（推測）」× 4項目はすべて**実機バリデーションで確定**。「（推測）」表記を削除。
- 「閾値」は「保存する」時点で初期値「1」が入っているためバリデーションエラーが出ないが、フィールド自体はspinbuttonで削除不可のため実質必須。

**追加確認事項**:
- ページタイトル: 「自動追加ルールを追加する」（記録L460「/automatic_add_rules/create）のフォーム」に対して正式タイトルを補記）
- 「閾値」フィールドにはスピンボタン（上下矢印ボタン）付きのUI
- 「条件」の選択肢は現状「一致する」のみ（記録L465と一致✓）
- 「項目」の選択肢は現状「ブランドコード」のみ（記録L464と一致✓）

- **スクショ**: ![自動追加ルールバリデーション](screenshots/audit-sales-settings-auto-add-rule-validation-2026-06-07.png)

---

### 監査C: 予約販売の詳細画面・子フォーム全容（最大空白の解消）

**照合対象**: 「予約販売フォームがタイトル1項目のみ」「詳細画面・子フォームが完全未踏」

**結果**: ✓実機確認済み(2026-06-07) — 詳細画面・子フォームを実測。

#### C-1: 作成フォーム（/admin/inventory_back_order_rules/create）

- 入力項目: 「タイトル*」のみ（必須）— L261の記録と一致✓
- ページタイトル: 「予約販売のルールを作成する」— L263の記録と一致✓
- GraphQL API で `name` フィールドのみ（チャネル等なし）— API確認済み

#### C-2: 詳細画面（/admin/inventory_back_order_rules/[id]）— **新規実測**

| 要素（UIラベル原文） | 種別 | 操作すると何が起こるか |
|:--|:--|:--|
| ← （戻る矢印） | リンク | 予約販売一覧へ戻る |
| ルールを編集する | ボタン | 編集モーダルを開く |
| インポート | リンクボタン | `/admin/csv_import/csv_import_operation_inventory_back_order_rule_product_variants` へ遷移 |
| バリエーションを追加する | ボタン | `/[id]/create` — SKU登録フォームへ遷移 |
| SKUコードで検索する | 検索フィールド | 登録済みSKUを絞り込む |
| 前へ / 次へ | ページネーションボタン | 前ページ / 次ページ |

- ページタイトル（H1）: ルール名そのもの（例: TEST_FAQ_予約販売ルール）
- 空状態: 「アイテムが見つかりませんでした」「絞り込みや検索ワードを変更してみてください」

**注目**: 詳細画面はタイトルH1がルール名そのもの。販売価格ルール詳細（H1「販売価格ルール」＋サブタイトルにルール名）とは構成が異なる。

#### C-3: バリエーション追加フォーム（/[id]/create）— **新規実測**

| 項目（UIラベル原文） | 必須/任意 | 形式・初期値 |
|:--|:--|:--|
| 商品バリエーション* | 必須 | テキスト＋「選択」ボタン。プレースホルダ「選択してください」 |
| 販売数* | 必須 | spinbutton（数値）。初期値「0」 |

- ページタイトル: 「バリエーションを追加する」、サブタイトル: ルール名
- URL: `/admin/inventory_back_order_rules/[id]/create`
- **「販売数」はGraphQL `quantity`（予約販売を受け付ける数量）に対応するUIラベル原文**
- APIの description: 「予約販売が可能な在庫数の固定値。注文も考慮した現在の予約販売可能数はInventoryLevel経由で取得することができます。」

#### C-4: 編集モーダル（詳細画面上）

- モーダルタイトル: 「ルールを編集する」
- 入力項目: 「タイトル*」のみ（既存ルール名が初期値）
- ボタン: 「閉じる」「キャンセル」「保存する」

#### C-5: インポート機能の存在

- インポートURL: `/admin/csv_import/csv_import_operation_inventory_back_order_rule_product_variants`
- CSVインポートで複数SKUを一括登録する経路が存在（詳細未踏）<!-- TODO: インポートフォームの詳細確認 -->

**旧記録（L261）への補足**: 作成フォームが「タイトル*」1項目のみという記録は正確。SKUごとの「販売数」設定は詳細画面の「バリエーションを追加する」子フォームで行う。期間設定フィールドはUIに存在しない（販売上限の`startsAt`に相当するものはなし）。

- **スクショ(詳細)**: ![予約販売詳細画面](screenshots/audit-sales-settings-back-order-detail-2026-06-07.png)
- **スクショ(バリエーション追加フォーム)**: ![予約販売バリエーション追加フォーム](screenshots/audit-sales-settings-back-order-variant-add-form-2026-06-07.png)
- **スクショ(編集モーダル)**: ![予約販売編集モーダル](screenshots/audit-sales-settings-back-order-edit-modal-2026-06-07.png)

---

### 監査A: 販売価格ルール詳細画面の実在確認・保存後遷移の確定・通貨表記照合

**照合対象**: 「保存後の遷移が推測のまま事実化」「通貨の表示表記が画面間で食い違い（詳細バッジ『日本円』vs 編集モーダル『JPY』）」

**結果**: ✓実機確認済み(2026-06-07)

1. **詳細画面（/admin/product_price_rules/[id]）の実在**: 実機で確認。URLは `/admin/product_price_rules/eb8cf243-650d-5172-a01b-594d4ae4587f_ProductPriceRule` 形式。L135以降の記述内容（ページタイトル「販売価格ルール」・バッジ・通常価格/セール価格カード）はすべて実測と一致。

2. **保存後遷移の確定（2026-06-07 追加実測で確定）**: ✓実機確認(2026-06-07) — TEST_FAQ_販売価格ルール_遷移確認_20260607 を実際に作成・保存したところ、保存後 `/admin/product_price_rules/409e8f6d-d73a-5c7b-a583-30559aa8a62b_ProductPriceRule`（詳細画面）へ遷移することを直接確認。**推測を事実に置換**。同様に予約販売も詳細画面へ遷移（TEST_FAQ_予約販売ルール_遷移確認_20260607 で実測）。**販売閾値のみ異なり一覧（`/admin/inventory_threshold_rules`）へ遷移**（TEST_FAQ_販売閾値ルール_遷移確認_20260607 で実測）。

  - スクショ(販売価格保存後): ![販売価格保存後遷移先](screenshots/kill-sales-settings-price-rule-save-redirect-2026-06-07.png)
  - スクショ(販売閾値保存後): ![販売閾値保存後遷移先](screenshots/kill-sales-settings-threshold-rule-save-redirect-2026-06-07.png)

3. **通貨表記の食い違い（日本円 vs JPY）**: 実機で再確認。
   - 詳細画面バッジ: `通貨: 日本円` (和名)
   - 編集モーダルのフィールド: `JPY` (ISO英字コード、disabled)
   - **これは記録の取り違えではなく、UIの仕様として2画面で異なる表記を使っている**。L150とL176の記録は両方正確。「食い違い」は仕様上の差異であり矛盾ではない。訂正不要。

4. **新発見**: 編集モーダルの「ルール名」フィールドのプレースホルダーが `販売価格 - 越境EC`（作成フォームのプレースホルダー「年末セール価格」とは異なる）。L175の作成フォーム記録は正確だが、編集モーダルのプレースホルダーは未記録だった。

- **スクショ(詳細)**: ![販売価格ルール詳細2026-06-07](screenshots/audit-sales-settings-price-rule-detail-2026-06-07.png)
- **スクショ(編集モーダル)**: ![販売価格ルール編集モーダル2026-06-07](screenshots/audit-sales-settings-price-rule-edit-modal-2026-06-07.png)

**本文L120への対応**: L120「成功時に詳細画面へ遷移する（推測）」の注記は保存後の遷移先のみ推測であり、詳細画面の記述（L135以降）は一覧→クリックで実測済み。L120の「（推測）」を「（保存後遷移先のみ推測。詳細画面自体は一覧→クリックで実測済み）」に訂正する。

---

## 監査総括（2026-06-07）

| 監査 | 照合内容 | 結果 |
|:--|:--|:--|
| A | 詳細画面遷移の推測確定・通貨表記食い違い（日本円 vs JPY） | 推測→実測の境界を明確化。両表記は仕様通りで訂正不要 |
| B | 販売上限チャネル任意/必須・上限数量欄の存在 | チャネルは「必須（1件以上）」に訂正。上限数量（limit）+集計開始日時（startsAt）の子フォームをGraphQL確認 |
| C | 予約販売詳細画面・バリエーション追加フォーム実測 | 詳細画面・編集モーダル・バリエーション追加フォーム（販売数*）を完全実測 |
| D | 通常価格URL誤記・通貨記号・税込フィールド任意確認 | URL `/regular_prices`→`/product_variant_regulars` に訂正。通貨記号は全角「￥」で統一。税込は任意で確定 |
| E | 自動追加ルール4項目の必須/任意バリデーション | 4項目（項目・条件・値・閾値）すべて必須を実機バリデーションで確定 |

**訂正件数**: 6件（チャネル必須/任意・通常価格URL×2・通貨記号・ラベル括弧・チェックボックス推測注記）
**新発見**: 3件（販売上限の `startsAt` 集計開始日時フィールド・予約販売詳細画面全容・インポート機能の存在確認）
**なお未確認**: チャネル統合設定時の販売上限詳細画面UIラベル・各ルールの閾値割れ/上限到達時の実挙動（在庫操作を伴うため本環境では未実施）

---

## 推測の潰し込み（2026-06-07）

| 推測番号 | 対象（元L番号） | 結果 | 確定内容 |
|:--|:--|:--|:--|
| 推測1 | L307: チャネルリスト空の目視確認 | ✓確定 | DOM上 `group "チャネル" > list` は空（0件）。ガイダンステキストなし。選択肢ゼロのまま保存するとバリデーションエラー（1件以上必須） |
| 推測2 | L373: 通常価格一覧のインポートボタン有無 | ✓確定（**インポートなし**） | `/product_variant_regulars` 画面のボタンは「通常価格を登録する」のみ。「インポート」ボタンは存在しない。セール価格専用機能 |
| 推測3 | L474: 自動追加ルール「項目」選択肢の全列挙 | ✓確定 | `option "ブランドコード"` のみが唯一の実選択肢（プレースホルダ除く）。DOM上で全選択肢を列挙し確認 |
| 推測4 | L571: 販売上限の自動追加機能有無（GraphQL） | ✓確定（**機能なし**） | GraphQL introspection で `automatic` 系Mutationを全検索: 販売閾値向け5件のみ存在、`inventorySaleLimitRule`系は0件。自動追加は販売閾値専用 |
| 推測5a | L677/監査A: 販売価格ルール保存後遷移先 | ✓確定（**詳細画面**） | TEST_FAQ_データで実測: 保存後 `/admin/product_price_rules/[ID]_ProductPriceRule`（詳細画面）へ遷移 |
| 推測5b | 予約販売ルール保存後遷移先 | ✓確定（**詳細画面**） | TEST_FAQ_データで実測: 保存後 `/admin/inventory_back_order_rules/[ID]_InventoryBackOrderRule`（詳細画面）へ遷移 |
| 推測5c | 販売閾値ルール保存後遷移先 | ✓確定（**一覧**、販売価格・予約販売と異なる） | TEST_FAQ_データで実測: 保存後 `/admin/inventory_threshold_rules`（一覧）へ遷移。詳細画面ではない |

**解決済み推測**: 7件（5項目・うち推測5は3サブ項目）
**確定/訂正の内訳**: 確定7件（うち訂正相当2件: 推測2=インポートなし確定・推測4=自動追加機能なし確定・推測5c=閾値のみ一覧遷移と判明）
**なお残る②③④分類**:
- ③外部: チャネル統合設定時の販売上限詳細画面UIラベル（外部連携が必要）
- ②実データ: 閾値割れ/上限到達時の実挙動（在庫操作=実データが必要）
