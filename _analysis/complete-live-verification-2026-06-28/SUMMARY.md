# 04-notion 完全実機検証 進捗サマリー 2026-06-28

## 目的

`04-notion` 全24ファイルに書かれている事項を、実機証跡で裏付けるための追加監査。

## 今回進めたこと

- `04-notion` 全24ファイルから「未確認 / 要確認 / TODO / 連携待ち」等を含む行を抽出
- 抽出行 450件を、実機再確認可能・外部/実データ待ち・影響操作・見出し等に分類
- 2026-06-28時点の現行URL索引から、具体URL 146件をPlaywright CDP接続で再巡回
- 保存した証跡: `route-crawl-20260628.json`

## 2026-06-28 URL再巡回の結果

| 区分 | 件数 | URL |
|:--|--:|:--|
| 巡回対象の具体URL | 146 | `04-notion` 現行URL索引 |
| クロール自体のエラー | 0 | なし |
| 404/存在しないページ | 10 | `/admin/inventory_suppliers`, `/admin/local_pickup_product_variants/create`, `/admin/order_returns/create`, `/admin/pdf_export/pdf_export_operation_packing_slips/create`, `/admin/sale_change_line_items/create`, `/admin/settings/members`, `/admin/settings/metafield_definitions/create`, `/admin/settings/organization_users`, `/admin/settings/staffs`, `/admin_api` |
| 予期せぬエラー画面 | 4 | `/admin/draft_orders/create`, `/admin/inventory_allocation_requests`, `/admin/orders/create`, `/admin/purchasing_customers/create` |
| TODO表示 | 4 | `/admin/analytics`, `/admin/analytics/reports`, `/admin/analytics/revenue`, `/admin/b2b` |

## 2026-06-28 TODO/未実装表示の追加再確認

保存した証跡: `visible-state-recheck-20260628.json`, `visible-state-recheck-20260628.md`

| 区分 | URL |
|:--|:--|
| 本文 `TODO` | `/admin/analytics`, `/admin/analytics/revenue`, `/admin/analytics/reports`, `/admin/b2b`, `/admin/settings/apps/c960abe2-56d7-5b23-b8f2-ec66df6c8060_App/admin_api` |
| 存在しない画面 | `/admin/order_returns/create`, `/admin/pdf_export/pdf_export_operation_packing_slips/create`, `/admin_api` |
| 予期せぬエラー | `/admin/orders/create` |
| 空一覧 | `/admin/order_returns`, `/admin/orders`, `/admin/settings/metafield_definitions` |
| 本文 `TODO` なし | `/admin/settings/metafield_definitions`, `/admin/settings/translation`, `/admin/settings/translation/translation_rules` |

## 今回見つけた本文修正

- `/admin/inventory_allocation_requests` は、2026-06-28現在のstagingでは `該当するProductVariantが見つかりませんでした。` のエラー画面になる。
- 2026-06-27の一気通貫検証で確認した在庫依頼フロー自体は残しつつ、現在の一覧URLはデータ欠損と思われるエラーで表示不可と明記した。
- 作成フォーム `/admin/inventory_allocation_requests/create` と確保済み一覧 `/admin/inventory_allocation_request_confirmations` は2026-06-28時点でも表示可能。
- 商品詳細（商品コード `486125`）で既存バリエーション軸を展開し、`オプション名` / `オプション値` / `コード` / `オプション値を追加` は表示されるが、作成フォームの `別のオプションを追加する` は表示されないことを確認した。
- 同じ商品詳細内に `採寸` / `測定` / `measurement` / `product_measurement` 系の入力・選択項目は表示されず、採寸ルールの商品紐づけは管理画面UI上にないものとして本文を更新した。
- カタログ詳細は現行UIで `商品` / `SKU` タブ切替。商品登録済みカタログのSKUタブは列 `商品` / `品番(SKU)` の2列で、旧URL `/admin/catalogs/{id}/catalog_product_variants` は対象カタログで存在しないことを確認した。
- 商品メタフィールド定義を一時作成し、行選択後の操作が `定義を削除` のみで編集導線がないことを確認した。検証用定義は削除済み。
- 取引先詳細の入力項目は `仕入れ先ベンダー名` と `取引先コード` の2つのみ。所在地/連絡先項目はなく、詳細の `その他の操作` には `アーカイブ` のみ表示され、削除は表示されない。一方、一覧の行選択では `アーカイブする` から削除確認ダイアログへ進めることを後続確認した。
- 在庫詳細の破損・検品・予備は表示列として存在するが、編集ボタンは `販売可能数を編集` のみ。`その他のアクション` は `在庫振替を行う` のみで、調整伝票の理由も `廃棄` / `紛失` / `在庫差異` / `その他` の4種のみ。管理画面UIから3区分へ直接振り替える導線はない。
- 発注伝票 `#IP-1007` 詳細で、本文・ボタン・リンク・`その他の操作` にPDF/印刷導線がないことを確認した。`その他の操作` はdisabledの `キャンセルする` のみ。
- 権限リソースだけに存在していた `announcements` / `mile_items` / `next_engine_integrations` を直接URLで確認。`/admin/announcements` は画面あり、`/admin/mile_items` と `/admin/next_engine_integrations` は存在しないページ。
- 管理メンバー作成・除外を追加検証。権限グループ未選択でもユーザー作成でき、詳細/一覧の権限グループは `未設定`、適用権限は `0件` になる。`組織から除外する` は確認ダイアログ `ユーザーを組織から除外しますか？` → `除外する` で、除外後は一覧検索に出なくなった。同じメールアドレスで再追加でき、再追加後は検証用権限グループを反映できた。検証用ユーザーは最後に除外済み。
- 会社一覧 `/admin/companies` の `インポート` ボタンを再クリック確認。クリック後もURL・ダイアログ・ドロップダウン・トースト・本文変化がないため、会社CSVインポート手順ではなく未実装/未接続UIとして扱う。
- 会社作成フォームで検証用会社を新規保存し、保存後に会社詳細へ遷移すること、会社IDが詳細画面に表示されること、ロケーション/担当者は保存時に作成されず詳細から追加することを確認した。
- 会社本体・会社ロケーションの削除UIを再確認。会社一覧の行選択後、会社詳細、会社ロケーション詳細のいずれにも削除ボタン/リンクは表示されなかった。削除実行は未実施で、誤作成時の削除方法は開発元確認が必要。
- 取引先一覧 `/admin/settings/suppliers` の `インポート` ボタンを再クリック確認。クリック後もURL・ダイアログ・ドロップダウン・トースト・本文変化がないため、取引先CSVインポート手順ではなく未実装/未接続UIとして扱う。
- カタログ作成で既存と同じ `TEST_FAQ_カタログ001` を保存できることを確認した。カタログ名は一意制約ではない。作成後詳細の `その他の操作` から削除でき、検証用重複カタログは削除済み。
- カタログ自動追加ルールの実効を検証。検証用カタログで `製造元=GU` ルールを作成しても既存GU商品は即時追加されなかったが、ルール作成後に製造元 `GU` の検証用商品を新規作成するとカタログへ自動追加された。ルール削除後も追加済み商品は残り、商品本体削除後にカタログ商品行も消えた。検証用商品・カタログは削除済み。
- カタログ自動追加ルールの複数ルール/部分一致を追加検証。同一項目 `製造元` のルール3件中1件に一致する新規商品は自動追加されたため、同一項目ではOR条件として動く。ルール値に前方一致するだけの商品は自動追加されなかった。検証用商品2件・検証用カタログは削除済み。
- カタログ自動追加ルールの異種項目・複数値・ワイルドカードを追加検証。`製造元` と `ブランドコード` の混在ルールでもOR条件として動き、製造元だけ一致・ブランドコードだけ一致・両方一致の商品はいずれも自動追加された。カンマ区切りは複数値指定ではなく文字列の一部、`*` はワイルドカードではなく文字列として扱われる。検証用商品7件・検証用カタログ3件は削除済み。
- 注文・返品の空状態を再確認。`/admin/orders` は `すべて` タブと空状態のみで、注文0件の現行画面では検索・絞り込みUIが表示されない。`/admin/draft_orders` の `注文を作成する` は `href` なしのdisabled相当でクリックしても遷移しない。`/admin/order_returns` も空状態のみで、返品0件の現行画面では `キャンセル` / `交換出荷` の絞り込みUIは表示されない。直URL `/admin/orders/create` と `/admin/draft_orders/create` は予期せぬエラー、`/admin/order_returns/create` は存在しない画面。
- 内部GraphQLで検証用購入顧客を作成し、顧客管理のデータあり状態を追加確認した。顧客一覧は `顧客名` / `テナント` / `ステータス` / `メール購読` が表示され、一覧列にメールアドレスは表示されない。検索パネルは `メールアドレスで検索する`。詳細画面にはポイント残高/有効ポイント/会員ランク/マイル残高/ディスカウント/メタフィールド/顧客情報が表示され、検証注文後は直近の注文、初回/最終購入日、初回/最終購入場所が反映された。ポイント変動・会員ランク再算出は未確認。
- 同じ購入顧客を作成した後でも、`/admin/draft_orders` の `注文を作成する` はdisabled相当のままで、クリックしても遷移せず、`/admin/draft_orders/create` も予期せぬエラーのままだった。購入顧客の存在だけでは下書き注文作成は解放されないため、従来の「購入顧客があれば作成可能」という推測を撤回した。
- ディスカウント詳細の対象顧客タブを追加検証。同一テナントの購入顧客は `顧客を選択して追加` から検索・選択・追加でき、一覧に `名前` / `メールアドレス` / `テナント` で表示され、行選択後に削除できた。別テナントの購入顧客は候補に出ず、`顧客セグメントを選択して追加` は表示されるがdisabledだった。
- 販売価格ルールのセール価格で、終了日時空欄の登録を追加検証。`TEST_FAQ_DEEP_202606080340_販売価格ルール` で `TEST_E2E_20260622_GU_1905_NAVY_M` に税抜900/税込990/開始日時 `2026-06-28 10:00`/終了日時空欄のセール価格を保存でき、SKU別詳細では終了日時が `-` と表示された。取り消しは削除ではなく「無効化する」で、確認ダイアログは巻き戻し不可。無効化後に再読み込みすると一覧から消えた。
- 販売価格ルールの税込価格空欄を追加検証。通常価格は税抜1234/税込空欄で保存でき、一覧の税込価格は `￥0` 表示になった。通常価格は詳細の `削除する` で削除でき、削除後一覧から消えた。セール価格は税抜876/税込空欄/開始日時あり/終了日時空欄で保存でき、保存後一覧では税抜価格/税込価格が `-` / `-`、SKU別詳細では税抜価格 `￥876`、税込価格・終了日時とも `-` 表示になった。セール価格は行選択後の `無効化する` で無効化し、再表示で消えた。
- 販売閾値ルールの保存・削除を追加再確認。検証用ルールを作成すると保存後は販売閾値一覧へ戻り、詳細には `自動追加ルール` / `閾値を追加する` が表示された。SKU別閾値フォームの初期値は `0`、`TEST_E2E_20260622_GU_1905_NAVY_M` を閾値 `3` で保存できた。SKU別閾値は行選択後の `閾値を削除` で削除でき、削除直後は行が残るが再読み込み後に消える。検証用ルール本体も一覧行選択後の `削除する` で削除済み。閾値割れ時の販売停止・通知等の説明は現行UIにない。
- 発注伝票作成フォームの商品選択時の単価初期値を追加検証。`TEST_E2E_20260622_GU_1905_NAVY_M` を参照ダイアログから追加すると、保存前の明細は単価 `1990`、数量 `0`、税率 `10`、金額 `￥0` になった。対象SKU詳細は上代 `1990`、原価未設定だったため、この確認範囲では原価ではなく上代と同額が単価初期値として入る。発注伝票自体は保存していない。
- 発注伝票の通貨選択・下書き変更を追加検証。`#IP-1010` を `米ドル / USD` で下書き保存すると、詳細の通貨selectは有効で `USD` 選択、金額は `$100.00`。同じ下書きで `ユーロ / EUR` へ変更保存すると、再読み込み後も `EUR` 選択で金額は `€100.00` になった。為替レート入力・換算表示は見当たらない。追加で `#IP-1013` を発注済みにした後は通貨selectと保存ボタンが表示されず、通貨は値表示のみになった。
- 発注済み伝票の差し戻し・再編集導線を追加検証。発注済み未入荷の `#IP-1013` と入荷完了済みの `#IP-1007` で、詳細画面・`その他の操作`・直接URL `/update` / `/edit` を確認した。どちらも編集/再編集/差し戻し/下書き戻し導線はなく、直接 `/update` / `/edit` は「このページは存在しないようです」。`#IP-1013` の `キャンセルする` は有効、`#IP-1007` はdisabled。
- 取引先一覧の一括操作を追加検証。未使用の検証用取引先を作成すると発注伝票作成フォームの取引先候補に即時表示された。一覧で対象行を選択すると `アーカイブする` が表示されるが、確認ダイアログは `取引先を削除しますか？`、確定ボタンは `削除する`。未使用取引先は実行後に「すべて」タブ、「アーカイブ」タブ、発注フォーム候補から消えた。発注伝票で使用済みの検証用取引先（`#IP-1013`）は同じ操作後も削除されず、すべて/アーカイブ両タブと発注フォーム候補に残った。
- 決済方法の保存挙動を追加検証。検証用決済方法で、ゲートウェイ任意文字列、`支払い待ちでも注文を出荷する` ON、`代引き` ONを保存できた。一覧にはゲートウェイ列が表示されるが、編集画面にはゲートウェイ欄がなく、後から管理画面で変更する導線は確認できなかった。編集画面の `アーカイブ` は押せるが、今回の確認ではダイアログや本文変化なし。
- ロケーション作成フォームの場所種別を再確認。`/admin/settings/locations/create` の `場所種別` selectは `倉庫` / `店舗` の2種のみで、内部値は `WAREHOUSE` / `RETAIL`、初期選択は `倉庫`。同画面の `ポイント利用種別` selectは `値引き` / `金種` の2種のみで、内部値は `DISCOUNT` / `PAYMENT`、初期選択は `値引き`。
- ロケーションの `ポイント利用種別=金種` 保存を追加検証。検証用ロケーション `PTPAY28061656` で `金種` を選択して保存でき、詳細画面でも `金種` が選択状態として残ることを確認。検証用ロケーションは詳細画面からアーカイブ済み。実注文・ポイント利用時の `値引き` との差分は未確認。
- 通知用メールアドレスの `名前` 欄の説明を整理。追加・削除・一覧表示は2026-06-27証跡で確定済みのため、`名前` は一覧の管理名として記載し、実通知メール上の宛先表示名・送信タイミングは実通知発生待ちへ分離した。
- 組織ID/テナントIDの表示とコピー導線を追加検証。設定トップの組織IDは `{uuid}_Organization` 形式の読み取り専用inputとして表示され、右隣のコピーボタンでクリップボードへコピーされる。テナント詳細のテナントIDは `{uuid}_Tenant` 形式で表示されるが、編集input/selectやコピー導線は確認できなかった。API/外部連携での具体的な渡し方は画面UIだけでは未確定。
- 発注起点の入荷指示作成後・入荷完了前の発注伝票キャンセルを追加検証。新規 `#IP-1009` を作成し、発注済み後に `#II-1027` を作成、入荷完了前に `その他の操作 > キャンセルする` を実行できた。`#IP-1009` は `キャンセル済み`、`#II-1027` は入荷管理一覧で `種別: 発注 / 警告 入荷キャンセル`、詳細は入荷予定0/入荷済み0になった。入荷完了済み `#IP-1007` は引き続きキャンセルdisabled。
- 在庫依頼の終了後再開・再利用可否を追加検証。`482787-30-ONE` / `ユニクロEC` / 希望数1で確認待ち依頼を作成し、`その他の操作 > 在庫依頼をクローズする` から `実行する` で終了にできた。再読み込み後は詳細ステータスが `終了`、`在庫を引当てる` は非表示、`在庫依頼をクローズする` はdisabledで、再開・再利用の操作は表示されなかった。検証中に作成した確認待ち依頼2件はいずれもクローズ済み。
- 同じ在庫依頼証跡で、管理画面から手動作成した在庫依頼は作成直後・クローズ後とも詳細の `チャネル` が `web-admin` になることを確認した。注文・販売チャネル・リテールポータル等の別経路のチャネル値は接続環境待ち。
- 出荷実績登録時の配送キャリア・追跡コード入力後の表示を追加検証。既存未完了の `#IM-1026` から関連 `#IO-1026` を開き、配送キャリア `FAQCarrier-20260628031813` と追跡コード `FAQTRACK-20260628031813` を入力して出荷完了できた。最終再ロード後の出荷指示詳細は `成功 完了 / 出荷完了`、出荷予定1/出荷済み1、出荷履歴は日時のみで、入力した配送キャリア・追跡コードは詳細・「すべて」一覧・「出荷完了」一覧には表示されなかった。関連 `#II-1028` は「入荷指示を一括受領で完了する」（`/complete`）が空状態で完了せず、「入荷実績を登録する」（`/receive`）で受領数1を登録すると入荷完了し、親 `#IM-1026` は `成功 完了 / 入荷完了`。
- 移動伝票の複数回入荷・累計過入荷を追加検証。`#IM-1027` / `#IO-1027` / `#II-1029` で、出荷完了後に入荷指示へ1回目1個、2回目2個を登録し、予定2に対して入荷済み3個の累計過入荷にできた。累計過入荷でも `#II-1029` と親 `#IM-1027` は自動完了せず `入荷待ち` のまま。入荷指示詳細の「完了に更新する」→確認ダイアログ「完了にする」で手動完了すると、入荷指示と親移動伝票が `成功 完了 / 入荷完了` になった。
- 取置伝票の多明細保存・処理済み化を追加検証。`#IR-1011` で、商品参照を2回開いて6件ずつ選択し、作成フォーム上で12明細・数量入力12件を確認して保存できた。保存0.836秒、処理済み化0.891秒で完了。これは管理画面操作の多明細確認であり、負荷試験ではない。あわせて `#IR-1011` / `#IR-1010` / `#IR-1009` / `#IR-1007` の詳細を各伝票ごとの新規ブラウザコンテキストで直接開き強制リロードしても、商品明細テーブルに同じ34行が表示されたため、取置伝票詳細の商品明細は伝票固有明細として案内しない。
- PDFエクスポート対象伝票を横断確認。移動/調整/取置/出荷/入荷/発注の各詳細（`#IM-1027` / `#IA-1013` / `#IR-1011` / `#IO-1027` / `#II-1029` / `#IP-1010`）にはPDF/印刷/納品書生成ボタンなし。`/admin/pdf_export` には納品書カテゴリがあり、カテゴリページ `/admin/pdf_export/pdf_export_operation_packing_slips` は空状態、`/create` は存在しない画面。PDF実ファイル生成はヤマトB2条件指定フォームの「同梱する納品書PDF」表示までで、メールリンク/実ファイルは接続環境待ちとして残す。
- ヤマトB2・納品書PDF履歴を追加再確認。`/admin/csv_export/csv_export_operation_inventory_outbound_order_yamato_b2_clouds` は空状態で、CSV/PDFのダウンロードリンクは表示されなかった。`/admin/inventory_outbound_orders/export/yamato_b2_cloud` には「同梱する納品書PDF」とメール/ダウンロードリンク通知文言が表示される。`/admin/pdf_export/pdf_export_operation_packing_slips` は空状態、`/create` は存在しない画面。
- 検索・絞り込みを主要8画面で横断確認。管理メンバーは初期表示で `キーワードで検索する` と `絞り込みを追加: メールアドレス` があり、`河野` / `陽介` / `kohno` は各1件、`特権管理者` / `河野 kohno` は0件。商品/在庫/ロケーション/出荷/入荷は `F` キーで検索パネルが開き、それぞれ商品コード/SKUコード/場所コード/`#`付き管理番号で検索する。カタログ/移動伝票は今回の方法では検索パネルなし。
- 注文管理/返品の空状態案内の矛盾を修正。2026-06-28証跡に合わせ、注文一覧・下書き・返品はいずれもデータ0件の現行空状態では検索/絞り込みUIが表示されないこと、下書きの `注文を作成する` はhrefなしdisabled相当であること、返品一覧には新規作成ボタンがなく `/admin/order_returns/create` は存在しない画面であることを機能別/Notionへ反映した。
- 売上実績の空状態案内を修正。空状態の文言 `絞り込みや検索ワードを変更してみてください` だけでは検索UIの存在確認にならないため、データ0件の現行画面では具体的な検索ボックス・絞り込み条件は未確認と明記した。
- 内部GraphQLで検証注文 `FAQ-ORDER-BYPASS-20260628100919` を作成し、注文一覧のデータあり列、注文詳細、顧客詳細反映、売上実績の正明細を確認した。`inventoryBehavior=BYPASS` / `requiresFulfillment=false` の注文では、在庫変動は注文詳細で「無視されます」と表示される。
- 追加で `FAQ-REMAINING-DECREMENT-20260628135953` を `inventoryBehavior=DECREMENT` / `requiresFulfillment=true` で作成し、対象倉庫SKUの `販売可能 1→0`、`引当済み 0→1`、`手持ち 1→1`、`売可 1→0` と、`ORDER` 出荷指示 `#IO-1034` の生成を確認した。外部チャネル流入注文で同じ条件になるかは連携待ち。
- 返品 `FAQ-ORDER-BYPASS-20260628100919-R1` で、返品一覧のデータあり列、返品単体URLなし、注文詳細内での `返送待ち` → `返品受取済み` → `返品処理済み`、返品完了後の注文合計0円化、売上実績の負明細を確認した。さらに交換明細付き返品 `FAQ-REMAINING-EXCHANGE-20260628140117-R1` は、作成直後/受取後には交換出荷指示なし、返品完了後に `ORDER_EXCHANGE` 出荷指示 `#IO-1040` が生成されることを確認した。Recustomer/外部チャネル経由の返品反映、実ポイント減算は未確認。
- 注文/返品/売上実績のデータあり状態で検索・絞り込みUIを追加確認した。注文一覧は `注文番号で検索する`、`絞り込みを追加`、`名前を付けて保存` が表示され、条件は `テナント` / `タグ付けされている` / `タグ付けされていません` / `チャネル`。返品一覧は `絞り込みを追加` が表示され、条件は `キャンセル` / `交換出荷`。売上実績一覧はデータあり状態でも検索ボックス・絞り込み追加・保存ビューなし。
- Webhook実作成を追加検証。既存テストアプリ `TEST_FAQ_20260624_APP_113636` で、イベント `注文の作成`、検証用 `.invalid` エンドポイントを保存できた。保存後はWebhookセクションにイベント名・エンドポイント・形式 `JSON` が表示され、トースト `Webhookを追加しました` が出た。作成直後の画面上ではWebhook単位の編集/削除/停止導線、行チェックボックス、送信成功/失敗ステータスは確認できなかった。削除導線がないため検証Webhookは残存。
- Webhookイベント選択肢を追加検証。既存テストアプリのWebhook作成ダイアログを保存せずに再確認し、イベントselectの選択可能値は `注文の作成` / `注文の更新` / `在庫の更新` の3種のみだった。各候補をselectで選択できることも確認済み。
- APIアプリ詳細/一覧の削除・失効・再発行導線を再確認。既存テストアプリ `TEST_FAQ_20260624_APP_113636` の詳細画面、一覧カード、Webhook作成ダイアログ、リクエストログを開き、可視操作にアプリ削除・トークン失効・アクセストークン/シークレット再発行、Webhook編集/削除/停止、送信成功/失敗ステータスが表示されないことを確認した。Storefrontの `トークンを発行する` は表示のみ確認し、秘密値を増やさないため未実行。
- API Playground導線を追加再確認。既存テストアプリ `TEST_FAQ_20260624_APP_113636` の `Playgroundを開く` は、stagingのアプリ詳細からでも `https://sq.stackservice.com/api/admin/v1/playground` を開き、GraphQL Playgroundページ自体は表示できた。URLクエリにトークン/アプリIDらしき値は付与されず、確認範囲ではstagingアプリの認証情報が自動注入される挙動は確認できなかった。秘密値露出を避けるためクエリ実行・スキーマ取得は未実施。
- ヤマトB2クラウド出荷実績CSVの実保存を追加検証。検証用 `#IM-1032` / `#IO-1032` / `#II-1034` を作成し、`お客様管理番号=#IO-1032` のCSVを `/admin/csv_import/csv_import_operation_fulfillment_by_yamato_b2_clouds/create` から保存すると、CSVインポート詳細 `/admin/csv_import/csv_import_operation_fulfillment_by_yamato_b2_clouds/{id}_CSVImportOperationFulfillmentByYamatoB2Cloud` が作成された。再確認時も `実行ステータス 未実行`、`検証成功 0件`、`検証失敗 0件` のままで、実行・出荷完了遷移は未確認。検証用出荷/入荷/移動は後処理で完了済み。
- 店舗受取対象SKUの選択挙動を追加検証。`TEST_E2E_20260622_GU_1830_NAVY_M` だけをチェックし、同候補一覧の `TEST_E2E_20260622_GU_1845_NAVY_M` は未チェックで保存したところ、チェックしたSKUだけが一覧に追加され、未チェックSKUは追加されなかった。追加ダイアログ内にカタログ選択項目はなく、検証で追加したSKUは削除済み。
- 店舗受取ルール作成フォームのロケーション候補と `店舗受け取りを有効にする` の関係を追加検証。`店舗受取を有効にする=OFF` の `FLAGOFF01` も候補に表示され、ONの `TEST_E2E_20260622_STORE_1740` も表示、倉庫 `W0001` は表示されなかった。管理画面の候補表示は場所種別=店舗で絞られ、店舗受取ON/OFFでは絞り込まれない。
- リテールポータル連携詳細のユーザー一覧・追加/削除導線を追加検証。既存連携 `ユニクロ - 銀座店` で、詳細画面に状態 `成功` / `有効`、追加済みユーザー一覧（名前/メールアドレス）、`ユーザーを追加する` ボタンを確認した。検証用管理メンバー `sq-faq-retail-20260627202741@example.com` を追加ダイアログのメールアドレス検索で絞り込み、追加確定後に一覧へ表示されること、行選択後の削除確認ダイアログ `リテールポータルからユーザーを削除しますか？` を経て削除確定後に一覧から消えることを確認した。検証用管理メンバーは確認後に組織から除外済み。実際のリテールポータルログイン可否・権限範囲は未確認。
- DHL出荷実績CSVインポートのフォーマット表示を追加検証。DHL一覧には `新規インポート` はあるがテンプレート表記はなく、作成フォームは `CSVファイル` アップロード欄と `保存する` のみ。ファイル未選択保存では `ファイルを選択してください` が表示された。2026-06-28時点の管理画面UIにはDHLの列数・必須列・列名説明は表示されないため、DHLフォーマット詳細は実CSVサンプルまたは開発元確認待ちとして扱う。
- 確保済み一覧 `/admin/inventory_allocation_request_confirmations` のフィルタUIを追加再確認。2026-06-28時点では、見出し `確保済み`、列 `商品 / SKU / 確保数 / 移動元 / 移動先 / 確保日時`、ボタン `移動伝票を作成する` は表示されるが、キーワード検索・絞り込み追加・ロケーションフィルタは表示されない。従来の「ロケーションフィルタで対象行だけに絞る」案内は撤回し、対象外行がある状態では一括作成を実行しない案内へ修正した。
- ヤマトB2条件指定エクスポートのステータス変更ONを追加検証。出荷待ち/作業中タブが空であることを確認後、検証用移動伝票 `#IM-1029`（関連出荷指示 `#IO-1029`、入荷指示 `#II-1031`）を作成し、開始 `2026-06-01T00:00` / 終了 `2026-06-28T06:59` / 出荷作業ステータス `出荷待ち` / ステータス変更ONで実行した。フォームは送信でき、トースト `ヤマトB2クラウドのエクスポートを開始しました` が表示されたが、`#IO-1029` は `出荷作業中` に移らず `出荷待ち` タブに残った。検証用出荷指示は出荷実績登録で出荷完了、関連入荷指示は入荷完了まで後処理済み。
- CRMの注文ポイント付与ルールで会員ランク倍率と商品倍率を追加検証。既存 `TEST_FAQ_注文ポイント付与ルール` の会員ランク倍率タブで `Bronze` を `2倍` として保存でき、一覧に `Bronze` / `2倍` と表示され、行選択後の `会員ランク倍率を削除しますか？` ダイアログで削除できた。商品倍率タブでは `TEST_20260622_OPTION` を倍率0で保存でき、一覧に `0倍` と表示され、行選択後の `商品倍率を削除しますか？` ダイアログで削除できた。あわせて利用外商品画面で同じ商品を追加・一覧反映・削除できることを確認し、商品倍率0倍は付与側、利用外商品は利用側の別設定として本文へ反映した。検証行はいずれも削除済み。
- CRMの会員ランク算出ルール詳細と編集フォームを追加検証。既存 `TEST_FAQ_会員ランク算出ルール` の詳細には `算出タイミング` / `リアルタイムに算出` が表示されるが、`/update` 編集フォームには算出タイミングやリアルタイムを変更する入力欄はない。編集フォームの算出方法ラジオは `購入金額` がcheckedかつdisabled、`獲得ポイント` もdisabled。対象詳細画面にはルール本体の編集/削除ボタンは表示されず、可視リンクにも本体更新URL `/update` はない。詳細の「編集」はランク段階ごとのリンクのみで、直接 `/update` URLは表示可能。
- Recustomer/OmnibusCoreの一覧・作成フォームを追加再確認。Recustomerは一覧見出し `Recustomer`、アクション `アカウントを接続`、作成フォーム `アカウントを接続する`、項目 `ストアID` / `シークレット` まで確認。OmnibusCoreは一覧列 `メーカーコード / テナント / カタログ / ロケーショングループ / 連携日時` と、作成フォームの `商品同期設定` / `在庫設定` / `注文設定`、主要項目（カタログ、ロケーショングループ、在庫予約ルール、販売上限ルール、下書き注文の有効期限日数等）を確認。共通導線の一般知識寄りの説明を実機フォーム項目ベースへ修正した。
- OmnibusCore接続済み詳細の3タブと追加ダイアログを追加検証。既存 `TEST_FAQ_20260624_OmnibusCore_100908` の詳細で `基本設定` / `連携サイト` / `通知メール` の3タブ、基本設定の `出荷指示のステータス` と `アクセストークン` 領域、連携サイト追加ダイアログの `連携サイトの場所コード` / `連携サイト名` / `販売価格` / `ロケーション` / `サイト倉庫ロケーション`、通知メール追加ダイアログの `名前` / `メールアドレス` を確認した。同期時の引当・出荷反映は未確認。
- スマレジ接続済み詳細を追加検証。既存 `TEST_FAQ_20260624_スマレジ_100908` の詳細で `商品連携設定` / `在庫設定` / `取引連携設定` / `外部アクセストークン` / `連携を解除` / `基本情報` を確認し、ラベル `カタログ` / `在庫同期の方向` / `注文による在庫変動を起こさない` / `取引の連携を有効にする` / `プラン` を確認した。タブ/リンクとして `店舗設定` / `商品管理` / `顧客管理` があり、商品管理には `連携済み` / `未連携`、顧客管理には `すべて` の切替が表示された。同期挙動、スマレジ側アプリ導線、連携解除実行は未確認。
- 販売チャネル連携の安全UI確認を追加実行。Shopify未接続コピーと作成フォーム主要ラベル、Recustomer一覧/作成フォーム/空保存エラー、スマレジ作成フォームのアプリインストールバナー、フォーム内に外部インストール手順リンクがないこと、OmnibusCore詳細の `トークンを作成` ボタン表示を確認した。スマレジ `連携を解除` の安全確認では確認ダイアログは表示されず、非GET通信も発生しなかった。OAuth開始、同期実行、連携解除確定は実行していない。
- OmnibusCore詳細の `下書き注文の有効期限日数` 空保存とアクセストークン作成/削除を追加検証。期限日数は空欄で保存ボタンが有効になり、保存後に `1から365の間で入力してください` が表示されたため、元値 `1` に復旧済み。`トークンを作成` は確認ダイアログなしで生成され、マスク済みトークン・作成日時・トークン用 `削除` が表示された。トークン用 `削除` は確認ダイアログ `アクセストークンを削除する` 経由で未設定状態に戻せることを確認した。トークン値は証跡に保存していない。
- OmnibusCore通知メールの重複登録を追加検証。通知メール一覧が空の状態から検証用メールを1件追加でき、同じメールアドレスを2回目に追加しようとしても一覧は1行のままで2行目は増えなかった。GraphQL errorsには `エラーが発生しました。しばらくしてから再度お試しください` が返った。検証用通知メールは削除済み。
- スマレジ詳細の `外部アクセストークン` も追加検証。既存接続済みレコードで `トークンを作成` を押すと非GET通信は発生するが、作成日時・削除ボタンは表示されず、再読み込み後も `外部アクセストークンが設定されていません` のままだった。画面上の明示的なエラー表示・GraphQL errorsはなし。生成成功条件は未確認。
- スマレジ詳細の `解除する` 実ボタンを追加安全確認。従来証跡は見出し `連携を解除` をクリックしていた可能性があったため、実ボタンを非GET遮断付きでクリックしたところ、確認ダイアログ `連携を解除する` が開き、ボタン `閉じる` / `キャンセル` / `解除する` を確認した。確定側の `解除する` は未実行。
- カタログの機能別/作業別ページを2026-06-28証跡に合わせて更新。SKU一覧は現行UIでは詳細内 `?tab=sku` で列は `商品` / `品番(SKU)`、旧 `/catalog_product_variants` URLは存在しない。自動追加ルールはOR条件、カンマや `*` は通常文字列。ルール作成前からある一致商品は作成直後・10秒後・60秒後には追加されず、商品詳細で保存し直すと追加されることを追加確認した。
- 商品バリエーション作成フォーム、翻訳、採寸、販売上限、ヤマトB2条件指定エクスポートを追加再確認。`在庫を追跡する` と `在庫切れの場合でも販売を続ける` は初期OFF、`配送を必須にする` は初期ON。翻訳トップ/一覧/作成/詳細には手動実行/再実行/翻訳/生成ボタンなし。採寸ルール詳細はreadonlyで、商品詳細2件（`486125` / `482787`）側に採寸・測定・measurement系の入力/選択項目なし。販売上限はチャネル候補なしで空保存時にルール名/チャネルの両エラー。ヤマトB2は決済方法3択、ステータス変更チェック初期OFF。
- 翻訳ルールの同一言語作成と自動生成チェック保存状態を追加検証。英語の検証用翻訳ルールを複数作成でき、一覧に同一言語の複数行が並ぶことを確認した。`翻訳データを自動で作成する` はオン/オフどちらも保存後の詳細画面で保持された。翻訳トップ・ルール一覧・作成フォーム・詳細画面に手動実行/再実行/翻訳/生成ボタンはなく、検証用翻訳ルール2件は削除済み。2026-06-28の追加再確認でも同ボタン一致数は0。実際の翻訳生成タイミング・反映結果・同一言語ルール間の優先順位は生成済みデータまたは連携環境待ち。
- 会計・売上実績・分析を追加再確認。`/admin/sale_change_line_items/create` は存在しない画面、売上実績一覧は空状態で検索/絞り込みヒントあり。売上実績CSVの注文軸/明細軸一覧はどちらも列 `作成日 / テナント / 対象期間 / ステータス / ダウンロード`、空保存エラーは `テナントを選択してください` / `開始日時を入力してください` / `終了日時を入力してください`。分析トップ/収益/レポートは本文TODOで、収益のみh1が `売上`。
- 売上実績CSVの注文軸/明細軸をテナント `ユニクロ`、対象期間 `2026年06月28日 00:00` 〜 `2026年06月29日 00:00` で実行し、成功完了後のダウンロードリンクから実CSVを保存した。注文軸は `ID / イベント / 作成元 / ... / 売上 / 税額 ...`、明細軸は `SaleChangeID / SaleChangeLineItemID / 処理日時 / SKU / 数量 / ...` のヘッダーで、検証注文・返品のデータ行を確認した。テナント欄はnative selectの単一選択で、フォーム上に全テナント指定や複数テナント選択UIはない。
- 注文・返品の空状態を追加再確認。`/admin/orders` は空状態で検索/絞り込みUIなし、`/admin/orders/create` は予期せぬエラー。`/admin/draft_orders` は空状態で `注文を作成する` が `href` なし・`Polaris-Button--disabled`、クリック不可/URL変化なし、`/admin/draft_orders/create` は予期せぬエラー。`/admin/order_returns` は空状態で検索/絞り込み/キャンセル/交換出荷UIなし、`/admin/order_returns/create` は存在しない画面。
- 会社担当者の追加・削除を追加検証。会社詳細のダイアログから担当者を作成でき、テーブルに名前/メールアドレスで表示されるが詳細リンクはない。行選択後に `担当者を削除` が表示され、確認ダイアログから削除できた。検証用担当者は削除済み。
- 商品CSVインポートの実行を追加検証。現行テンプレート列（`option1_type` 含む）で検証用商品1件をアップロードし、`検証成功 1個の商品`、確認ダイアログ `1件の商品を登録します。この操作は巻き戻すことができません。`、実行ステータス `成功 完了`、商品一覧/詳細反映を確認した。検証用商品は商品詳細から削除し、削除後に `該当するProductが見つかりませんでした。` を確認した。
- カタログ商品CSVインポートの `NEW` / `DELETE` 実行を追加検証。テンプレート列は `product_code,command` で、`NEW` は対象商品をカタログへ追加し、`DELETE` はカタログから外す。どちらも `検証成功 1件の商品`、確認ダイアログ `1件のカタログ商品を登録します。この操作は巻き戻すことができません。`、実行ステータス `成功 完了` を確認した。NEW後はカタログ詳細の商品一覧に検証商品が表示され、DELETE後は消えた。検証用の商品・カタログは削除済み。

## 未完了

「全事項の完全実機検証」はまだ完了していない。2026-06-28の最終再集計では抽出済みの未確認系403行のうち、`verified-by-route-crawl` 25件はURL巡回/再確認証跡で確認済み、`verified-by-live-execution` 14件は実行/再確認証跡で確認済み、`not-a-claim` 39件は見出しやスクリーンショット参照など検証対象の主張ではないため残件外に分離した。実際の未完了バックログは325行で、内訳は `blocked-by-environment` 264件、`needs-seeded-business-data` 61件。`needs-specific-test` は0件。現行staging管理画面で追加単体検証に回すべきものは消化済みで、残りは外部接続環境またはポイント/顧客/実売上などの業務データが必要なものに整理済み。
