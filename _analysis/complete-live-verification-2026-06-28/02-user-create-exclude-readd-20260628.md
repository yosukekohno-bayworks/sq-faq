# 02 管理メンバー作成・除外・再追加 実機確認 2026-06-28

## 対象

- `/admin/settings/users/create`
- `/admin/settings/users`
- 検証用メール: `sq-faq-member-20260627172757@example.com`

## 結果

- 権限グループ未選択でも管理メンバーを保存できた。
- 保存後は詳細画面へ遷移し、トースト `ユーザーを追加しました` が表示された。
- 未選択で作成したユーザーの権限欄は `未設定`、適用されている権限一覧は `0件`。
- 一覧検索では対象メールが表示され、権限グループ列は `未設定`。
- 詳細画面の `組織から除外する` を押すと確認ダイアログ `ユーザーを組織から除外しますか？` が表示される。
- ダイアログ本文は `ユーザーを組織から除外します。この処理は巻き戻すことができません。`、確定ボタンは `除外する`。
- 除外後は管理メンバー一覧へ戻り、トースト `ユーザーを組織から除外しました` が表示された。
- 除外後に同じメールアドレスで再追加できた。再追加時は既存と同じユーザー詳細URLに遷移し、選択した権限グループ `TEST_権限検証_20260620` が反映され、適用権限一覧は `1件`。
- 再追加した検証ユーザーも最後に除外済み。最終検索では `アイテムが見つかりませんでした`。

## 証跡

- JSON: `02-user-create-exclude-readd-20260628.json`
- 実行スクリプト: `user_create_exclude_readd_20260628.py`
