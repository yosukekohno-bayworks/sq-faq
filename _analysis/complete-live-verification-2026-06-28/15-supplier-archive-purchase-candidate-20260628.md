# 15 発注・仕入 取引先の一括アーカイブ/削除と発注フォーム候補 2026-06-28

検証用取引先:

- 名前: `TEST_FAQ_ARCHIVE_SUPPLIER_20260628_021648`
- コード: `TEST_SUP_ARCH_20260628_021648`

## 操作

1. `/admin/settings/suppliers/create` で検証用取引先を作成した。
2. `/admin/inventory_purchase_orders/create` の取引先セレクトに表示されることを確認した。
3. `/admin/settings/suppliers` の一覧で対象行を選択した。
4. 一括操作 `アーカイブする` をクリックした。
5. 確認ダイアログの `削除する` をクリックした。
6. 取引先一覧、アーカイブタブ、発注伝票作成フォームの取引先候補を確認した。

## 結果

| 確認項目 | 結果 |
|:--|:--|
| 作成直後の発注フォーム候補 | 表示された |
| 一覧行選択後の一括操作 | `アーカイブする` が表示された |
| 確認ダイアログ見出し | `取引先を削除しますか？` |
| 確認ダイアログ本文 | `選択されている1件の取引先を削除します。以下に当てはまる取引先は削除されません。過去の仕入れで使用された取引先` |
| 確定ボタン | `削除する` |
| 実行後の「すべて」タブ | 対象取引先は消えた |
| 実行後の「アーカイブ」タブ | 対象取引先は表示されなかった |
| 実行後の発注フォーム候補 | 対象取引先は表示されなかった |

## 判断

- 取引先一覧の一括操作ラベルは `アーカイブする` だが、確認ダイアログと確定ボタンは削除操作の文言になっている。
- 未使用の取引先は、この操作で「アーカイブに残る」のではなく、一覧・アーカイブタブ・発注フォーム候補から消える。
- 過去の仕入れで使用された取引先はダイアログ文言上「削除されません」。使用済み取引先がアーカイブタブに残るのか、単に削除失敗になるのかは未検証。

## 証跡

- JSON: `_analysis/complete-live-verification-2026-06-28/15-supplier-archive-purchase-candidate-20260628.json`
