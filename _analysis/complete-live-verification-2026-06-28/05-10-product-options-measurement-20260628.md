# 05/10 商品詳細: オプション軸追加・採寸ルール紐づけ確認

日時: 2026-06-28
対象URL: `https://www.sqstackstaging.com/admin/products/2c32fb97-1f83-5cae-b20c-3d83046800d0_Product`
対象商品: `オーバーサイズスウェットシャツ` / 商品コード `486125`

## 確認結果

- 商品詳細のバリエーションセクションには `バリエーションを追加する` リンクがある。
- 既存軸 `カラー` / `サイズ` を展開すると、各軸に `オプション名`、`オプション値`、`コード`、`オプション値を追加` が表示される。
- 画面全体のDOM/本文上に `別のオプションを追加する` は表示されない。
- 画面全体のDOM/本文上に `採寸`、`測定`、`measurement`、`product_measurement` を含む項目は表示されない。
- `保存する` ボタンは未変更状態ではdisabled。今回は表示確認のみで保存していない。

## 証跡

- スクリーンショット: `product-detail-options-measurement-20260628.png`
- browser-useのDOM抽出結果:
  - `contains.addAxis = false`
  - `contains.addValue = true`
  - `contains.measurement = false`
  - option関連コントロール: `バリエーションを追加する`、`オプション値を追加`、`すべてのバリエーションを表示`

## 反映判断

- `05-商品・SKU.md`: 既存商品で後から新しいオプション軸を追加する管理画面導線は確認できない。既存軸の値追加・編集導線は確認済み。
- `10-採寸.md`: 商品詳細側にも採寸ルール紐づけUIは確認できない。管理画面UIでは紐づけ導線なしとして扱い、API/外部連携での適用可能性は別残課題に分離する。
