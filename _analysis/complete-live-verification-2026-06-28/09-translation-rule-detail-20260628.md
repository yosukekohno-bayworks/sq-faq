# 09 翻訳ルール詳細 実機確認 2026-06-28

## 対象

- URL: `/admin/settings/translation/translation_rules/a2598ab4-5847-5562-9bfb-1494e5198c1b_TranslationRule`
- ルール名: `TEST_FAQ_翻訳ルール_英語`

## 結果

- 詳細画面の言語 `select` は `disabled=true` で保存後変更不可。
- `翻訳データを自動で作成する` チェックボックスは表示される。
- `商品 / 商品オプション / 商品オプション値 / メタフィールド` の各セクションに `親プロンプト` と `カスタムプロンプト` がある。
- 親プロンプトの textarea は `readonly=true`。
- カスタムプロンプトの textarea は `readonly=false`。
- 詳細画面内に `実行`、`再実行`、`翻訳する`、`生成` といった手動実行ボタン/文言は見当たらない。
- `保存する` ボタンは存在するが、未変更状態では `aria-disabled=true`。

## 残る未確認

実際に翻訳データが生成されるタイミング、生成結果、チャネル反映は未確認。
