# 確保済み一覧のフィルタ再確認 2026-06-28

対象URL: `/admin/inventory_allocation_request_confirmations`

## 結果

- 見出しは `確保済み`。
- 一覧列は `商品 / SKU / 確保数 / 移動元 / 移動先 / 確保日時`。
- 表示ボタンは `移動伝票を作成する`、ビュー切替系のみ。
- 入力要素は行選択チェックボックスのみ。
- `絞り込み`、`キーワード`、`検索`、`フィルタ`、`ロケーション` のフィルタUIは表示されない。

## 結論

過去資料の「ロケーションフィルタで対象行だけに絞る」という手順は、2026-06-28時点の実機UIでは再現できない。確保済み一覧は一括作成ボタンのため、対象外行がある状態で実行しないよう案内する。
