# スマレジ連携 実機再確認 2026-07-02

## 対象

- 環境: `https://www.sqstackstaging.com`
- 画面: `/admin/smaregi_integrations`
- 確認レコード: `TEST-SQ`
- 実行しなかった操作: 店舗連携の保存、外部アクセストークン削除、連携解除確定、商品/在庫/顧客の同期実行

## 一覧

`/admin/smaregi_integrations` には2件表示された。

| 名前 | テナント | 連携日時 |
|:--|:--|:--|
| TEST-SQ | ユニクロ | 2026年06月27日 18:46 |
| TEST_FAQ_20260624_スマレジ_100908 | ユニクロ | 2026年06月24日 10:10 |

## TEST-SQ 基本設定

| 項目 | 結果 |
|:--|:--|
| 商品連携設定 | カタログは未選択 |
| 在庫同期の方向 | `SQからスマレジへ在庫を同期する` |
| 注文による在庫変動を起こさない | オフ |
| 取引の連携を有効にする | オン |
| 外部アクセストークン | マスク済みトークン、作成日時、トークン用 `削除` が表示。トークン設定済み状態でも `トークンを作成` ボタンは残る |
| プラン | リテールビジネス |
| 保存する | 変更なしのためdisabled |

## 店舗設定

- 一覧は0件。
- `追加する` で `店舗連携を追加する` ダイアログが開いた。
- 必須項目は `スマレジ店舗*` と `店舗*`。
- `スマレジ店舗*` の選択ダイアログでは、列 `店舗名` の候補として `あなたのお店` が1件表示された。
- `店舗*` の選択ダイアログは `ロケーションを選択する`。検索欄は `場所コードで検索する`、列は `名前` / `場所コード`。`ユニクロ - 銀座店` など複数候補が表示された。
- 保存は実行していない。

## 商品管理

- `連携済み` / `未連携` の切替あり。
- `連携済み` は0件。
- `未連携` も0件。
- `SKU一覧` はdisabled。
- 検索欄は `商品コードで検索する`。

## 顧客管理

- `すべて` タブのみ。
- 一覧は0件。
- 検索欄は `顧客IDで検索する`。

## 判断

- 2026-06-28時点で未確認だった「スマレジ外部アクセストークンが設定済みの画面表示」は確認できた。
- 店舗連携は、スマレジ店舗候補とSQ側ロケーション候補の取得までは確認できた。
- ただし、店舗連携保存、商品/顧客/在庫/注文の実同期、スマレジ側への外部反映、同期頻度、エラー時挙動は未確認のまま残す。

## スマレジ側POS管理画面の確認

### 対象

- 環境: `https://www1.smaregi.jp/control/`
- 確認日: 2026-07-02
- 操作方針: 読み取り中心。登録・更新・削除・在庫操作・取引作成は未実行

### 到達状態

- スマレジPOS管理画面トップへログイン済みで到達。
- 選択店舗は `あなたのお店`。
- メニューには `商品`、`取引履歴`、`店舗`、`会員`、`在庫`、`受注`、`設定` が表示された。
- ヘッダー内に `SQ Staging` への外部リンクが表示された。

### 店舗

`/control/master/store/` を確認。

| 項目 | 結果 |
|:--|:--|
| 件数 | 1件 |
| 店舗名 | `あなたのお店` |
| 種別 | `販売店` |
| 店舗コード | `1` |

SQ側 `TEST-SQ` の店舗設定ダイアログで取得できたスマレジ店舗候補 `あなたのお店` と一致。

### 商品

`/control/master/product/` を確認。

| 項目 | 結果 |
|:--|:--|
| 件数 | 84件 |
| 一覧列 | `商品ID / 商品コード / 商品名`、`部門名`、`税設定`、`商品単価`、`原価`、`在庫`、`端末表示`、`表示順` |
| 検索 | `商品検索`、詳細条件に商品コード・商品名・部門名・サイズ・カラー・グループコード・品番・タグ等 |

代表商品詳細（`/control/master/product/detail.html?...`）では、商品コード、商品名、商品カナ、部門、税区分、税設定、商品単価、会員価格、原価、サイズ、カラー、説明、タグ、グループコード、売上区分、在庫管理区分、店舗別在庫を確認。店舗別在庫は `あなたのお店` の在庫数0。

SQ側 `TEST-SQ` の商品管理タブは `連携済み` / `未連携` とも0件のため、スマレジ側84商品がSQ連携で作成されたものとは断定しない。

### 取引履歴

`/control/transaction/` を確認。

| 項目 | 結果 |
|:--|:--|
| 検索期間 | 2026/06/01〜2026/07/31 |
| 件数 | 0件 |
| 主な列 | `取引区分`、`取引ID / 取引日時`、`単品`、`小計`、`数量(返品)`、`合計`、`取引店`、`端末`、`スタッフ`、`会員`、`締め日` |

### 会員

`/control/master/customer/` を確認。

| 項目 | 結果 |
|:--|:--|
| 件数 | 0件 |
| 検索 | 会員検索、会員コード、メールアドレス、氏名、カナ、住所、電話番号、ステータス等 |

### アプリ設定・アプリマーケット

`/control/configuration/appSetting/` を確認。

| 画面 | 結果 |
|:--|:--|
| アプリ設定 | 0件。`SQ` の設定行は表示されない |
| 設定メニュー | `スマレジAPI設定`、`Square連携`、`外部会員連携` 等の導線あり |
| スマレジ・ストア | 試用期間中のため使用不可エラー |
| アプリマーケット | `accounts.smaregi.jp/login?client_id=app-market` の追加ログイン画面へ遷移 |

### 在庫・受注の別ログイン

| 導線 | 結果 |
|:--|:--|
| 在庫一覧 / 在庫変動履歴 | `accounts.smaregi.jp/login?client_id=inventory` の追加ログイン画面へ遷移 |
| 受注管理 / 受注検索 | `accounts.smaregi.jp/login?client_id=order-shipment` の追加ログイン画面へ遷移 |

POS本体にはログインできているが、在庫管理・受注管理・アプリマーケットは別クライアントとして追加ログインが必要。
