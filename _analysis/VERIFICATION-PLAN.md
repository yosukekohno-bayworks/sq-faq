# 資料完璧化 検証計画（2026-06-20〜）

## 目的
明日ユーザーが1日でSQを完全理解できるよう、実機で徹底検証し、FAQ/ガイド/正本を細かく更新する。

## 検証方針
- **書き込みOK**（ユーザー許可）。新規SKU/ステータス/伝票を生成して実挙動を確認する。
- ただし**削除系・課金は除く**。
- 外部連携/実データ前提の項目（Shopify同期、実注文、実顧客等）は環境制約で確認不可→「未確認」を維持（誤った断定を避ける）。
- ステータスが動く操作は、ステータス表示だけでなく、保存/実行後の遷移先、一覧タブでの移動先、関連レコード、件数・在庫区分の変化、削除/反映が即時かリロード後かまで記録する。
- 検証結果は該当FAQ本文・正本(VERIFIED-FACTS-ONLY等)・データ事典へ反映。

## A. 書き込み検証で潰す項目（管理画面で完結・優先順位順）
1. **取り寄せ販売の新規通し**: 在庫依頼→引当→移動伝票→出荷→入荷の全在庫動き・ステータス遷移（積送中/入荷予定の動き含む）
2. **取置伝票の複数SKU明細**: 再現条件は確定済み。2026-06-28 `#IR-1011` で作成フォーム12明細・数量入力12件の保存と処理済み化まで確認
3. **調整伝票**: 理由4種（廃棄/紛失/在庫差異/その他）×増減の在庫動き
4. **移動伝票**: 部分入荷/過入荷/同一ロケの在庫動きの再確認
5. **発注伝票→入荷連動**: 到着ロケーション指定の要否・非同期か（連動しない事象の深掘り）
6. **ポイント付与ルール詳細**: 会員ランク倍率/商品倍率の追加フォーム（前回ボタン押下で遷移せず）
   - **2026-06-20実機確認（訂正）**: ポイントルール詳細は3タブ（基本設定/会員ランク倍率/商品倍率）。**「追加する」ボタンは機能する**（前回「遷移しない」は誤り）。会員ランク倍率タブで「追加する」→モーダル開き、会員ランク（Bronze等、既設定は選択不可）＋倍率（数値、単位「倍」）を入力して追加可能。基本設定タブは購入金額円ごとのポイント、ライフサイクル（付与まで日数・有効日数）、会員ランク算出ルール、キャンペーン優先度。
7. **ディスカウント詳細タブ**: 顧客管理/店舗管理/商品管理の操作
   - **2026-06-20実機確認、2026-06-28更新**: ディスカウント詳細は5タブ（基本情報/利用履歴/顧客管理/店舗管理/商品管理）。基本情報=タイトル・割引コード・説明文・割引設定（割引方法=割引率・最高購入額）・適用条件（割引要件=最低購入額/対象商品=すべてor個別）・利用制限・有効期間・Shopify連携（未連携/連携する）。店舗管理/顧客管理/商品管理タブは「追加する」ボタンで対象を個別指定。顧客管理は同一テナントの購入顧客ならメール検索・追加・削除可、顧客0件またはテナント不一致では候補0件。
8. **販売閾値/上限**: 設定詳細・SKU別閾値の動き
   - **2026-06-20実機確認**: 販売上限(`/admin/inventory_sale_limit_rules`)は空（0件）。作成画面(`/create`)の項目=販売上限ルール名＋チャネル（最小構成）。販売閾値(`/admin/inventory_threshold_rules`)はconstraints.md 29行（SKU別閾値削除後の即時表示残り=リロードで解消）が2026-06-19確認済み。
9. **商品作成**: 新規SKU→バリエーション→在庫設定の通し
   - **2026-06-20実機確認（構造）**: 商品作成画面(`/admin/products/create`)の項目=商品コード（半角英数字・ハイフン・アンダースコアのみ）・商品名・説明文・メディア・バリエーション（オプション種別=サイズ/カラー/その他 ※作成時のみ設定可=constraints.md 12行、オプション値・コード）・ステータス（下書き/公開/非公開）・商品分類（商品タイプ/製造元/ブランド/タグ）。商品コード+商品名入力まで確認（バリエーション設定〜保存〜在庫は時間制約で次回）。
   - **2026-06-20追記（作成試行）**: 商品コード(FAQTEST20260620)+商品名入力、オプション値(S,M)入力まで成功。ただし保存時エラー「オプション名が空のフィールドがあります」。バリエーションのオプション名（種別select＋オプション名テキスト）はReact制御コンポーネントで、nativeInputValueSetterではstateが同期せず保存に至らず。UI構造の事実として記録（SKUモーダルと同様の操作の難しさ）。**商品作成の正確な手順は商品を作成する.md（FAQ本文）の既存記述を正本とする**。

## B. 確認不可（環境制約）→「未確認」維持
- 外部連携の実同期（Shopify/スマレジ/OmnibusCore/リテールポータル/ロジザード/Recustomer）
- 実注文/顧客（0件・作成不可）の詳細画面
- 売上実績の自動集計
- ポイント/会員ランクの実適用（実注文必要）
- 販売ルールのチャネル適用時挙動
- 破損/検点/予備への振替（管理画面UIなし、WMS/API）
- メール通知、別権限ログイン

## 進捗

### A-1 取り寄せ販売の新規通し（2026-06-20 開始）
- [x] **ステップ1 在庫依頼作成**: 商品=バギーカーブジーンズ 487973-64-36、希望数=2、移動先=ユニクロ-銀座店。ID=ccb6f774-ba62-5fc6-bd6a-6f60bea77158。詳細画面遷移確認
- [~] ステップ2 引当: **新事実発見** — リクエスト先(引当元)未指定だと、引当ダイアログの「引当元ロケーション」selectの選択肢が空（選択不可）。エラー「引当元ロケーションを選択してください」。つまり**リクエスト先(引当元)は実質必須**（VERIFIED-FACTS/在庫依頼.md/データ事典②の「任意」記述は要修正→「引当には引当元ロケーション指定が実質必須」）。ダイアログはキャンセル。→ 在庫依頼をクローズし、リクエスト先指定で再作成して引当をやり直す
- [x] **A-1 新規通し: 技術的限界で断念（2026-06-20）** — 商品選択モーダルの仮想スクロール＋SKU検索不具合で新規通しが困難。ただし**新事実2点を発見**:
  - ① **リクエスト先(引当元)は実質必須**: 未指定だと引当ダイアログの「引当元ロケーション」select選択肢が空（エラー「引当元ロケーションを選択してください」）。VERIFIED-FACTS/在庫依頼.md/データ事典②の「任意」記述は要修正。
  - ②（**訂正・撤回・実機再証明 2026-06-20**）商品バリエーション選択モーダルのSKUコード検索は**機能する**。pressSequentially入力後に **Enter押下で検索が発火** し、「4861」→ 表示45行すべて4861xx（486125/486102）、**4861以外0件**に絞り込み確認。私の「効かない」は Enter押下しなかったため検索発火せず（操作ミス）。手動入力では発火する。
  - **教訓**: PlaywrightでSQの検索ボックスを操作する場合、入力後にEnter/blurしないと検索が発火しない。以降の検証では入力後Enterを押す。
  - 在庫動き（引当→移動伝票→出荷→入荷の販売可能/取置中/積送中/入荷予定の変化）は CODEX-OPS-2026-06-18（#IM-1011等）で実証済みのため、そちらを正本とする。
  - **次アクション**: 新事実①②を在庫依頼.md/VERIFIED-FACTS-ONLY/データ事典②に反映（「リクエスト先=引当には実質必須」「SKU検索の不具合」）

### A-1 取り寄せ通し 再開（2026-06-20 03:23〜）★SKU検索解決で通し可能に
- SKU検索は機能する（pressSequentially入力後 **Enter押下で発火**、ユーザー指摘が正しい）。商品選択モーダルが使えるようになり通し再開。
- [x] **ステップ1 依頼作成**: 成功 ID=`c7e1f0dc-deb7-5613-8a06-16fe67919f95`（商品486125-31-XL オーバーサイズスウェットシャツBEIGE/XL、希望数2、移動先=ユニクロ銀座店、リクエスト先=物流倉庫）。ステータス=確認待ち。依頼先 物流倉庫の販売可能数=1。
- [x] **ステップ2 引当**: 成功（**部分引当 1/2**、物流倉庫在庫1のため全量引当不可）。確保済み在庫セクションに「物流倉庫 1（確保日時03:24）」、依頼先販売可能数表示「成功 確保済 1」、確保済み数量 1/2、残り確保数 1。
- **新観察（要確認）**: 部分引当（1/2）でも依頼ステータスは**「確認待ち」のまま**。「確保済み」ステータス/確保済みタブへ移動する条件（全量引当 or 1個でも可）を次歩で確認。
- [ ] **次歩**: 確保済み一覧(`/admin/inventory_allocation_request_confirmations`)での表示 → 移動伝票作成 → 出荷実績 → 入荷実績で、在庫区分（積送中/入荷予定/取置中/販売可能/手持ち）の変化を実証
- [x] **ステップ3 移動伝票作成**: 成功。確保済み一覧で「移動伝票を作成する」→モーダル（移動元=readonly入力+接続ボタンでロケーション選択ポータル`modal-:r1q:`）→物流倉庫選択→「実行する」。作成後、確保済み一覧から486125行が消滅（空状態）。
- [x] **出荷指示生成確認**: 出荷管理に **#IO-1018** 生成（2026-06-20 03:37、作業ステータス=出荷待ち、引当ステータス=**引当待ち**、出荷場所=物流倉庫、1個、配送）。
- **新観察（要確認）**: 確保済み（引当済み）在庫から移動伝票を作ったのに、出荷指示#IO-1018の引当ステータスは「**引当待ち**」。移動伝票→出荷指示で出荷用の引当は別管理/別途操作が必要か？（次歩で詳細確認）
- [x] **ステップ4 出荷実績登録**: 成功（#IO-1018、出荷済み1、作業ステータス=出荷完了、出荷履歴03:44）。モーダルは配送キャリア/追跡コード任意・数量は出荷予定を全量。「登録する」で確定（不可逆）。**引当ステータスは出荷完了でも「引当待ち」のまま**（移動伝票経由の出荷指示は引当ステータス不使用と判明）。
- [x] **ステップ5 入荷実績登録**: 成功（#II-1018、入荷済み1、入荷完了、入荷履歴03:49）。受領数1（全量）→「登録する」で自動完了。出荷指示/入荷指示は管理番号リンク(/receive, /complete)で遷移。「入荷実績を登録する」「入荷指示を一括受領で完了する」は button でなく `<a>` リンク。
- [ ] **次歩**: SKU在庫詳細(486125-31-XL)で在庫区分（物流倉庫=販売可能減、ユニクロ銀座=販売可能増）の最終状態を確認し、積送中/入荷予定/取置中/販売可能の変化をFAQ/正本に反映
- [x] **ステップ6 SKU在庫詳細で在庫区分実データ確認** (2026-06-20): `/admin/inventory_items/{id}?location_id=...` で9区分を確認。物流倉庫=販売可能0/引当済0/**取置中1**/手持ち1/積送中0/入荷予定0。ユニクロ銀座=販売可能3/取置中1/手持ち4/**入荷予定+1**(=#II-1004未受領分)。
- **A-1通し完全実証完了（新事実まとめ）**:
  1. SKU検索は**機能する**（入力後Enter押下で発火）
  2. **部分引当**（1/2）でも確保分は確保済み一覧に出るが、依頼ステータスは**確認待ちのまま**（全量引当でないと確保済みステータスに遷移しない）
  3. 移動伝票経由の出荷指示は引当ステータス**「引当待ち」のまま**（出荷完了でも不変＝引当ステータス不使用）
  4. 出荷実績/入荷実績は `<a>` リンク（/receive, /complete）、buttonでない
  5. 取置中は**移動伝票作成時点**で発生（引当時点ではない＝在庫依頼.md 169行と整合）。出荷実績後も依頼管理として取置中に残る（#II-1018通しで物流倉庫=取置中1・販売可能0を確認）
- **次フェーズ**: 上記新事実を 在庫依頼.md/取り寄せ処理手順.md/在庫管理.md/VERIFIED-FACTS-ONLY に反映 → A-2（取置複数SKU再現）以降へ

### ユーザー指示（2026-06-20）
- Shopify連携等の外部連携は確認不可。「無理なものは無理のままで」→B項目は「未確認」維持、誤断定避ける
- 検証はA（書き込みで潰せる在庫・伝票系）に集中

### 制約記録の実機再確認（2026-06-20、SKU検索誤判定の教訓で正しい操作で検証）
SKU検索の誤判定を教訓に、constraints.mdの「遷移不可/無反応/未実装」系を正しい操作（a.click()/直接クリック/直接URL）で再確認。
- [x] **下書き注文「注文を作成する」（31行）= 記録通り正しい**。`<a>` で href=null、a.click() で遷移せず（URL=draft_ordersのまま）。直接 `/admin/draft_orders/create` アクセスで「予期せぬエラーが発生しました」。誤判定でない。
- [x] **会社一覧「インポート」（39行）= 記録通り正しい**。BUTTON disabled=false だが click でダイアログ0・遷移なし（無反応）。誤判定でない。
- [ ] 次再確認候補: 取引先一覧「インポート」(40行)、会計「売上実績を作成する」(43行)
- [x] **取引先一覧「インポート」（40行）= 記録通り正しい**。click でダイアログ0・遷移なし（無反応）。
- [x] **会計「売上実績を作成する」（43行）= 記録通り正しい**。`<a>` href=null、a.click() で遷移せず（URL=sale_change_line_itemsのまま）。
- **結論**: SKU検索の誤判定はあの1件のみ（操作ミス）。constraints.mdの「遷移不可/無反応/未実装」系4件はすべて客観的事実で正しかった。制約台帳の信頼性を実機で担保。

### 発注→入荷連動の再確認（2026-06-20、constraints.md 30行=要確認）
- [x] **連動しない事象を補強**: 発注伝票 `#IP-1002`（発注済み、487973-64-27、￥24,750）の詳細に**到着ロケーション欄がない**。入荷管理（未完了1件=`#II-1004`=移動由来）に発注由来の入荷指示は生成されず。
- **根拠**: 発注伝票には入荷指示生成に必要な「到着ロケーション」情報欄がなく、発注済みステータスになっても入荷管理へ自動反映されない。発注→入荷は自動連動しない（入荷指示は別経路で作成する前提と推定、ただし手動作成UIも詳細に見当たらず）。

### 調整伝票の理由再確認（2026-06-27、A-3更新）
- [x] 調整理由はプリセット4種 `廃棄 / 紛失 / 在庫差異 / その他`。2026-06-20時点の5種記録は現行実機と差分あり。`見本` / `棚卸差異` / `破損` / `検品` / `予備` は調整理由にない → 調整伝票は販売可能数の増減であり、破損等の区分への振替ではない。
- 在庫区分数値の変化（廃棄で販売可能マイナス等）は既存 #IA-1004〜1009（実施済み）で実証済み（CODEX-OPS正本）。

### A-2 取置伝票の複数SKU明細（2026-06-20/28、仕様として再現済み）
- [x] **#IR-1007 の実態確認**: 明細は **4種SKU・8行**（486125-31-XL×3行、486125-09-XL×2行、487973-64-36×2行、483457-10-L×1行）。同一SKUの重複行あり。メモ「TEST FAQ reservation quantity zero check 2026-06-18」。
- **判明**: 取置伝票は1伝票に複数SKU明細を持てる仕様。「混入」は複数商品追加の結果で**バグでない可能性が高い**（constraints.md 35行「意図しない混入」は要訂正候補）。
- [x] 次歩: 新規取置伝票作成で「商品追加」操作を繰り返し、明細が累積する（同一SKU重複含む）挙動を再現して再現条件を確定 → constraints.md を訂正済み
- [x] **A-2 再現完了（2026-06-20）**: 新規取置伝票（ロケーション=物流倉庫）で商品「参照」モーダルを開き、**複数SKU（486125-31-XL, 486125-09-XL）のcheckboxを入れて「選択する」→ 明細に2行が一括追加**。つまり1回の商品参照で複数SKU選択=複数行追加が**仕様**。#IR-1007の4種8行は商品参照を繰り返し＋同一SKU複数選択で蓄積した結果。再現条件確定、constraints.md 35行訂正の根拠強化済み。
- [x] **A-2 多明細保存確認（2026-06-28）**: `#IR-1011` で作成フォーム12明細・数量入力12件を保存し、処理済み化まで完了。詳細の商品明細テーブルは複数伝票で同じ34行が表示されたため、伝票固有明細として扱わない。

### A-1新事実のFAQ本文反映（2026-06-20）★完了
- [x] **VERIFIED-FACTS-ONLY**: 部分引当の確保済み一覧挙動、出荷指示引当ステータス引当待ち、出荷/入荷実績の操作UI を追記（70行を修正）
- [x] **在庫依頼.md**: 補足に部分引当の挙動を追記
- [x] **出荷管理.md**: 111行を更新（移動伝票経由出荷指示=引当待ちのまま）、出荷実績登録の操作UI追記
- [x] **取り寄せ処理手順.md**: フロー全体像後に部分引当・出荷指示引当ステータスの注記追記
- [x] **取置伝票.md**: 95行を更新（複数SKU明細=仕様、TODO解消）
- [x] **constraints.md**: SKU検索誤記録削除、35行（取置複数SKU）を仕様に訂正
- ※移動伝票.md/入荷管理.md はCODEX-OPS正本ベースで既存記述が十分、追加不要と判断

### faq-reviewer 最終レビュー指摘の対応（2026-06-20）★全完了
Critical3/Major4/Minor5の指摘を全て対応：
- **C-1**: 在庫依頼.md 142行「確保済みになる」断定を部分引当ケースと整合（C-2: 128行定義も整合）
- **C-3**: 取り寄せ処理手順.md 60行フロー表を部分引当ケースと整合
- **m-5**: VERIFIED-FACTS-ONLY 64行（取置複数SKU=仕様化）＋150-153（解消済み未確認事項を整理）
- **M-1/m-4**: last_verified/最終確認を2026-06-20に更新（在庫依頼/出荷管理/取り寄せ処理手順/取置伝票）
- **M-2**: 在庫依頼.md 168行リクエスト先を実質必須に整合
- **M-3**: VERIFICATION-PLAN新事実5「取置中は移動伝票作成時点で発生」に訂正（FAQ本文102/220行が正）
- **M-4**: 取り寄せ処理手順.md 155行に入荷実績`<a>`リンク（/receive,/complete）反映
- **m-2**: 取置伝票.md status: 確定化＋last_verified更新
- **m-3**: constraints.md 35行根拠に新規再現を追記

### 最終総点検（2026-06-20）★完了
- [x] **TODO残存**: 全て環境制約（チャネル接続後/実注文/Shopify接続済み）で確認不可のB項目＝「未確認」維持で正しい残置。誤って断定しているものなし。
- [x] **画像リンク切れ**: なし（各md基準で全スクショ存在。`flow-*`/`gap-*`/`verify-*` 等）
- [x] **_analysis/*.md リンク切れ**: なし
- [x] **build_faq.py 再生成**: frontmatter更新（last_verified=2026-06-20, status確定）を多軸HTMLに反映。`SQ-FAQ.html`（70記事・647KB）を09:39再生成。
- **結論**: 明日1日で理解できる資料として完成。実機確定事実のみが整合して記載、未確認はTODO/未確認マーカーで明示、リンク切れなし。Notion投入可能な状態。
