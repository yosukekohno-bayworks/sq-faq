# CRM 注文ポイント実効確認 2026-07-01

## 対象

- 注文ポイント付与ルール: `TEST_FAQ_注文ポイント付与ルール`
- 対象顧客: `検証顧客ユニクロ095318FAQ`
- 対象注文: `FAQ-ORDER-DECREMENT-SHIP-202607010132`
- 注文URL: `/admin/orders/9cb49fb1-8c41-56dd-a2e5-702d89518343_Order`
- 顧客URL: `/admin/purchasing_customers/e284db39-9ba1-587e-a3b1-aa0700076c2e_PurchasingCustomer`

## 確認結果

- 注文ポイント付与ルール一覧は `注文ポイント`、列は `ルール / 利用テナント数`。`TEST_FAQ_注文ポイント付与ルール` は `0件のテナントで利用`。
- ルール基本設定は `購入金額 100円ごとに / ポイント 1ポイント`。
- `付与までの日数（オンライン）` は `0日後`、`付与までの日数（店舗）` は `0日後`、`利用可能になってから有効な日数` は `365日間`。
- 会員ランク算出ルールは `TEST_FAQ_会員ランク算出ルール` が選択済み。
- `会員ランク倍率` タブ、`商品倍率` タブはいずれも現在は空。過去の検証用追加行は削除済み。
- 対象顧客のポイント欄は `保有ポイント 3ポイント / 付与予定 0ポイント`。
- ポイント履歴全体に表示される行はCSV一括加算 `FAQ_POINT_PLUS_20260701` の `+5` とCSV一括減算 `FAQ_POINT_MINUS_20260701` の `-2` のみ。注文由来のポイント履歴は表示されない。
- 顧客の注文一覧には `FAQ-ORDER-DECREMENT-SHIP-202607010132` が1件表示され、チャネルは `FAQ_INTERNAL_GRAPHQL_ORDER_SHIP_FLOW`、決済状況は `支払い済み`、出荷状況は `発送済み`。
- 注文詳細の右サイドバーでは `ポイント` セクションに `付与対象に含む` と `ポイント付与 0ポイント`、`会員ランク` セクションに `算出対象に含む` が表示される。

## 判断

この内部GraphQL注文はポイント計算・会員ランク算出の対象表示には入るが、管理画面上のポイント付与結果は `0ポイント` のまま。顧客ポイント履歴にも注文由来のポイント行は作られない。

正式連携SKUのShopify注文や、注文ポイント付与ルールをテナントへ実際に利用させる設定が別途必要かどうかは、この確認だけでは確定できない。
